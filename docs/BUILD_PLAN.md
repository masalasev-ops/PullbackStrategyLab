# BUILD_PLAN.md

Six phases. Each checkpoint states its deliverable, its done condition, and how it is verified. The general definition of done is in `CLAUDE.md` under "Definition of done for a checkpoint" and applies on top of everything here.

Cross-document references cite a heading, never a section number, for the same reason decisions are named rather than numbered: a misremembered number resolves to the wrong place silently.

Every phase produces two things: a screen you can open, and a generated report saying whether what was built matches the architecture. Neither replaces the other. The screen is how you notice something looks wrong; the report is how you know, and how a build session checks its own work without asking anyone.

The harness is checkpoint 1.7, built before most of what it checks exists. From there every checkpoint adds its expectations to the golden fixture, and every phase sign-off requires `tools/verify-phase` green with nothing listed as unexamined.

---

## Phase 1 — Ingest and charts

Nothing here depends on any open question. Start immediately.

**`git init` before 1.1.** There is no repository yet, and the done conditions, the progress entries and the merge rule are all written against commits.

| # | Deliverable | Done when |
|---|---|---|
| 1.1 | Solution `PullbackStrategyLab.sln` under `/src`: Core, Data, Worker, Api, Web, Tests, all namespaced `PullbackStrategyLab.*`. Clock abstraction, config binding via one shared extension, store pragmas set in that same extension, `RunLogger` as sole writer of `run_log` | `git init` done first. `tools/ci.*` green. Api has no transitive reference to Worker, asserted against compiled deps, with `PullbackStrategyLab.Tests` the one declared exemption. **The CI matrix runs windows and macos from this commit onward**. Config is wired by one shared extension used by Worker, Api and Tests alike, pinned by two tests: an environment variable overrides a value present in `appsettings.Secrets.json`, and a project starts cleanly with no secrets file on disk (see: Secrets live in a gitignored appsettings.Secrets.json, registered before environment variables) |
| 1.2 | Clock abstraction proven on both platforms | A test resolves `America/New_York` and asserts it is behind UTC by at most a day, with both bounds read from the clock. The matrix already exists from 1.1, so this checkpoint adds the test rather than the runners (see: Every line of code runs unmodified on Windows and on Apple Silicon macOS) |
| 1.3 | UniverseBuilder from the exchange symbol list, common stock only, $5 floor and the liquidity floor | `universe_member` populated. **The survivor count is measured and recorded in PROGRESS**, since the backfill call budget is one call per survivor and no figure for it exists yet. `universe_snapshot` written per night |
| 1.4 | DailyBarIngestor, one bulk request per night, idempotent | Re-running the same date changes no row. Bar count and date range recorded. A test asserts the connection reports `journal_mode=wal` and `foreign_keys=1`, since both are silently off by default |
| 1.5 | ActionIngestor, and the EMA rebuild-on-split path | A synthetic split fixture forces a full recompute for that ticker and only that ticker |
| 1.6 | IndicatorEngine: EMA 9/21/50, ADR20, ATR14, median dollar volume | Values for three hand-picked tickers match an independent calculation to 4 decimal places. **The independent calculation and its source are recorded in PROGRESS**, and those values carry forward as the fixture's first `DERIVED` expectations at 1.7 rather than being checked once and discarded (see: Every fixture expectation records how it was produced, and only the independently derived ones verify anything) |
| 1.7 | **Phase report harness** and the golden fixture: real bars for 30 tickers over 250 sessions, committed, with expected indicator values beside them, **each carrying its tier**. `tools/verify-phase` runs the pipeline over the fixture, diffs against expectations, parses the architecture tables, located by heading text rather than by position, and asserts each claim against the code, then writes `artifacts/phase-report.html` and `.json` | **Openable, and machine-readable.** Report shows three sections: doc conformance, fixture diff, coverage. Every architecture claim is pass, fail, or **unexamined**, and unexamined is not a pass. The fixture diff is broken down **by tier** rather than reported as one number, and the report states how many expectations changed since the last commit alongside how many passed. **The fixture's bars are `CAPTURED` from real vendor responses**, which the backfill at 1.6 makes possible, and the synthetic split case from 1.5 is kept and marked `AUTHORED`. **The report breaks inputs down by tier beside expectations**, and a path with no captured input reads as unexamined however many authored cases pass (see: Fixture inputs record where they came from, and a path a live run exercises needs a captured one). **1.7 back-fills expectations for 1.1 to 1.6 with their tiers**, since those checkpoints predate the fixture and could not meet done condition seven when they landed. That discharges the obligation raised at 1.1 as far as tiers reach and no further: done condition seven also asks each checkpoint for one expectation that is `DERIVED` or `CONFIRMED`, 1.3, 1.4 and 1.5 came back frozen-only, and so did 1.7 itself, so the obligation stays open against those four and is carried below. From here on every checkpoint adds its expectations to the fixture (see: Every phase ends in a generated phase report, not in a page somebody looks at) (see: Every fixture expectation records how it was produced, and only the independently derived ones verify anything) |
| 1.8 | Web shell: routing, layout, the five-item nav, the status band, and one shared candlestick component | **Openable.** Every page is reachable and renders its empty state. Nothing is stubbed with fake data, because an empty page that says so is honest and a page of invented rows is not |
| 1.9 | IndexIngestor | `index_bar` populated for SPY, QQQ, IWM |
| 1.10 | Chart page: any ticker, candles, three averages, ADR readout | **Openable.** You compare it against a chart you already trust and agree |
| 1.11 | Migration rehearsal end to end | `RUNBOOK.md` "Moving the store to another machine" executed on a second machine, all ten steps, counts matched. Result recorded in PROGRESS |
| 1.12 | Phase sign-off | Fresh session reviews. Findings batched into one pass, and `tools/verify-phase` green with nothing unexamined |

**1.11 is not optional and not deferred.** It is cheap now and it is how you discover which paths are hardcoded and which secrets do not travel, at a point where losing the store costs nothing.

---

## Phase 2 — Detection

| # | Deliverable | Done when |
|---|---|---|
| 2.1 | Four things, in this order, because the spec pass authors what the code is written against: **(a)** BUILD_PLAN's malformed obligation row and the parser that dropped it; **(b)** the spec pass, whose deliverable is a `Signals` section of SCHEMA giving every signal its formula and its source columns, plus the phase 2 underspecifications settled as decisions; **(c)** the examined floor moved from per check to per scope; **(d)** the `DERIVED` expectations owed at 1.3, 1.4, 1.5 and 1.7 | Every signal traces to named stored columns. Any that does not is a finding, not an assumption. Every check states a floor under each scope it names, with the size-of-corpus scopes marked as context and never summed with the scope carrying the property, and the narrowing that passed at the phase 1 sign-off fails. The four frozen-only permits are gone from `fixtures/expectations.json` rather than re-dated |
| 2.2 | SignalVectorizer and the frozen signal row shape, `setup_signal`, with migration 011 creating `setup`, `calibration_setup` and `setup_signal`. Plus the out-of-scope coverage naming rule, moved here from 2.6 because this is where it bites | Written once, never updated by the vectorizer. Asserted four ways: a rerun writes nothing, a restated bar does not revise a frozen value, the store's own key refuses a second write, and no `UPDATE` against `setup_signal` exists in the shipped source. Every `active` signal in SCHEMA's library is either frozen here or names the checkpoint that supplies what it reads, and that checkpoint has to exist and be open. Every out-of-scope coverage item names a checkpoint, a price, or a stated reason nothing can close it |
| 2.3 | ScanEngine, six scans, three per direction, with migration 012 creating `scan_hit` and the magnitudes shared in Core. The six thrust signals move from awaiting a checkpoint to frozen | Hit counts per scan per night recorded. Each scan takes the top 50 by its own magnitude, ranked, with ticker as the tiebreak so the boundary does not depend on row order. Every magnitude reads the adjusted basis, asserted against a split inside the month window rather than described |
| 2.4 | TierClassifier, writing a later observation of the session rather than updating the row IndicatorEngine wrote. The ladder grade moves from awaiting a checkpoint to frozen | Ladder grade on every universe member per night. The three grades are a partition, swept over every arrangement of the four numbers rather than sampled, and `mixed` is a grade rather than a bucket for names the stage failed on. A grade the store refused is counted and reported rather than absorbed |
| 2.5 | RegimeLabeler, two scores summed, with migration 013 creating `regime_daily`. The three mood signals move from awaiting a checkpoint to frozen | Both raw scores stored alongside the label, and both raw ladder counts with them. **Label filters nothing**, asserted against the shipped source rather than stated: no component outside the labeller, its reader and the vectorizer names either extreme. Neither extreme is reachable without both scores agreeing, and an unmeasurable tracker scores zero rather than minus one |
| 2.6 | LongSetupDetector, ten checks, all results recorded, with SectorResolver and ThemeClusterer behind the cluster check. The geometry and the rules live in Core so the nightly run, the calibration run and a test share one implementation. The last seven signals move from awaiting a checkpoint to frozen | `check-completeness` passes: every recorded setup has a result for every check, with the check names read from ARCHITECTURE's own gate lists and reconciled in both directions. **Per check, how many fixture setups passed and how many failed, with any one-sided check named individually in the report**; more than two one-sided is decided at this checkpoint rather than deferred |
| 2.7 | ShortSetupDetector, ten checks, with the three that are not sign flips written as their own rules. Plus the two things all twenty gates first make possible: the degeneracy proof over the gate list, and the authored boundary suite that closes the one-sidedness raised at 2.6 | Same as 2.6, both directions. No setup row carries a direction its detector does not own. A name a detector cannot read gets an error row and the run is recorded partial, rather than being skipped into a night that merely looks lighter. Every gate on both lists fails on an input that makes its quantity absent, zero or undefined, proved over the gate list so a gate added later inherits it and a gate with no degenerate case fails the proof. Every gate has a pass and a fail from a case built to sit either side of its own threshold, no check reported one-sided, and the boundary cases marked `AUTHORED` so nothing reads them as evidence about the market. `reached-ceiling` runs two of its three clauses and the third is out of scope naming 4.4 |
| 2.8 | SetupCapper, verb `cap`, 60 a night, 40 long 20 short, unused slots released, with the arithmetic in Core so the release rule can be swept rather than sampled | Truncation recorded with the pre-cap count, so a night can say whether the cap bound rather than only what it kept. The release rule holds over every arrangement of the two counts up to twice the cap, and which side is offered the spare first never changes the answer, which is what makes the absent tiebreak a property rather than an omission. Ranked within a direction and never across, on give-up distance ascending with ticker as the tiebreak. **The cap applies to the shared candidate list**, asserted now while there are no versions: `setup` carries no column naming one, and no public read of it takes a name of any kind |
| 2.9 | Setup inspector, the gallery page: prev/next, filter by failed check, agreement capture, with `LabSetups` in the Api answering a night both directions and taking the one write the read surface makes | **Openable.** You page through a night's setups by keyboard and record agree or disagree per setup. The two sides are two lists on the wire and two blocks on the screen. Every check appears on every card, passed and failed alike, and a check handed nothing shows what was absent rather than a blank. The thumbnails come from the one shared candlestick component. The recording is a form post and works with the script removed, which is what makes the keyboard paging a convenience rather than the mechanism. A filter that hides everything says it hid it rather than showing an empty night |
| 2.10 | `point-in-time` as a named CI step, three halves: the readers' signatures, the statements written by hand beside them, and the behaviour | A deliberately future-dated observation causes a loud failure, in CI rather than at runtime, and the test is permanent rather than a manual break-and-revert. Every public read on a store reader takes a date, with the one calibration read exempted by name and its reason. Every hand-written statement selecting from a table that carries an observation stamp bounds that stamp, or is exempted by file with its reason. And the same session read from both sides of one correction's instant returns two different figures, because one figure alone is satisfied by a row that is simply missing |
| 2.11 | One-time calibration. Calibration mode carries the session in memory: each name's figures through `IndicatorEngine.Calculate`, its ladder through `TierClassifier.Grade`, and the session's six scan rankings through `ScanMagnitudes` and `ScanEngine.Top`, all four shared with the nightly stages that own them. The market-cap clause of `tradable-shortable` is exempted by name. The run over the golden fixture becomes the fixture's expectations; the distribution a threshold is read from comes from the live universe | Detector rows **into `calibration_setup`, never into `setup`** (see: The evidence store holds only setups flagged forward, never setups reconstructed from history), asserted by comparing the evidence store before and after rather than by asking whether it is empty, and asserted again on a rerun of the same range. **The fixture's thirty names cannot answer for a threshold and the record says why**: thirty candidates against a scan breadth of fifty puts every name inside every scan on every session, so every geometry check fails on every row and the count is nought by construction. The distribution is recorded per side as a raw count and as a rate per name per session. If the median falls outside 5 to 60 per side, the thresholds marked "phase 2 count check" are adjusted **once**, before phase 3, recorded as a dated event with before and after counts and naming which distribution it was set against. **Unless the run shows the band is out of the five thresholds' reach**, in which case the finding is recorded with the pass rate of every check and the distribution of every threshold's own quantity, and the adjustment waits on a decision: spending a once-only adjustment on a threshold set that cannot reach the band spends it for nothing, and the condition that fires it was written assuming a nudge would do. Every short verdict a calibration run writes records that the cap clause was exempt, and a forward night still fails a name with no resolved capitalisation |
| 2.12 | Phase sign-off | Fresh session. The gallery review is part of it, and `tools/verify-phase` green with nothing unexamined. **Plus one reading pass over what the fixture's composition cannot be evidence about.** `thrust` is the worked example: thirty fixture names against a scan breadth of fifty puts every one of them inside every scan on every session, so the check's own quantity is nought on every fixture row and the authored gate cases carry its threshold alone, while the live calibration run reads it at 100% only because it is one of the four checks in the recording floor. Both instances of that shape so far were found by an accident in a different checkpoint rather than by anything looking for them, so the pass asks which other checks stand where this one does and records the answer whether or not it is acted on |

**2.11 is calibration, not tuning.** At this point no forward return exists anywhere in the store, so there is nothing to fit toward. It is a row count and nothing else.

**And the rows it counts are not evidence.** A historical detector run has no record of who was listed on those dates, because the nightly universe snapshot only starts when the lab does. Delisted names are absent, so every reconstructed setup carries survivorship bias. Counting is unaffected; measurement would be destroyed. The rows go to `calibration_setup` and the evidence store stays empty until the first forward night. After phase 3 begins, these thresholds move only through the normal proposal route.

**2.9 is where the real time goes,** and it should. Paging through the gallery and disagreeing with the detector is the actual transfer of the strategy into code.

---

## Phase 3 — Measurement, and the decision point

| # | Deliverable | Done when |
|---|---|---|
| 3.0 | Seven things, in this order, because the instrument has to exist before the thing it measures and the spec pass authors what the code is written against: **(a)** the geometry instrument, being `DERIVED` expectations over `PullbackGeometry.Of` on non-degenerate inputs; **(b)** the thrust scan recorded on the setup row; **(c)** the thrust-window correction, alone, and the prediction settled; **(d)** the surfaces sweep; **(e)** the remaining hygiene obligations; **(f)** the spec pass settling control draw, ceiling arithmetic and the interval method as named decisions, plus the SCHEMA repairs phase 3's tables need; **(g)** a check result carrying a value per clause | Every obligation due here is discharged or repointed with its reason, and none names a checkpoint whose deliverable is not its work. (a) lands before (c), and (c) moves the geometry and nothing else, because a pass that moves the geometry and the thresholds together can say nothing about either. **The prediction is settled by the calibration re-run and recorded in PROGRESS, never encoded as an assertion**: the fixture cannot produce a claim about 2,016 names over 631 sessions, and a threshold in the suite saying the median retrace is below 1.0 is a number a later session adjusts to make green. **The once-only threshold adjustment is not spent here whatever the prediction does**, because a nightly count five hundred times below the band's floor argues for a second wrong quantity rather than for thresholds set too tight, and the once cannot be re-spent |
| 3.1 | SetupJournal finalised | Setup rows immutable after write, except by a correction the lateness bound admits, and asserted in both directions: nothing rewrites a plan or a gating verdict, and a correction whose input arrived more than the bound after the setup's own session is refused (see: A late answer is attributed to the session it was fetched for, up to a recorded lateness bound) |
| 3.2 | ForwardReturnFiller: 1, 3, 5, 10 sessions, signed by direction, plus MFE and MAE in ATR | Holiday handling correct: next trading day used, actual date stored beside the intended horizon |
| 3.3 | ControlSampler: loose and tight control sets per flagged setup | Controls drawn from names that cleared the liquidity floor and were not flagged. Match quality recorded |
| 3.4 | CeilingCalculator | Bound computed from the actual outcome distribution, recomputed weekly |
| 3.5 | ScoreboardBuilder and the Lab scoreboard page, bands 0, 1 and 2 | **Openable.** Flagged against both control sets with confidence intervals, rank-decile curve, ceiling gap |
| 3.6 | **The decision point, per direction. Parked, not a sign-off condition, and it gates admission rather than construction** | **A direction's gate opens on that direction's own panels and on nothing else.** Both band 1 conditions reached, which is at least twenty sessions **and** at least 262 effective observations, on both control sets for that direction. Long clearing opens the long side and licenses nothing on the short side, and the reverse. **This is one gate read twice rather than two gates**: the conditions, the thresholds and the never-pooled rule are unchanged, and what is corrected is a row that read as four panels all clearing when the decision it cites already said the gate holds for the direction concerned (see: 3.6 gates what may be admitted, not what may be built) (see: Long and short are never pooled into one figure). Then a person reads that direction's panels and decides whether to continue on it. **What it holds back for a direction that has not cleared is admitting a variant, changing a rule and spending a holdout window; it holds back no construction at all.** **Short's twenty-session count begins on the first night the anchored clause runs against minutes covering that row's own swing, and the nights before it are not counted toward the short side's gate.** The condition is a property of the store rather than a checkpoint, and it is read off the rows: `vwap_run.anchors_priced` says how many anchors a night could price and `ShortPullbackRules.ClauseSetOf` says which clause set each stored verdict ran, so the first night either is non-nought for the anchored set is the start, observed rather than declared. It read "which is 4.4" until that checkpoint landed and measured the difference: **4.4 builds the clause, and building it is not the same as running it.** The level is a volume-weighted average over minute bars from the swing forward, the fetch buys one session a night per flagged name, and a swing sits three to twenty-seven sessions back, so the clause has a level to read only once the store holds minutes back to a row's own swing. On the night 4.4 landed the store held none at all. The seam is therefore read off the rows rather than off a date, which is what it always said, and the checkpoint that made the count possible is not the night the count starts. `reached-ceiling` is a three-clause disjunction and the third clause needs the anchored volume-weighted average VwapEngine computes, so it has run on no night yet. **A disjunction missing a disjunct is strictly harder to pass**, so every short row recorded before 4.4 came from a detector narrower than the document describes, and a count taken over those nights is not a count of the gate this condition names. **The long side is unaffected and its count stands**: no long check is waiting on anything, its twenty sessions run from the first forward night as they always did, and nothing here moves a long figure or a long threshold. **The seam is in the store rather than in this sentence**: every `reached-ceiling` verdict records the clause set it actually ran and `ShortPullbackRules.ClauseSetOf` reads it back off a stored row, so somebody opening the store sees which gate produced a row without knowing this paragraph exists. There are four such sets from 4.4 and they are different facts: the clause had not been built, it was built and the store could not reach the swing, the row is a reconstructed session no minute bar exists for, or the full disjunction ran. Only the last is a session of the evidence this condition counts. The date is deliberately not the discriminator, because a row recovered late or replayed from an older checkout would be classified by when it was written rather than by what produced it. **Nothing is disabled and this is not permission to disable anything.** ShortSetupDetector keeps flagging, ControlSampler keeps drawing its controls and ForwardReturnFiller keeps filling outcomes, on every night between now and 4.4, because short's evidence cannot be reconstructed and a night not recorded is gone for good. What waits is the counting, not the collecting. Due when the panel reports both for the direction concerned, which the panel says every night (see: The minimum sample is 262 effective observations, ratified at two points and 90% power) |
| 3.7 | Phase sign-off | Fresh session, and `tools/verify-phase` green with nothing unexamined. **Signs off 3.0 through 3.5 and does not wait on 3.6** |
| 3.8 | **The correction pass, after the sign-off and before accumulation runs on.** Seven parts, in this order because the diagnostic has to survive before anything is diagnosed by it: **(a)** the slot script's lost stderr, with a check and the runner job that exercises it; **(b)** one call spent on the name that threw, captured as the fixture's first failing-shape response for `fundamentals`; **(c)** SectorResolver skipping a bad name rather than dying on it, with the skipped count in the run log; **(d)** the sector walk run to completion and what the failures have in common; **(e)** the correction rule, superseding immutability as it was written; **(f)** CheckRecomputer and the fifteen setups an input stage's death left null; **(g)** the obligations this pass raises rather than takes | A stage that writes to stderr and exits non-zero puts both its message and the line saying the slot stopped in the night's log, proved by a real stage denied a real precondition on a runner rather than by hand, and under the interpreter the registered tasks actually execute. One ticker's failure costs that ticker and no other, and a run that spent calls without product is not recorded as clean. **The repair is bounded to inputs the night had**, asserted rather than trusted: a recompute reading a value stamped after the setup's own date fails and says so, and a corrected row records that it was corrected, with the date and the reason, so a later reader can exclude corrected rows. **What stays forbidden is unchanged**: no trigger, stop, size or gating verdict is ever rewritten |
| 3.9 | **The post-pass, in run order, before phase 4 planning resumes.** Nine parts: **(a)** the three questions the repair owed, being the population its cluster was formed over, what lateness is measured from, and what forty-four is; **(b)** the merge, and the nightly job pointed at the branch it merges to; **(c)** the session boundary, closed at the end of the session's own day in the session zone, separately from the conflation it is not; **(d)** `scan_hit` given an observation stamp, with the blast radius of refusing a null measured before any bound moves; **(e)** an in-place rebuild that writes nothing refusing rather than reporting success; **(f)** a claim for every behaviour change this pass and the last one made; **(g)** the obligation ledger reconciled move by move against its commits; **(h)** the session-date migration scoped and dated rather than started; **(i)** 2.11's funnel reported with a denominator per stage | The cluster a repaired row receives is asserted to be formed over the night's whole scan population, by a test in which a member with no setup row at all is counted. Lateness has one stated origin, the session's own end of day, and every figure in the corpus measured from anything else is corrected. **No point-in-time bound is built by appending a UTC literal to a session date**, asserted by a claim that fails if one returns and by a behavioural test that fails against the old expression in January and in July. `scan_hit` is stamped going forward and the count of existing rows and the reads that would refuse them are reported before the bound changes. A rebuild that writes nothing exits non-zero naming what it skipped. Every behaviour change is mapped to the claim asserting it or to a recorded reason it is not an architecture claim, one line each. The obligation count reconciles line by line with a commit against every move |
| 3.10 | **The verification repair, in dependency order, because every part after the first is verified by it.** Eight parts: **(a)** `tools/ci.sh` reporting a failing step's own exit code, and each shipped step function run against a command that fails; **(b)** `point-in-time` reading the store readers it excludes, and the unbounded reads that turns red; **(c)** the two session bounds still built on a fixed UTC offset, and the guard whose two patterns could not match them; **(d)** `carried-obligations` reconciling a live tail that is not empty by construction; **(e)** the claims that pass by comparing nothing, counted apart from the claims that pass; **(f)** `bar-append-only` and `writer-ownership` reading migration SQL, where every table rebuild in this project lives; **(g)** the scope floors re-recorded against a run rather than left where 2.6 put them; **(h)** the shipped-code defects (a) through (g) reveal | **A failing step fails the run, on both scripts, proved by running the shipped function rather than by reading it**, because a scan for the corrected shape passes against the broken one. Every public read on a store reader is read by `point-in-time`'s statement half, and a read that names its stamp without bounding it fails: naming the column in a `SELECT` list is not a bound. No point-in-time bound is built from a fixed UTC offset in any syntactic form, asserted by a guard that fails against the constructor form and the concatenation form alike. `carried-obligations` states the count it reconciled and that count carries a floor, so a filter that empties itself fails rather than passes. A claim decided by comparing two empty collections is reported as unexamined rather than as passed. Every floor in `fixtures/checks-baseline.json` is re-recorded from a full run on the date the file states, and the file states the date of that run. **Each defect (h) discharges is fixed with the check that would have caught it, in the same commit**, so no part of this checkpoint lands a repair whose guard arrives later |
| 3.11 | **The findings 3.10 recorded and did not start, in the order the harm runs.** **(a)** the setup row expressing an absent trade geometry as absence rather than as nought, and `SignalVectorizer` freezing no signal the detector recorded as absent; **(b)** the `degraded` clause of the vendor-ceiling rule, which has no column anywhere; **(c)** the status band reading one `run_log` row of about eighteen; **(d)** the decile panel's denominator, which is pooled where the rank it divides is per direction; **(e)** `CeilingCalculator` given the tests it has none of | **A quantity the detector recorded as absent is absent in the store, in the frozen signal and on the screen**, asserted by a setup whose geometry is degenerate carrying no geometry signal and no figure derived from one. The fixture already holds such a row, so this is asserted over the golden fixture rather than over an authored case alone. **A stage that stopped short marks the setups it degraded**, and a read that reports a night's outcome reports the worst outcome of that night rather than the last one written. **The decile denominator is the direction's own allocation**, and the panel states which. Each part lands with the check or the test that would have caught it |
| 3.12 | **The sign-off's findings, in the order the harm runs.** **(a)** a stage refusing to run against a store at a version other than the build's, and the band saying so; **(b)** the status band's night, bounded in the session zone rather than on the UTC date; **(c)** the vendor-ceiling claim asserting the third of its three clauses; **(d)** the two rendered statements 3.11 added and no surface claim held; **(e)** the night of 2026-08-28 recovered from the inputs it already had, or recorded as lost with the reason; **(f)** the seam 3.11(f) dated to a session on which nothing was flagged, corrected | **A stage that needs a column the store has not got refuses before it opens the store, naming both versions**, proved by standing a store up one migration short and running a detector against it, with the three exemptions asserted rather than listed. **A night is the night in the session zone**, proved over run rows on the installed schedule, which crosses UTC midnight: a test population that does not cross it cannot tell the two bounds apart, and that is why the ones written at 3.11 did not. **The vendor-ceiling claim asserts every clause of the sentence it names**, being the third as well as the two it already had. **Amended from the universal form, and the amendment is named as one in the PROGRESS entry.** It read "every clause of a corpus sentence a claim names is asserted by that claim, or the claim says which clause it does not reach", which is the whole corpus where (d) beside it is scoped to one checkpoint's statements. Measured before choosing: 40 of the 76 live claims name a corpus sentence of more than one clause, 13 of 14 in Failure behaviour, 12 of 25 in the Component catalogue, 6 of 6 in Build order, 6 of 6 in Running on Windows and macOS and 3 of 3 in The phase report, about 160 clauses in total. Each needs its clauses classified as assertable or as rationale before anything can be asserted about them, and that classification is judgement no check can make, so it is a checkpoint's work rather than a part of one. The sweep is carried. Every statement 3.11 put on a page is named in `fixtures/surface-claims.json` and read off the rendered page. **The recovery is bounded to inputs the night already had** and writes no row for a session whose inputs were not present, and what it produced is recorded against what the first night produced. A record corrected by a new dated entry, never edited |
| 3.13 | **The review's findings, in the order the harm runs.** Three parts: **(a)** the phase report written whole or not at all, which its own doc comment already claimed; **(b)** `recheck --restore` honouring the check it validates and discards; **(c)** `recheck`'s date stopping being positional | **A failure rendering the page leaves both files exactly as the previous run left them**, asserted by making the render throw rather than by reading the method, because the property is an ordering and a scan for the corrected shape passes against the broken one. Both payloads are produced before either file is touched and both are moved into place, and the alternative of writing the page first with the JSON as a marker is rejected in the source with its reason. **A restore scoped to one check leaves another check's corrections standing**, asserted over the case that will exist rather than the one that does: a second check admitted in the test, rows corrected under both on one date, one restored, the other's corrections read back. **Every documented ordering of `recheck`'s arguments parses to the same command**, and no flag's value is eligible to be taken as the date, asserted rather than fixed by naming `--check` again. Each part lands with the test that would have caught it, and the two in (a) are defects in code committed by the session that signed 3.12 off, so this checkpoint is where the read that session's own commits never had is discharged |
| 3.14 | **The completeness review's findings, in the order the harm runs.** Eleven parts: **(a)** `fixture-replay`'s permit guard, whose four clauses were split across two loops and whose due-point clause reached no permit that exists; **(b)** `CheckRecomputer` recomputing `cluster` from a hit the detector did not read; **(c)** the effective-observation count, computed for a row-weighted mean beside a night-weighted estimate; **(d)** `surface-claims` declaring a source scan it does not perform, backed by a test that compares two of its own constants; **(e)** `tools/ci.*` deleting the store under a pre-set data root; **(f)** `tools/verify-phase.ps1` choosing the Windows Subsystem for Linux launcher, and its own refusal unreachable; **(g)** `tools/verify-phase` leaving one report part uncleared; **(h)** the phase report's page listing a voided expectation as one that did not hold; **(i)** the corpus corrections (a) through (h) reveal, and the rows the rest of the review raises; **(j)** `carried-obligations` reading four of the six forms the record writes a due point in, and the obligation that went unscheduled behind the two it could not read; **(k)** the pointer naming which checkpoint the build is on, which two rules in CLAUDE.md could not both leave where it was | **Every clause of the permit guard is applied to every permit**, asserted over the population every live permit is in rather than over the one the proof tests happened to build, and the coverage record names each permitted checkpoint rather than reporting the set it cannot see as complete. **A repaired verdict is the number the night's own detector computed**, asserted by a case where the largest count the name carries is not that number, which is the case the live store already held. **A figure states the estimator it describes**: the effective count is asserted over a series whose nights differ in pair count, which no series in the suite or the fixture could express. **A check that reads no shipped source declares so**, and the assertion it makes is proved by running it rather than by a test that compares two literals. **The two entry points set the data root rather than defaulting to it.** **The Windows wrapper rejects a bash that cannot run the script and reaches its own exit code**, proved by running it rather than by finding the code in the text. **A due point is read in every form the record writes it in**, asserted by running the parser over a record holding all of them rather than over hand-built values, which is what the existing proof did and is how the parser stayed unexercised. **The pointer is the furthest checkpoint the record holds rather than the last entry written**, asserted over a set a dated correction would produce rather than over the live record, which holds whatever it holds today. The obligation the unread forms hid is a row in the table, and the floor under the scope rises to the count the widened pattern reads, because a floor above a scope that never held the property holds nothing. Each part lands with the test or the check that would have caught it, and every part proved red before green by removing the thing it guards |
| 3.15 | **Phase sign-off, covering 3.8 through 3.14** | Fresh session, and `tools/verify-phase` green with nothing unexamined. **This row is what 3.13 deferred its figures to and what the plan did not have.** Every other phase's table ends in its sign-off; phase 3's ended at 3.7, whose own done condition scopes it to 3.0 through 3.5, and six checkpoints landed after it with no row scheduling a pass over them. 3.8 through 3.11 were read by the sign-off of 2026-08-29 and 3.12 by the one after it, so what is new here is 3.13 and 3.14; the row covers the range rather than the remainder, because a sign-off that names a subset is how the last gap opened. **Does not wait on 3.6**, on 3.7's reasoning |

**3.5 was reopened on 2026-08-27 by the sign-off review, before the nightly job's first run.** Three defects blocked it and all three are fixed under this heading rather than as a new checkpoint, because none of them adds a deliverable: each is a stated behaviour of 3.2, 3.4 or 3.5 that the code did not have.

- **Nothing ever wrote a control forward return.** ForwardReturnFiller bound `subject_kind` to the literal `setup`, so band 1's difference series was empty on every night, `n_effective` was pinned at nought, and 3.6's trigger could never fire. 3.5's done condition asks for the flagged population measured against both control sets with confidence intervals, and no interval could ever be produced.
- **PairedInterval was not a bootstrap.** Every draw was one lattice rotated, so ten thousand draws was bit-identical to N draws and the intervals ran two to three point seven times too narrow.
- **A positive adverse excursion was read as its own size.** A subject that never traded against its entry was judged stopped out, which removes from the ceiling exactly the rows a perfect forecaster selects.

**The sign-off that found them did not fix them, and the session that fixed them cannot sign them off.** Both halves are the fresh-session rule.

**3.6 is parked, and 3.7 does not wait on it.** The two ask different questions and only one of them is answerable today.

**3.7 signs off that 3.0 through 3.5 hold and meet their done conditions.** That is a claim about code, documents and a fixture, it is what `tools/verify-phase` was built to answer, and it is verifiable now (see: Every phase ends in a generated phase report, not in a page somebody looks at). **3.6 decides whether the pattern works**, which needs a sample that does not exist yet and which no report can produce. Holding the sign-off on it would hold finished, reviewable work behind an accumulation nobody can hurry.

**Phase 1 is the precedent and it is the argument.** It signed off with the `CONFIRMED` indicator values outstanding, on the reasoning that an obligation whose due point moves at every sign-off is permanent while reading as pending. The same reasoning applies here in the other direction: a checkpoint that cannot be reached for months, left as a sign-off condition, makes the sign-off itself permanently pending while every part of it that could be checked already passes.

**What parking does not change, said outright so it is not read as dropping.**

- **3.6 still has to happen, and it gates admission rather than construction.** It is the project's own question. Building the trading layer, the variant machinery and the research loop does not wait on it, because none of that apparatus encodes the baseline's thresholds; admitting a variant, changing a rule and spending a holdout window do wait on it, for the direction concerned (see: 3.6 gates what may be admitted, not what may be built).
- **Its trigger is measured rather than waited out.** Both band 1 conditions, reported by the panel, not a date and not a judgement about whether enough time has passed.
- **The panel is what fires it.** Band 1 shows its raw count, its effective count and the minimum every night, so the trigger arrives on the screen rather than in somebody's diary. A trigger a reader cannot see is a date in disguise.

**Two conditions, and both are needed because they are settled by different things.** Twenty sessions is what the block bootstrap needs before an interval exists at all, and no number of rows substitutes for a session. 262 effective observations is what the decision needs, and no number of sessions substitutes for the evidence. A panel can reach one and not the other, so the trigger names both.

**What has to happen before 3.6, none of which is code.**

1. **Schedule the nightly job.** Scheduling lives outside the application, so no checkpoint starts the clock, and nothing accumulates until it runs. The one item whose absence stops everything, and it is carried as an obligation rather than left in this list.
2. **Accumulate**, against the branch the phase lives on if 3.7 has not merged it, otherwise against `main`. Band 1 fills from about eighty-two flagged setups a night; band 2's decile curve fills from candidates and at the calibrated thresholds it will stay near empty, which is a finding rather than a fault.
3. **Rule on whether the tight control set may draw from neighbouring sessions**, before 3.6 rather than after, because the tight comparison is the number 3.6 turns on. Carried as an obligation.
4. **3.6 itself**, once the panel reports both conditions reached.

**How long step 2 takes is not known and is deliberately not estimated.** The effective count is the row count divided by the measured design effect and discounted by the measured serial overlap, and neither has been measured against a real series. If the pairing removed most of the common movement the trigger arrives near the twenty-session floor; if the ten-day overlap dominates it is months. That is a wide range, it is the honest one, and the panel closes it within about three weeks of the job running.

**Step 2 used to say "three months" and no longer does, and the reason is worth keeping.** That figure was written before anything was measured and read as a derived quantity ever since. It was not: it rested on about twelve setups a night, band 1 as built fills at about eighty-two, and it is a paired comparison against same-night matched controls, so the market factor that dominates cross-sectional correlation cancels rather than needing to be waited out.

**A calendar is not restored here later.** If the effective count climbs slowly the answer is that the evidence is thin, which is a finding about the strategy rather than a reason to read the scoreboard anyway on a date. If it climbs fast the decision arrives early, which was always the point of measuring instead of estimating.

**The threshold ruling is not on this path and that is deliberate.** It was, while it was unclear which population band 1 measured. `ARCHITECTURE.html` settles that: its worked night is twenty-two flagged, fourteen passing every check, and all twenty-two followed up, so the evidence population is the flagged one and band 1 fills whatever the thresholds do. What the thresholds decide is whether anything is ever *traded*, which band 2 reports and phase 4 acts on.

**3.6 is the project's own question, answered.** If the tight-control comparison does not clear zero, the pattern has nothing in it beyond owning stocks in uptrends and everything built after this point would be optimising noise. If the ceiling gap is near zero, selection has no room and the loop should point at execution instead. **Either answer is worth having, and phase 4 does not wait for it.** 3.6 gates admitting a variant, changing a rule and spending a holdout window, for the direction concerned; it holds back no construction at all, and the caps, the fill model, the variant machinery and the research loop are indifferent to which rule they carry (see: 3.6 gates what may be admitted, not what may be built).

---

## Phase 4 — Trading

**The phase is seventeen checkpoints, and the order of the rows is the order of the work.** Three were added when the phase was planned on 2026-08-31: the corpus corrections at 4.14, the decisions at 4.15 and PlanBuilder at 4.16. **A fourth was added at 4.6**, being 4.17, which holds the thirteen verification defects that had piled up at 4.6 and takes its place in the order before the sign-off rather than at the end of the numbering. **Existing identifiers are not renumbered**, because 4.1 to 4.13 are cited by number in the carried obligations table's due points, in `Navigation.cs`'s `ArrivesAt`, in the phase report's out-of-scope groupings and in `fixtures/checks-baseline.json`'s deferrals, and `CheckCoverage.DeferralProblems` fails a deferral naming a checkpoint that does not resolve. So the number is an identifier and the table's order is the build order, which this table already did when it put 4.2 and 4.3 ahead of 4.1; the three new numbers ascend in that order. **None of them is 4.0**: at 2.1 and at 3.0 that identifier named the pass that opens its phase, neither of these does, and a number saying opening on a row that is not the opening is the same fault as a commit subject naming a checkpoint it does not build.

**A row names a component only where that row is the checkpoint that builds it.** `Schedule.CheckpointFor` answers when a component is owed with the earliest checkpoint row whose text contains its name, and `architecture-conformance` asserts the catalogue's claim about it from that checkpoint onward. A row mentioning a component in passing therefore brings its claim forward to a checkpoint that does not build it, and the claim fails on the day that checkpoint lands. Two rows below name a component by description instead, and each says where it is doing it. This is stated here because the avoidance reads as an omission, and the next session to notice it would helpfully undo it.

**Every figure this phase produces is over an authored population, and it is stated once here rather than on sixteen rows.** The funnel passes a median of nought candidates a night on both sides, ruled on 2026-08-30 to be held until 4.4 rather than adjusted, so no checkpoint from 4.5 onward will have a live row to exercise it. Each done condition names the population it is verified over, and **nothing in this phase may assume a trade will ever fire.**

**The night's budget after this phase is the data budget's own total, because both stages this phase starts are already rows in that table**: minute bars for every flagged setup and the spread snapshots. Nothing here adds a row to it, so the figure and its range are ARCHITECTURE's and are not restated. What a build session needs from it is stated there too and is a consequence rather than a number: the spread snapshots spend their calls during the session, ahead of the largest consumer, so on a night that reaches the ceiling the stage that stops short is the one storing the night's bars rather than the one that overspent.

| # | Deliverable | Done when |
|---|---|---|
| 4.2 | IntradayFetcher, every flagged setup | **First, because a minute bar not captured on its own evening cannot be bought later**, and minute bars are the only unrecoverable input to the session loop this phase builds. **The fetch is for the session the plan was live in, never the session it was written on**: this stage runs at 20:30 because minute bars publish two to three hours after the close, while detection runs at 18:20 and the plan is written at 18:30, both for the *next* session, so on the evening of session N it stores session N's bars for the setups flagged on the evening of N−1. The first night has no prior evening and fetches nothing, recording that it fetched nothing rather than falling back to tonight's setups. **Asserted rather than described**, fail-closed as a point-in-time read is: a stored bar's session is provably later than its setup's own, and a fetch aligned to the setup's own session fails. The golden fixture holds one session, so it is the population that proves it, and the assertion cannot pass by finding nothing. **The fixture's minute-bar day was attempted on day one and the answer is recorded.** It is the session after the fixture's as-of rather than the as-of itself, since bars for the as-of day resolve nothing. One live call on 2026-09-01 for `intraday/AAPL.US` over 2026-08-25 returned **959 minute bars**, so that session is inside the vendor's intraday horizon and the fixture's minute-bar day is 2026-08-25. The forward-pair consequence this row carried does not arise. **What one call establishes about the horizon is a lower bound and it is stated as one**: seven calendar days as at 2026-09-01, which is the age of the session that answered. The horizon decays daily and the next question about it costs another call. Two things the capture settled that were reasoning until then: the response carries **390 bars between 09:30 and 16:00**, which is the session length derived rather than stated, and it carries **569 more outside them**, from 04:00 to 19:59, which is why the stage stores every bar and labels it rather than filtering to the regular session. **Which names is settled here**: this row said "all 60", the capped set, where SCHEMA and (see: Minute bars are fetched for every flagged setup, not only the planned ones) say every flagged setup, and the budget turns on it. **Measured rather than assumed**: the population is distinct flagged names, and the three nights the evidence store holds, 2026-08-27 to 2026-08-31, flagged 44, 73 and 83, so the row is 220 to 415 at 5 calls each rather than the flat 300. The old figure was wrong in both directions, not only low: it assumed the capped sixty, which is neither the population nor a constant, so the nightly total moves to 2,723 to 4,118 rather than upward from 2,803. `intraday_bar` gains columns **and types**, with the bar interval, the session window and the price basis each stated, because each moves the average price, the day's extremes, and whether a gap is a gap. `bar-append-only` extends to it, the guard already written for that firing when the table first appears. A new vendor endpoint owes a `CAPTURED` input. **A partial fetch is defined** rather than left to be discovered. Migration 037, the first since the phase 3 sign-off, which is what woke the three obligations due here. **Two are discharged**: `tools/nightly.ps1` now refuses to dispatch a slot from a tree that is not on `main`, with the `-AllowBranch` escape the 3.6 attempt lacked and was removed for, proved by running it on this branch and watching it exit 4 with no stage run; and the status band no longer states positively that the schema agrees when the read surface did not answer, nor collapses a store ahead of the build into a store behind it. **The third is restated and repointed to 4.6**, its two other halves discharged and its `ControlSampler` half not reproducible against the code as it stands |
| 4.3 | SpreadSnapshotter, twice per session | **The second unrecoverable input, and the harder of the two.** A minute bar can be bought for some days after its session; a quote cannot be bought at all once the instant has passed, because the vendor publishes no history of the book, so a session sampled nought times has no spread for ever. **The reader is named, and it is entry slippage at 4.7.** Question 1 is answered and slippage takes the spread form; the fraction, and whether it is symmetric between the directions, arrive with the other ten and are 4.7's to apply. It is named at the capture rather than discovered at the consumer because until this row `spread_snapshot` was the one store SCHEMA declared with no reader anywhere in the solution, and a capture spending 120 unrecoverable calls a session on an input nothing consumes is one nobody can justify. Nothing here computes a slippage figure. **The endpoint had to be established rather than assumed, and the first answer was wrong.** A probe of `real-time/AAPL.US` on 2026-09-01 answered with open, high, low, close, volume, previous close and change, and no side of the book at all, which would have made the whole capture impossible on this vendor. The route that carries it is `us-quote-delayed`, confirmed live on the same day: `bidPrice`, `askPrice`, their sizes, and **a stamp for each side**. The two stamps differ, by 32 seconds on AAPL on the capture of 2026-09-01, which is one name on one response and is stated as one rather than as a rate, so a spread is a figure taken across two instants and the store keeps both. **That property is an input to 4.7 and is stated there**, since applying a fraction to a figure taken across a gap is a decision the fill model owes rather than a note the capture can make. The feed is delayed by design, so **the lag is recorded rather than corrected for** (see: A delayed quote records its own lag rather than being corrected for it). **The two clock times are authored here, being the whole of the sampling design and stated nowhere.** **10:15**, past the opening auction whose quotes describe an event rather than a name, and inside the first hour, where a pullback trigger most often fires. **15:45**, late enough to catch a book that widened through the day, which is exactly the property deciding whether a tight stop is meaningful, and outside the closing auction. Two and not one, and two and not three, is a decision rather than a preference and is recorded as one (see: Two spread samples a session, not one and not three). **The 120 is derived from them**: the capped sixty at one call each, twice. The endpoint takes a batch and prices it **per name**, so splitting a pass into two requests or thirty moves the round trips and not the figure, and a budget charging a batch as one request would let one call spend sixty and report one, which is the accounting error the ceiling exists to catch arriving from the one direction no other endpoint can produce. **It reads the capped setups `SetupCapper` writes**, from the store and not the watchlist: the watchlist arrives at 4.1 after this row, and a component reading a page rather than a store would breach the read surface. **It carries the same offset 4.2 settled and cites the same decision rather than restating it** (see: Minute bars are fetched for the session a plan was live in, never the session it was written on): it runs inside session N over the names capped on the evening of N-1, whose plans are live in the session it is running in, and a pass aimed at its own evening refuses through the same type. **Failure behaviour gains a row, and the three cases are three answers.** One pass missed is **degraded**, a real answer resting on one observation and one nothing later can raise to two. Both missed and the read **refuses**, because a fill charged no slippage on a session nobody measured is the silently wrong result, and no fill is a different answer from cannot-say. Some names missed is **partial** with the count, per name rather than per session. All three are legible only because a pass writes a `spread_pass` row whatever it did, so a session nobody sampled is absence rather than a quiet result. **A defect found by its own test rather than by review**: a batch is charged whole, so a fixed twenty asked against a remainder of fifteen was refused entire and fifteen buyable spreads were lost with the budget to buy them sitting unspent. The last batch is trimmed to what the ceiling can still cover. A recoverable input could have afforded not to do that and this one cannot. **The captured input carries the case a live pass has to tell apart, and it is captured rather than authored**: the response holds thirty of the thirty-one names asked for, and the absent one is MUZ, the same fund trust whose fundamentals answer took the sector walk down on 2026-08-27. A name the vendor never mentioned and a name it quoted with one side are different facts and only the second is evidence about the name. The spreads it holds run from 0.9 basis points on NVDA to 327 on IESC, which is the range the decision's own argument is about. **Discharges the quota-day obligation due here**: `VendorQuotaDay` names the window the ceiling is counted over, both reads bound between two instants, the truncation is in no shipped statement, and `point-in-time` carries the guard that could not be written while one correct use of the expression remained. Migration 038. **And it raises one**, being that the two intraday captures take different populations, so a name outside the capped sixty has minutes bought for it and no spread |
| 4.14 | **The corpus corrections, five of them.** ARCHITECTURE and CHANGELOG only, clean edits with prior text recorded, no threshold and no code | Third rather than first, because the capture leads and a document keeps. **The sixth correction, the data budget, landed with the plan on 2026-08-31** rather than here, because 4.2 sizes its own calls against that total; the count is five by that move and not by omission. **(a)** Three catalogued components each carry a nightly slot and appear in no phase's Builds row, so as written P4 builds an auditor of a thing it never builds. They are placed in the phases whose rows already build them, the plan writer and the replay clock in P4 and the variant resolver in P5. **They are described rather than named here**, on the rule stated above this table, since naming them in this row would move each component's claim to this checkpoint from the one that builds it. **Asserted in both directions by extending `architecture-conformance`** rather than by a new check: it already runs both ways over the same tables, and a check states a floor under each scope it names, so the placement scope carries its own floor and the roster count is unchanged. **(b)** The P4 ordering sentence matches the recorded order, with the reason that displaced it in its place and what the original protected kept: the watchlist still arrives before the trading machinery even though it is third. **(c)** No screen description carries a copyable figure. Ten `fig` blocks, of which two are screen descriptions carrying live figures, and **56 figures expected** across them, 26 in the watchlist and 30 in the scoreboard; an authored parameter or cap stays a number and a measured quantity is masked. The watchlist caption gains the sentence the scoreboard's already has, that the figures are illustrative rather than measured, which is why one of them was readable as a fact. The count is stated before the sweep and reconciled after, with any difference explained rather than absorbed. **(d)** The superseded sentence saying the phase should not start without 3.6's answer goes. **(e)** Four causes becomes four causes plus `unclassified`, and "never triggered" is stated not to be a closed loss and to have no home in a table grained on `trade` |
| 4.15 | **The decisions.** What the sitting produced, recorded | **Its deliverable is recording, not eliciting**: the eleven questions were put on 2026-08-31, before the phase's first commit, so no checkpoint waits on operator availability. Every answer lands as a named decision and a filled authored-parameters row, and every answer that did not arrive stays an OPEN row carrying its reason, named for 4.13. Plus the mechanism decisions a build session may take: cap precedence and which reason a blocked order records when two bind; whether a position whose stop has trailed above entry still consumes total risk at stake; the anchored average price's store and the anchor it is measured from; and what WatchlistPublisher writes. Plus the three ownership rulings SCHEMA owes, `position`'s writer split between the two components that write it, **WatchlistPublisher's store, which was taken at 4.1 rather than here** and is no longer this checkpoint's: it owns none, on the `SetupJournal` precedent, and SCHEMA records the absence with its reason. It was moved because this checkpoint is gated on ten operator answers and none of them bears on it, so a page would have waited on an unrelated question. And `order` carrying blocked and filled rows with the reason column owed at 4.6; **columns stay owed at their checkpoint**, which is SCHEMA's stated design and is not changed. Plus the authored parameters mapped to the checkpoint that builds the component they govern, which `pinned-constants` reports rows out of scope for want of. **The ten answers arrived on 2026-09-01 and every one of them lands here.** Nine named decisions, because the long trail's answer closes two rows: the trail's own and the long side's other side of the comparison, which asked what ends a long position beyond the trail. **Three rows carry a number that is arbitrary within a defensible range and say so on the row**, being the 15% trim, the 0.1 ADR give-up offset and the 1R noise boundary, so a later session reads a choice rather than a derivation; recording that is the point, since an arbitrary number that does not say it is arbitrary is the one a later session defends. **One row is filled as dropped rather than as answered**, being the trim into support, and it carries the reason: support is defined nowhere in the corpus, a definition invented here would be authoring the strategy rather than recording it, and phase 5 is where a rule variant is screened against evidence. A dropped clause recorded as dropped is a decision; a dropped clause recorded nowhere is an omission the next reader restores. **ARCHITECTURE's completeness sentence returns here**, and nought rows of the authored-parameters table remain open, which `stated-counts` derives from the table rather than reading from this sentence alone. Done when every decision resolves, no phase-4 statement in ARCHITECTURE disagrees with SCHEMA or BUILD_PLAN, and the tripwire in `AuthoredParametersTests` is paid rather than lowered: it asserted at least six rows were OPEN so that a sweep finding none would mean the marker had changed, and at nought open rows that floor cannot tell a changed marker from a filled table, so the property moves to an authored case that keeps it permanent |
| 4.1 | **Watchlist page** and WatchlistPublisher, long and short in divided panels, ranked, failed checks greyed with the failing check named, conflict banner | **Openable, and fifth rather than first.** It led this phase until 2026-08-31 and lost the place to the two capture checkpoints, which lose data every evening they do not run where a page loses nothing by waiting. **The status band's fields each have a source or name a checkpoint that has not landed.** Positions, shorts and total risk need `position`, which arrives at 4.7, and they render the checkpoint rather than a nought: a nought positions-open is a fact about the account and reads identically to a component that does not exist. **The mood had a source since 2.5 and said "not until 2.5" through the whole of phase 3**, which is a deferral outliving its own due point on a surface, where the report's out-of-scope rule cannot see it; it reads the night's label now and a guard fails any field waiting on a checkpoint `PROGRESS` records. **The caps those figures are shown against are not rendered either**, because four positions, two shorts and 3% are stated in two of ARCHITECTURE's tables and held by no code constant, and 4.6 is where they are pinned against the component that applies them: the first copy of a cap belongs anywhere but a view. Plans do not exist yet, so a row shows the trigger, the give-up price and the distance the detector computed, and **no share count at all** rather than an empty column. **WatchlistPublisher owns no store, ruled here rather than at 4.15**, which is gated on answers that do not bear on it. `setup` already carries `rank` and `capped_out` and every read of it is bounded, so a replay returns the list an evening showed; a stored copy would be a second statement of one night that could disagree with the rows it came from with nothing reading both. The stage reports what the page would show, which is where a night that was never capped is noticed. SCHEMA records the absence beside SetupJournal's. **Discharges its two remaining obligations.** A `CheckResult` carries a verdict and a number per clause, so `tradable-shortable` says which of its four floors a row missed and the watchlist names it beside the greyed row; the long `tradable` has **two** clauses and the reading beside it said four, which is a count restated in a display helper and is now derived from the gate. And `surface-claims` reconciles in the direction it never did: every corpus sentence claiming something appears on a surface is declared or exempted by name, proved by removing a declaration and watching its own sentence surface |
| 4.4 | VwapEngine, session and anchored | **Question 3 is answered here as a named decision rather than carried to 4.15**, because it is 4.4's own and blocking: the grid anchors to the session open, the six complete hourly bars are the bars, and the closing remainder is not one and cannot trigger the rule. The decision carries its reasoning and its rejected alternative, being a clock-anchored grid that would put the stub at the open. The grid is derived from `SessionBoundaries` and restates neither boundary. A test proves a bar opening at 15:30 cannot satisfy the close rule and one opening at 14:30 can. Question 3's OPEN row is filled and ARCHITECTURE's completeness sentence stays absent, because the other ten rows are still open when this checkpoint ends. **They were closed at 4.15**, which is where the count that used to be stated here now lives. **The anchored average price has no store anywhere and gains one, with the anchor stated in terms the store holds**: which session the swing sits in, which minute inside it the extreme traded, and what kind of swing it is. Declared in SCHEMA with its writer, and a test proves the anchored value differs from the session value on a day where the anchor is not the open, and equals it on a day where it is. **Every verdict written from here on records the full disjunction rather than reading `CeilingClauses.Unrecorded`**, and a test distinguishes a two-clause verdict from a three-clause one. **Short's twenty-session count for 3.6 begins on the first night the third clause runs against minutes covering that row's own swing, which is not the night this checkpoint lands, because building the clause is not running it; the nights before it are explicitly not counted and the long side's count is unaffected and stands.** Earlier rows are kept and distinguishable in the store rather than in a note: each carries the clause set its verdict ran, read back by `ShortPullbackRules.ClauseSetOf`, and the first row recording the full disjunction is the seam. **The start is a reported figure rather than a declaration**: `vwap_run.anchors_asked` and `anchors_priced` are written every night and the run line prints both, and the fixture carries the count of each clause set as a derived expectation, so a night that priced no anchor says so on the store and in the report. **And it discharges the re-measurement of the short funnel with all three clauses running**, on the same terms as the measurement of 2026-08-30 so the two are comparable: same windows, same denominators, same per-gate reporting. Before and after side by side, the delta per gate, the median a night per direction and the effective observations per night, since that is the figure 3.6 turns on. **No threshold is proposed**: the adjustment is the operator's and is spent once. The 2.11 row records the re-measurement as taken and stays open at the operator for the decision itself. The phase report's mapping of the VwapEngine claim to 3.6 is corrected here, since every other document says 4.4 |
| 4.16 | **PlanBuilder**, one baseline plan per capped candidate | **The order prices are answered at 4.15 and this checkpoint computes them**: the intraday floor is the lowest low of the final pullback session's regular-hours minutes, the give-up point sits 0.1 ADR beyond it on both sides, the thrust among several scan hits in the window is the one with the extreme with ties broken by recency, and a setup with no pullback gets no plan at all (see: The order prices are derived from the final pullback session's minutes, not from the screening geometry). That last part is the (d) this row already carried as a defect, and the answer is that it is not a value. **Sixth, and before anything that reads a plan.** SCHEMA declares `trade_plan` under Trading, the catalogue slots this component at 18:30 and the runbook reserves it, and no phase built it: as written the phase built PlanAudit, an auditor of a thing it never built. **No version column**, because the catalogue's "per version per candidate" is written for the multi-version world; 5.1 adds the fan-out and does not move what this wrote. Written before the session and never updated after its session date (see: The plan is written before the session and is immutable after publication). Done when a second write is refused by the store's own key, a rerun writes nothing, and **a setup whose geometry is absent produces no plan rather than a plan sized on nought**, which is the case migration 031 names and whose pre-031 rows carry a flattened nought that clears every threshold written as a maximum (see: A gate handed an absent or degenerate quantity fails rather than passing). **That clause covers the equal pair as well as the absent one**, which is the obligation raised at 3.15 and due here. **The half this checkpoint owes is discharged and the other half is raised as its own row**: a capped candidate with an equal pair gets no plan, counted apart from the absent case in `plan_run` so the defect cannot hide inside the ordinary refusal, and what is untouched is that the detectors go on writing the pair. **And the discharge is narrower than it looks, which the row says rather than leaving to be found**: the path into it is already closed one stage earlier, because a no-pullback row carries a null give-up distance, `exit-tight` fails outright on one, so the row never passes every check, never carries a cap decision and never reaches this stage. The clause is defence in depth against a live fault rather than a fix for one, and it is asserted over an authored candidate because no captured night can produce the shape: a setup whose thrust has not pulled back yet is stored with a trigger and a stop that are the same price, so the geometry is absent while two of its four columns state a number, and this is the first component that would read the pair as a size. **The plan records the session it is live in**, not only the setup it came from, so the pairing 4.5 asserts is a column rather than a calendar step, which is the shape that made `intended_date` differ across every weekend. **The plan carries a share count and PlanBuilder is what computes it**, which is the first of the three readings this row put and is recorded as a named decision. RiskGate at 4.6 may reduce that count to fit a cap or block the order and never recomputes it, and every reduction records the cap that bound. A reduction keeps the plan's give-up price, because recomputing the distance from a reduced size would change R for the same trade and R is the unit every downstream figure is denominated in. The catalogue line giving sizing to the component that runs on trigger is corrected here, and 4.1's watchlist comment naming 4.6 as where a share count comes from is corrected with it (see: The plan carries its own size, and RiskGate reduces or blocks it but never recomputes it). **The size rounds down and the plan stores both figures**, being the budget it was sized from and what the rounded count actually risks, so the rounding is visible rather than assumed away. **Every done condition here is verified over an authored population and it is stated once**: the funnel passes a median of nought candidates a night on both sides, so no captured night holds a plan and the cases below are authored to sit either side of each property (see: Gate boundaries are exercised by authored cases and the captured fixture is not asked to do it). **The population is the rows the cap kept, and 2.11's row can be read as saying otherwise.** That row settled for this phase that the plan is written against flagged setups rather than passing ones. Read as a statement about this stage it would mean a committed instruction for every flagged name, including ones a gate refused; read as a statement about what the phase is designed around, which the clause after it is, it means the records cover the flagged population rather than assuming the passing one is non-empty. The second reading is taken, on three grounds: this row says one plan per capped candidate, the catalogue says per version per candidate, and a plan is an instruction to trade, so writing one for a setup the lab has already declined would put RiskGate in the position of blocking every order it ever placed. **The first reading is not obviously wrong and the ambiguity is recorded rather than resolved silently.** `capped_out` is written only by SetupCapper and only over rows that passed every gating check, so the consequence of the reading taken is that this stage plans nothing on almost every night, which is the finding rather than a fault. **The half of the sizing decision a test cannot reach yet is named as such**: RiskGate does not exist, so "nothing recomputes a size" is held by a scan naming the one caller of the sizing function, the scan cannot see a component that reimplements the division, and the behavioural half arrives at 4.6 with the component that could break it |
| 4.5 | TriggerResolver over SessionReplayClock, point in time enforced within the day | **Trigger confirmation is answered at 4.15 and this checkpoint builds against it**: touched, being a minute bar's high reaching the trigger long and its low reaching it short, with no margin. It is not a detail of the resolver, because the contention rule compares trigger times across names and the three readings of "the trigger traded" order the same session differently (see: The trigger is touched, not closed through). **And it states the fetch window as a storage question, which is the obligation raised at 4.4 falling due here.** The vendor charges per request and not per session, so reaching further back costs nothing in calls and everything in rows, and no option below is more expensive than another in vendor calls. **The unit is 959 minute bars per name per session**, being the one call of 2026-09-01 for `intraday/AAPL.US` over 2026-08-25, 390 in the regular session and 569 outside it, and the mean of 959.3 over the 82 sessions of the same day's 78,662-bar probe. It is one name and the most heavily traded on the exchange, so it is a ceiling on a flagged name rather than a typical one: a minute bar exists only for a minute that traded. **The population is 44, 73 and 83 distinct flagged names**, over the three nights the evidence store holds, 2026-08-27 to 2026-08-31, and the row counts below are the first fill of one name's window at that ceiling rate, across 44 to 83 names. **The window is a one-off per name and not a nightly multiplier**, because `IntradayBarReader.IsStoredUnchanged` writes nothing for a bar the store already holds unchanged, so after a name's window is filled the nightly cost returns to one session for it. Four options, and their boundaries are derived from the scans rather than chosen: a swing sits the thrust span plus the 2-to-7-bar pullback back, so `gainer` and `gapper` put it 3 to 8 sessions back and `leader` and `laggard` 22 to 27. **One session**, as built, **42,196 to 79,597 rows**: no anchor is ever inside it, the nearest sits 3 back, and short's twenty-session count never starts. **Eight sessions**, **337,568 to 636,776 rows**: reaches every swing the one-session scans flag and none the twenty-session scans do, so nights would carry short rows running the full disjunction beside short rows that cannot, which is a population split inside one count rather than a start. **Twenty-seven sessions**, **1,139,292 to 2,149,119 rows**: reaches every swing on both scan families, so a name whose window is filled is anchorable whichever scan flagged it and the count starts cleanly. **The vendor's per-request maximum**, 120 calendar days and 82 sessions in the probe, **3,461,128 to 6,528,946 rows** taken from the probe's own per-name total: the same start as twenty-seven with history behind it. **No window is proposed and no bytes are stated**: the choice is the operator's because the cost is the size of their store, and no byte figure is given because none has been measured over a populated `intraday_bar`. Nothing is fetched at this checkpoint to answer it. A test proves the resolver cannot see a later minute than the one it is evaluating. **And it names the pairing**, which SessionReplayClock inherits beside it: the plans written on the evening of N−1, resolved against session N, citing 4.2's decision rather than restating it. **Asserted: a plan is never resolved against a session at or before its own date**, fail-closed, and a resolver handed its own session refuses rather than returning no fill, because no fill and cannot-resolve are the conflation this corpus keeps finding. **And whether one clock walks every name for a session at once**, which the contention rule requires, since the earliest trigger filling is a comparison across names. What "the trigger traded" means is applied here per direction |
| 4.6 | RiskGate, all six caps, both directions, over the size the plan already carries | Blocked orders written with reason, including the contention case. A test asserts that when two plans trigger and only one slot remains, the earlier trigger fills and the later is blocked (see: Plans are resting orders and fills go in time order when the caps bind). `order-provenance` passes. **The six caps become pinned constants**, being stated in two tables and pinned nowhere. **What RiskGate does to a size it did not compute was answered at 4.16 and this checkpoint builds against it**: reduce or block, per cap, never recompute, and every reduction records the cap that bound. The three count caps can only block; the two proportional caps are the ones that could do either, and which of the two a cap is belongs on the blocked order's reason. **A reduced position keeps the plan's give-up price**, because recomputing the distance from the new size would give a different R for one trade and R is the unit every downstream measurement is scored in (see: The plan carries its own size, and RiskGate reduces or blocks it but never recomputes it). **What is open here is the behavioural half of that decision**, which no test could reach until this component existed: 4.16 held "nothing recomputes a size" with a scan naming the one caller of the sizing function, and a scan cannot see a component that reimplements the division. This checkpoint is the one that could break it, so it is the one that asserts it. **The carried obligations falling due here are classified below the obligations table**, on the same terms 4.1's were, so the decision is taken once with the groups in front of whoever takes it rather than once per row by whoever is closest to each |
| 4.7 | PaperBroker and the fill model | Gap-through recorded as a loss greater than 1R and tagged. Never clamped. **The fill price of that gap is stated**, not only that the loss is not clamped. Entry slippage arrives here as a form and a number, and **exit slippage with it**, since the model prices one end of every trade and not the other. **Both are answered at 4.15 and this checkpoint builds against them** rather than choosing: the whole captured spread, the wrong way, on both sides and at both ends, with a gap filling at the session's first regular minute open and not slipped again (see: Entry slippage is the whole captured spread, symmetric between the directions) (see: Exit slippage is charged on the same terms as entry slippage) (see: A minute that opens through a resting price fills at that open, whatever time of day it is), **which is the wording after 4.8 widened it** from the session's first regular minute to any minute. The fill model's third rule names a profit-taking level that does not exist until 4.8, and the row says how it behaves until then. **And it decides what the slippage fraction is being applied to, because the stored spread is a figure across two instants.** The vendor stamps a quote's bid and its ask separately: on the capture of 2026-09-01 AAPL's two sides were 32 seconds apart, one name on one response and stated as one rather than as a rate. So a stored `spread_bps` is not the spread that existed at either stamp and need not be one that existed at any moment between them, and on a name whose book moved it can be wider or narrower than anything a trader could have crossed. **The row states what a fraction of it means and what happens to a row whose two sides are far enough apart to describe different markets** — charged anyway, widened, or refused, and if refused then what the fill costs instead. It rests on nothing new: `spread_snapshot` already carries both stamps and the lag from the older of them, which 4.3 stored for this. **This is 4.3's finding handed forward rather than 4.3's note**, and the vendor half of it is in ARCHITECTURE under "What each vendor endpoint carries", where a checkpoint choosing any later intraday input reads it |
| 4.8 | PositionManager, two rule sets | Long trail and short trim are separate code paths, not one routine with a sign flag. **And a stated rule for when the trail takes over from the fixed stop**, which is unstated on both sides today. **Four of the eleven questions were answered at 4.15 and this checkpoint builds against the answers** rather than taking them, which is the change 4.15 makes to this row. The trail is evaluated on the daily close, active from entry, filling at the next open, and the exit is whichever of it and the fixed give-up point comes first, which is also the long side's answer to the exit trigger the short side already had. The trim is 15% of the planned position, once, at 3R. The support clause is dropped from the baseline. So what is left here is the code and the test, plus the one thing 4.15 does not settle: **a stated rule for when the trail takes over from the fixed stop**, which is unstated on both sides today and is a mechanism rather than an authored value (see: The long trail is evaluated on the daily close and fills at the next open) (see: The short trim is 15% of the planned position, once, at 3R) (see: Trimming into support is dropped from the baseline rather than defined here) |
| 4.9 | PlanAudit and TradeJournal | Planned stop against executed stop recorded on every trade. **Which fields are audited is stated**, since ARCHITECTURE, SCHEMA and the mockup each name a different one. **And the ordering between the two**, both of which run on exit |
| 4.10 | LossClassifier, four causes | Every closed loss classified. Unclassifiable is itself a recorded category, not a silent skip. **The boundary between a noise stop-out and a failed setup is answered at 4.15 and applied here**, at +1R on the direction-signed ten-day return from the trigger: at or above it the stop-out was noise, below it the setup failed. **And the precedence rule with it**, which is not one ordered list: a gap loss is classified as a gap first because that names how the loss occurred, and noise against failed classifies what happened afterwards among the rest, so the case that satisfies two causes at once is answering two questions rather than colliding. The row asks this checkpoint for the code and the test, and for the classifier to record both answers separately rather than collapsing them (see: A stop-out is noise when the ten-day return reached one R, and cause of loss is two questions rather than one ordered list) |
| 4.11 | Trade journal page, split long and short | **Openable.** Intraday chart per trade with trigger, fill, stop and exit drawn. **It absorbs the layout `SCREENS.html` carries** before 4.12 deletes it, rather than leaving it to be reconstructed. `Navigation.cs` records this page at 4.12 and it arrives here, which is corrected in the same commit and is a surface claim because the empty state renders it |
| 4.12 | Retire the mockups | `SCREENS.html` deleted (see: The corpus is eight documents plus one artefact, and a ninth requires retiring one). Any layout detail it carried that the built UI relies on has moved into the built UI, not into another document |
| 4.17 | **The verification defects phase 3 carried, thirteen of them.** No component, no store and no threshold: the checks, the tests behind them and four records that state something the code does not do | **Its deliverable is the pile classified below the obligations table, and the classification is the specification.** Nineteen rows arrived at 4.6 because a verification defect with nowhere obvious to go lands at the next checkpoint that plausibly cares, and 4.6 discharged the seven whose subject was its own. The thirteen left have one subject between them, which is why they are a checkpoint rather than thirteen repointings. **The first group is the one that matters and it is the corpus's own argument, twice over**: a check reconciling a hand-named list in one direction reports nothing when it is the list that is short, which is how five stages went undispatched to 4.5 and how five columns went undeclared to 4.6. **Every row is discharged or repointed with a reason, and the count is derived rather than restated.** A row this checkpoint declines to fix names where it goes and why, on the same terms the 4.1 and 4.6 sections set. **No new check unless a row asks for one**: the defects here are in checks that exist, and adding an instrument to watch the instruments is how a verification corpus stops being read |
| 4.13 | Phase sign-off | Fresh session, and `tools/verify-phase` green with nothing unexamined. **And it states which authored-parameters rows are still OPEN**, rather than discovering them: the completeness sentence and an OPEN row cannot both stand, which is asserted in both directions. **4.15 filled the last ten and the sentence returned there**, so what this row now asks is whether anything reopened one between 4.15 and the sign-off, which is a different question from the one it was written for and is the reason it stays |

**The out-of-scope count was projected to rise before it fell, and it has not risen once.** The projection was made when phase 4 was planned: a claim added by a document edit has no verdict until its component lands, so the corrections at 4.14 were estimated to take the count from 50 to between 51 and 54 against a ceiling of 52, and 4.14 was named as the checkpoint that reads it against that ceiling before committing. **Three checkpoints have now moved it and every one of them moved it down**: 50 to 48 at 4.2, 48 to 47 at 4.3. The plan's own reading of why the first projection missed was that the data budget is read by `pinned-constants` rather than by `architecture-conformance` and so enters no claim register, and that reason generalises further than it was applied.

**Re-derived at 4.14 from the five corrections rather than carried forward, because the band was built when they were six.** The register `architecture-conformance` keeps is keyed on **rows and tables**, not on sentences: a claim table yields one claim per row, and every table in the document yields one placement claim whatever it contains. So the two edits that move the count are a **new row in a claim table** and a **new table**, and nothing else does. Against that, correction (a) edits the cells of two Build order rows that already exist and (b) edits a cell of one of them, so the Build order table yields the same claim per phase it did before; (a)'s reverse assertion is a coverage scope with a floor rather than a claim, which is what the row already says it is; (c) edits `fig` blocks, which are not tables and which no claim register reads; (d) removes a sentence from a prose block; and (e) edits cells of two tables that are exempt by checkpoint and stay exempt. **The expected move is nought, and the count is expected to read 47 against a ceiling of 52.** Stated here before the run rather than after it, which is the point of naming a checkpoint as the one that reads the ceiling.

**The ceiling still gets raised if the run says otherwise, and the reason for the raise is the phase and not the run.** A phase that genuinely adds claims before it retires them is a phase whose ceiling was set too low, and that is a different act from raising a number to turn a red green. The distinction is only legible if the expectation is on the record beforehand, which is what the paragraph above is for: a raise now has to explain why the derivation was wrong, not merely that the count was high. **The one thing that turns this red is not the count.** `Schedule.NamesIn` reads component names out of a Builds cell, and a name it finds that the catalogue does not hold fails that row's claim outright. All three components (a) places are catalogued already, which is what makes the placement an edit rather than an addition. From 4.1 onward the count falls as each component retires its catalogue claim, and phase 4 retires roughly 30 of the 50, which is this phase's largest single contribution to the report.


---

## Phase 5 — Variants, without any AI

| # | Deliverable | Done when |
|---|---|---|
| 5.1 | `variant` store, VariantAdmitter registering it and VariantResolver deciding which are live, PlanBuilder produces one plan per live variant | **The twelve obligations due before the freeze discharged first**, then baseline V0 registered and frozen. The order is the done condition rather than a preference: each of the ten changes a figure the baseline carries, and the freeze is what makes such a figure uncorrectable |
| 5.2 | VariantScorer, two families scored differently | Selection on forward return, execution on R. A variant spanning both is rejected at admission |
| 5.3 | ReplayHarness, **which screens selection variants and has no history to screen an execution variant over** | Re-filtering the stored history with a **selection** rule completes in seconds and reproduces the baseline's own selections exactly. **The acceptance test is scoped to selection because selection is all the stored history can answer.** An execution variant is scored on R, R needs fills, and a fill needs minute bars, which exist only from the night 4.2 begins capturing them: the vendor sells daily history and no purchase substitutes, so every setup recorded before that has no intraday path to replay a stop or a trail against. The nights that do have minute bars are the forward-collected ones, so replaying an execution rule over them is the forward paired test arriving slowly rather than a cheap screen run before it. **The gap is in history rather than in the code, so it does not expire and no later checkpoint closes it**; the hole in the record stays the same size for ever while the screenable set grows only at one night a night. What screens an execution variant instead is nothing: it is admitted on forward evidence alone, spends a live slot from the day it is admitted, and accumulates its pre-registered minimum sample of paired trades in real time. So the screening half of (see: Replay screens proposals and the forward paired test admits them) is empty for one of the two families, and the plain consequence is that a selection proposal can be killed before it costs anything while an execution proposal cannot |
| 5.4 | HoldoutRegistry, eight quarterly windows | Spent windows recorded with date range, purpose and outcome. A spent window cannot be re-spent |
| 5.5 | Research ledger page | **Openable.** Variants, samples, targets, status |
| 5.6 | Two hand-written variants per family running | Paired differences accumulating and visible |
| 5.7 | Phase sign-off | Fresh session, and `tools/verify-phase` green with nothing unexamined |

**5.1 cannot be reached until the twelve obligations due before the freeze are discharged, which
makes them phase 4's real tail rather than phase 5's opening.** They are listed in the carried
obligations table with 5.1 as their due point, and reading that as "work done during 5.1" is the
mistake this paragraph exists to stop: 5.1's own deliverable is the freeze, so an obligation due at
5.1 is due *before* its done condition rather than alongside it. In scheduling terms the twelve sit
between 4.13 and 5.1, and whoever plans the end of phase 4 should count them as part of it. They are
repairs to stored figures rather than new components, so they do not need a checkpoint of their own;
what they need is to be finished before V0 is registered, because after that a repair costs the
accumulated variant record rather than an afternoon.
(see: An approved proposal creates a new version from zero, and a running version is never edited)

**5.3's acceptance test is the important one, and it is an acceptance test for the selection half:** running the baseline's own rule through the harness must reproduce the baseline's historical selections exactly. If it does not, the harness and the live detector disagree and every replay result is worthless. It says nothing about execution, and the row above says why the stored history gives it nothing to say.

---

## Phase 6 — The loop

| # | Deliverable | Done when |
|---|---|---|
| 6.1 | SignalBackfiller | A new signal computes across the entire stored setup history in one run |
| 6.2 | SignalAdmissionTest | A signal is admitted only if it tightens outcome-similar neighbourhoods, and rejected above 0.70 correlation unless stored as a residual |
| 6.3 | TwinPairFinder | Z-scored over the trailing 250 setups. Pairs under 0.5 distance with outcomes over 15 points apart |
| 6.4 | ContextPacker, versioned, with the planted null signal | Pack version recorded on every proposal. The null appears in the conditional tables |
| 6.5 | ResearcherSeat, built against the narrow interface, with both implementations present and selected by configuration | A test asserts the subscription implementation is constructed with an **empty tool set**. A test asserts `ANTHROPIC_API_KEY` is absent from the environment. A proposal row carries the **configured model**, the **transport** used and the **served model** string the response reported. Budget or plan-limit exhaustion queues the job and returns nothing, on both paths, and never a partial proposal (see: The researcher transport is a configuration switch between subscription and API key, over a deliberately narrow interface) |
| 6.6 | ProposalRegistry, rule proposals and signal requests | Abstention is a valid recorded outcome, not a failure |
| 6.7 | AcceptanceGate | Reads only status and resolution date. Cannot touch a target |
| 6.8 | Scoreboard band 3 and the Pack comparison page | **Openable.** Proposal hit rate by pack version |
| 6.9 | Phase sign-off | Fresh session, and `tools/verify-phase` green with nothing unexamined |

**6.5 has a trap, and it survives the transport switch.** `ANTHROPIC_API_KEY` stays banned from the environment on both paths, for a reason that now applies to each of them: on the subscription path its presence silently defeats plan auth and bills API rates, and on the API path the key belongs in configuration like every other secret, so one in the environment means two places supply the same credential and nothing on the surface says which won. Assert its absence at startup and fail loudly.

**A proposal citing the planted null signal fails that pack version immediately.** No proposal from that version is admitted.

---

## The lazily-resolved attribute, scoped and not started

An observation stamp records when the lab asked. Every reader treats it as when the fact became
true. For a bar the two are the same, because the observation is dated by the market. For an
attribute looked up lazily they are not: a company's sector was true for years before anyone asked,
so a name resolved on the 28th is invisible to the session of the 27th although the sector it
carries was as true on the 27th as it is now.

Written up at 3.9(h) and **not started**. What follows is the scope, the counts and the decision the
work waits on. The obligation points here rather than restating it.

### It is one table wide, and the count that stopped it was a count of something else

The 3.8 attempt stopped on **43 stamp-bounding query sites across 17 shipped-source files**, which is
the size of changing the basis for every stamped table at once. That is not this work. Re-derived
per table, thirteen of the fourteen stamped tables carry a value whose truth is dated by the session
it belongs to, and the stamp on them is doing exactly what a point-in-time bound needs:

- `daily_bar` and `index_bar` are dated by the market through `bar_date`; a bar for a past session
  fetched today is still information the lab did not hold on that session, which is what the bound
  says.
- `indicator_daily`, `setup_signal`, `control_setup`, `forward_return`, `ceiling_bound`, `scoreboard`
  and `setup` hold something computed for a stated session. Recomputing an old session today
  produces a value the session did not have, and the bound is right to hide it.
- `corporate_action` and `history_refetch` record when the lab learned something it genuinely could
  not have known earlier.
- `detector_error` and `scan_hit` are dated by the session they were produced for.

`security` is the exception and is the whole of it: `sector`, `industry` and `market_cap` are
resolved on demand, stamped with `sector_resolved_at`, and **the table has no session-date column at
all**. That absence is the conflation rather than a consequence of it.

**Five files read that stamp**, being `SecurityReader`, `SectorResolver`, `ThemeClusterer`,
`CheckRecomputer` and `SessionFigures`. That is the blast radius, not seventeen.

### What a session-date column would mean here

Not a session date. An attribute that is true over a span needs the date **from** which the lab is
willing to assert it, which is a different thing from the date it was asked on and a different thing
again from the session that asked. The three candidates, in increasing cost:

**Assert it from the date the scan first surfaced the name.** The lab asks about a name because a
scan hit it, and that hit is dated. `security.first_seen` already holds a date of that shape for
2,083 rows. Cheapest, and it claims the least: the attribute is asserted from the first session the
lab had any reason to hold it.

**Assert it from a validity date the vendor states.** The fundamentals endpoint carries an updated-at
field the lab does not currently read. Truer, and it costs a re-capture and a re-derivation of the
fundamentals expectations, of which there are 121.

**Assert it from the beginning of the record.** A sector is stable over years and treating it as
timeless is defensible for this lab's horizon. Cheapest of all and the least honest, because it
would make a name resolved next year visible to a session two years ago.

### What happens to rows whose instant was never recorded

**2,083 securities, of which 234 carry a resolution and 1,849 have never been asked.** The 1,849 need
nothing: they carry no attribute, so there is no value to date. Of the 234, one is a name the vendor
holds nothing on, stamped so it is never asked again, and it carries no attribute either.

So the backfill is **233 rows**, and every one of them has a first scan hit whose date is recorded.
There are no rows whose instant was never recorded, which is the condition that made this look like a
migration with an invented backfill in it.

### What it costs downstream

**29 frozen signal values** would change basis: `industry`, `market_cap` and `cluster_count` are
recorded on the 29 setups of 2026-08-27 that had a resolved sector when the vectorizer ran. Those are
frozen by design and would be recomputed under the new basis, which is a correction under the
lateness rule and records prior state like any other. The other 15 of that night carry the repaired
cluster and no industry, and would gain one.

Every fixture expectation resting on a `security` bound would be re-derived. Today that is the
`fundamentals.*` set, 121 expectations, all `DERIVED`.

### The decision, which is the operator's

**Before or after phase 4.** Stated here rather than assumed, because the two are not the same
project.

**If it lands before**, phase 4 starts against a store whose one lazily-resolved attribute is dated
the way every other value in the lab is dated, and 29 frozen signals are corrected once, while
nothing is trading. The cost is a phase-4 start delayed by the work above.

**If it lands after**, phase 4 records orders against setups whose `industry` and `market_cap`
signals rest on the current basis, and the correction then has to reach rows an order was placed
against. Nothing in the corpus permits rewriting a plan, so those setups would keep the basis they
were flagged under and the record would carry two bases with a date between them. That is not fatal
and it is not free: every figure computed over the boundary would have to state which side of it the
rows came from, which is the population rule applied to a basis change rather than to a filter.

**The conservative answer is in force until it is answered**, which is that the stamp is the basis and
a late resolution is invisible except through the lateness bound.

## Carried obligations

Findings that did not block a checkpoint. Each names the checkpoint at which it falls due.

| Raised | Obligation | Due at |
|---|---|---|
| 4.9 | **Two figures called the result now exist and nothing downstream has chosen between them.** `trade.result_r` is after the borrow a short is charged and `position.realised_r` is before it. They are equal on every long and differ by the borrow line on every short, and the difference is small: at 1.0% a year a four-day hold is about two tenths of one per cent of a unit of risk. **What matters is not the size but that both are correct**, so a component reading either produces a defensible number and two components reading different ones produce a comparison nobody can reconcile. LossClassifier is the first thing that reads a closed loss, and its noise boundary is stated at +1R on a forward return rather than on the trade's own result, so the choice is live there rather than settled by it. **Due at 4.10** | 4.10 |
| 4.9 | **`plan_audit` is written every night and no surface reads it.** Three pairs per trade, each answering a different question, and the one the mockup drew is the journal page's plan-against-actual column. Until that page exists the audit is a table with a writer and no reader, which is the state `spread_snapshot` was in until 4.7 and the state this corpus names as worth recording rather than leaving to be noticed. **Due at 4.11**, which builds the page and is the same checkpoint `closed_in_their_own_session` waits on, so the two arrive on one surface | 4.11 |
| 4.8 | **`closed_in_their_own_session` is the size of an approximation and it is in the store alone.** RiskGate reads the book as it stood coming into the session, so a position opened at 09:31 and closed at 09:45 still occupies a slot the 10:00 trigger is refused on, and the decision that keeps the gate where it is rests on the cost being countable rather than argued. It is counted, on `manage_run`, every night. **What it is not is read**: no screen carries it, and a figure a person never sees is one nobody reviews the choice against, which is the sixth failure shape stated the other way round. **Due at 4.11**, which is the trade journal page and the first surface a position's life is drawn on (see: RiskGate reads the book as it stood coming into the session, and what that costs is counted) | 4.11 |
| 4.7 | **From 5.1 a version can select a name outside the capped sixty, and every such order is refused a fill.** 4.7 answered what an uncapped name is charged, which is what 4.3's row was due here for: nothing, because it is not filled, and the order is written as an unfilled position with the reason. That is the fail-closed answer and it is the right one, and it turns the capture asymmetry from a note into a cost. **The cost is measurable now**: the three nights the evidence store holds flagged 44, 73 and 83 distinct names against a capped sixty, so a version selecting outside the cap meets a name with minutes and no spread. **The choice is between two things and both are stated**: widen the spread capture from the capped sixty to the flagged population, at 88 to 166 calls a pass against 120 on a nightly total of 2,723 to 4,118, or accept that a version selecting outside the cap trades nothing and say so where the scores are read. **Due at 5.1**, and it is a purchase decision rather than a code one | 5.1 |
| 4.4 | **The anchored clause is built and has nothing to read, because the fetch buys one session a night and a swing sits three to twenty-seven sessions back.** `VwapEngine` reads the store and spends no vendor call, so a level exists only where the store holds minutes back to that row's own swing. On the night 4.4 landed the store held none at all, so `anchors_priced` is nought and the short side's twenty-session count has not started. **The constraint is the store and not the vendor**, established by probe on 2026-09-01: one call for `intraday/AAPL.US` over 2026-05-05 to 2026-08-31 returned 78,662 minute bars across 82 sessions, and the vendor's own 422 states the per-request window as 120 days. **So widening `IntradayFetcher`'s window from one session to the anchor window is free in vendor calls and costly in rows**, because the 5 calls are charged per request rather than per session. That is a decision about the fetch and about storage, taken where the fetch is, and it is what the count waits on rather than any further work on the clause. **Due at 4.5**, the next checkpoint that reads stored minutes and so the first with a reason of its own to ask how many sessions the store holds **Repointed to the operator at 4.5, where it fell due and where the pricing it asked for was done.** 4.5's row states four windows with the rows each would add, the boundaries derived from the scan spans rather than chosen, and the fact that a window is a one-off per name rather than a nightly multiplier. What is left is not work: no option costs a vendor call more than another, so the choice is the size of the operator's store and nothing a build session can take. Listed with the others below rather than discharged, because the count that has not started still has not started | the operator |
| 4.16 | **The detectors still write a trigger and a give-up price for a geometry the same row calls absent, and 4.16 closed the consumer half rather than the writer half.** The 3.15 obligation had two: that `PullbackGeometry.Of` returns both prices equal and non-null on a thrust that has not pulled back, which the detectors write while nulling the two distances beside them, and that PlanBuilder would be the first component to read the pair as a size. The second is discharged at 4.16, where a capped candidate with an equal pair gets no plan and the run row counts the refusal apart from an absent one. The first is untouched: `SCHEMA.md` marks `trigger_price` and `stop_price` **Absent where the setup has none** in the same words it marks `stop_distance_ranges`, and one of the three is written that way. **The harm is now contained rather than removed**, and the containment is worth stating because it makes this row look smaller than it is: nothing downstream of 4.16 reads a setup's price pair as a trade, because what a trade is placed from is the plan. What is left is a store whose rows contradict its own declaration, and a later session reading `trigger_price` on such a row gets a price rather than the absence the corpus says is there. **Not fixed by rewriting stored rows**, which is forbidden; what is owed is what the detectors write from here on, and whether the rows already carrying it are marked or left. **Due at 4.6**, on the reading its own classification section gives: a record defect with no checkpoint that obviously owns it lands at the next one that plausibly cares, and 4.6 is where a sole-writer rule and money columns first meet. | 4.17 |
| 4.16 | **The watchlist has no share count column and the plan now carries a size, so the page and the store disagree about what the evening knows.** 4.1 built the page with trigger, give-up price and distance and no size, and its comment said a share count was waiting for RiskGate at 4.6. That is no longer where a size comes from: PlanBuilder writes one at 18:30 and the watchlist publishes at 18:40, so the column has a source ten minutes before the page runs. The comment is corrected at 4.16 and the column is not added, because the page reads through the Api and the Api exposes no plan, so the change is a read surface and a surface claim rather than a column. **What it costs to leave is a page that understates what the lab committed to**, which is the opposite of the failure 4.1 was avoiding: it drew no column rather than draw one from a number that did not exist, and now the number exists. **Due at 4.11**, which is the phase's other page checkpoint and where `SCREENS.html`'s layout is absorbed, so the two page changes are decided together rather than one at a time. | 4.11 |
| 3.6 | **The tight set's within-night draw has no fixture expectation, because the golden fixture holds one market day and a one-day fixture cannot tell a draw that stays inside the night from one that would leave it.** **Restated on 2026-08-31 against the current behaviour**; it described the cross-session reach, which was measured and reversed the same day (see: The tight control set draws within the night, because a within-night draw controls the market mood exactly). The subject changed and the gap did not: `controls.*.nearest` reports `sessionsApart` nought on every row of the fixture, and it did so under the reaching draw as well, because the only session either design can reach is the setup's own. So the committed expectations hold the recorded shape of a tight draw and would go on holding it if the draw started reaching again. The property is held by `ControlSamplerTests.A_tight_control_is_drawn_from_the_subjects_own_session` over an authored multi-session store, proved red before green, so what is missing is regression detection over the replayed pipeline rather than verification. **Priced, because that is what decides when it closes**: it needs a capture holding more than one market day, which is the same input the whole-market liquidity floor is already waiting on at 1,900 vendor calls and about 130 MB committed for ever. It closes with that capture and not before, and it is rowed here rather than discharged by authoring a second population into the replay, which would put authored rows into a fixture whose figures are reported as captured | 3.6 |
| 3.14 | **`recheck --expect` is compared after every correction has been written, so it reports a set that moved rather than refusing one.** `Run` calls `Recompute` or `Restore`, which perform the whole walk and issue every `UPDATE`, and only then compares `expect` against the candidate count and returns 2. The flag's own doc says the run fails on any other number and the 3.8 record says it exits 2 on any other number, both of which describe a guard that fires before the writes. An operator who names fifteen and finds twenty has corrected all twenty by the time the exit code says so, and the exit code is the only thing that says so. The repair is to derive the candidate set, compare it, and return before the first `UPDATE`. **The 3.15 sign-off adds two more of the same stage's command line, found by running it.** `--as-of` given twice with different values is not refused: `Arguments.Parse` overwrites `named` with no duplicate guard, so `recheck --check cluster --as-of 2026-08-27 --as-of 2026-08-28` runs against the 28th and exits 0, where two positionals and a positional disagreeing with a named one are both refused. `--check` and `--expect` repeat silently on the same footing, so what is owed is one rule for a repeated flag rather than a third named exception, which is the shape 3.13(c) replaced with a declared arity for exactly this reason. And the refusal a corrected row already carries cannot name its check: `Candidates` selects `corrected_at` and not `corrected_check`, so a row corrected under a second admitted check refuses a `cluster` recompute with a message reading as though `cluster` had been done, and 3.13(b) added that column so the check's name would stop being prose | 4.17 |
| 3.14 | **`recheck` without `--apply` opens a write connection and a run scope, and a dry run can mark the night degraded.** The flag says the stage reports what it would do and writes nothing. Both `Recompute` and `Restore` call `OpenWrite` and `RunLogger.Begin` before looking at `apply`, and both complete the scope as `Partial` when anything was refused, whatever `apply` said. `RunLogger.IncompleteStagesOf` selects every non-clean run of the session's day and the detectors write that string into `setup.degraded_because` for every row of the night, so an operator asking what a repair would do can mark a whole night degraded by asking. Nothing has observed it because every live dry run so far refused nothing | 4.17 |
| 3.14 | **Two figures `SectorResolver` prints are taken over a population other than the one named beside them.** The night line reads N asked, of which M skipped, and `asked` is incremented only after a successful answer while the catch increments `skipped` and continues, so the skipped are not inside the count they are stated as a subset of: on the night the repair was written for, 149 requests and 148 answers, it reads 148 asked of which 1 skipped. And `PassNumber` counts `run_log` rows on the **UTC** date while the line calls the answer pass N of the night; the lab's evening crosses UTC midnight, and `data/live/logs/nightly-2026-08-28.log` already records the first pass of that session as pass 2, because a repair run at 00:19 Eastern on the 28th shares its UTC day. Both are the shape where every number is right and the sentence beside it names a different population | 4.17 |
| 3.14 | **`point-in-time`'s statement half cannot see a statement built by interpolation, and one such statement reads `daily_bar` with no bound.** The scanner's first pass requires a quote immediately after `CommandText =`, so it does not match an interpolated literal; its second requires a raw string literal. `LabStatus.CountRows` builds its statement through `string.Create` with an interpolated handler and is matched by neither, so it is neither asserted nor exempted, and it counts `daily_bar` rows without bounding `observed_at`. The count is a health figure on the status band rather than an answer about a session, which is why this is a row rather than a defect in the store, but the check reports a coverage it does not have and that is the half that matters | 4.17 |
| 3.14 | **`architecture-conformance`'s catalogue scan names a behavioural backing that is itself a source scan.** The declaration says the direction the scan cannot see is a registration that is present and unreachable, and that `ComponentReachabilityTests.Every_stage_the_entry_point_advertises_has_an_arm_in_the_dispatch` closes it by resolving each advertised name against the dispatch. That test reads `Program.cs` as text and matches switch-arm shapes with a regex. It is a scan backed by a scan, and the run records it as backed. The repair is to invoke the dispatch once per advertised stage name and assert on what comes back, with an authored bad name asserting the other direction | 4.17 |
| 3.14 | **`path-casing`'s literal scanner reads neither verbatim nor multi-line raw string literals, and its own comment says it reads both.** `QuotedLiteral` opens with a lookbehind rejecting the opening quote of a verbatim literal, and its character class excludes carriage return and newline, so a literal spanning lines is never matched; there is no separate raw-string reader anywhere in the file, which the comment says there is. Nothing is missed today because the two verbatim literals in the shipped source are regexes rather than paths, so this is a latent hole plus a false comment. It earns a row rather than a shrug because this is the check that once compared no paths at all and passed | 4.17 |
| 3.14 | **RUNBOOK step 3 tells the operator to create a data root outside the repository and the decision names two roots inside it.** The decision names `data/live` and `data/ci`, both under the repository, and the shipped code follows the decision. Step 3 is also what armed the fault 3.14 corrected in `tools/ci.*`: an operator who followed it and exported the variable had the CI scripts delete whatever it pointed at. The scripts no longer yield to it, so nothing is armed today, and what is left is that a spec and a decision disagree about where the store lives. A decision is changed only by another decision, so this is not a document pass (see: The lab keeps one store per purpose under one data root, and CI never opens the operator's) | 4.17 |
| 3.14 | **Ten phase-3 commits replaced lines in a spec document and recorded no prior text in CHANGELOG.** The rule is that a clean edit to a spec lands there with its prior text and the decision authorising it, and the last commit of the phase replaced eight lines of `BUILD_PLAN.md` without one. Nothing checks it: `CHANGELOG.md` is a record and no check reconciles it against the spec diffs, which is the missing direction the 3.7 row names for eight other checks. What is owed is the reconciliation rather than ten retrospective entries, because an entry written now would be dated today about an edit made then | 4.17 |
| 3.14 | **Two rows in the live evidence store carry a `cluster` value the detector's rule would not have produced, and neither can be corrected a second time.** `2026-08-27-PATH-long` reads 13 where its `leader` thrust counts 6, and `2026-08-27-PURR-long` reads 4 where its `gainer` thrust counts 3. Both verdicts are unchanged, because the threshold is 2 and both numbers clear it, so what is wrong is the value rather than the gate. The recompute is corrected as of 3.14 and will not repeat it, and `AlreadyCorrected` refuses a second correction, so putting the two right means a restore of that date's cluster corrections followed by a re-run. **That is an act on the running lab and belongs to the operator**, not to a build session and not to a check: a green report is a statement about the build and never about the store | the operator |
| 3.12 | **Forty claims are narrower than the corpus sentence they name, and the narrowing is recorded here rather than on each claim.** 3.12's done condition (c) asked that every clause of a corpus sentence a claim names be asserted or be named as unreached, and it was amended to the one claim its deliverable names, which is the vendor-ceiling claim's third clause. The measurement that decided it: of the 76 claims `architecture-conformance` gives a live verdict, 54 sit in a table stating a sentence per row and **40 of those sentences carry more than one clause** — 13 of 14 in Failure behaviour, 12 of 25 in the Component catalogue, 6 of 6 in Build order, 6 of 6 in Running on Windows and macOS, 3 of 3 in The phase report — about 160 clauses, counted by splitting each cell at a sentence end, a semicolon, or a comma before `and`, `which`, `because` or `so`, with citations excluded. The work each one needs is not the assertion but the classification before it: a Failure behaviour cell mixes the behaviour with the reason it is that behaviour, and "silently skipping shrinks the recorded universe" is rationale rather than a property, so a sweep that asserts every clause would assert prose. That judgement is one no check can make, which is why the sweep is a checkpoint and not a pass. What is owed is a decision per claim, recorded on the claim, on which clauses its verdict reaches **Repointed from 4.1 to 4.11 on 2026-08-31**, because the second page roughly doubles the claim register, and the row is about the register rather than about a page. | 4.11 |
| 3.12 | **Two doc comments state behaviour the code does not have, and both were written in the checkpoint that recorded the behaviour correctly elsewhere.** `LabStatus.LatestRun` says "the band names the stage that went wrong rather than the stage that went last", where the read orders by `started_at DESC` within one outcome and names the last stage to reach it, which is the row above this one; its neighbouring sentence "the ordering was right and the population was a different night" is half of the same overstatement. `MigrationRunner.Apply` says its `throughVersion` bound "exists for one caller", where 3.12 added `StoreVersionGuardTests` standing a store up one migration short through the same parameter. Neither is a defect in behaviour and neither blocks: the first cannot be corrected before the tie-break question above is answered, because a corrected sentence would state a behaviour 4.1 may change, and the second travels with it rather than being swept alone. Recorded because a comment asserting the opposite of the code is the same instrument fault as a check asserting less than its name, in the one place nothing scans. **A third arrived at 3.14 and the 3.15 sign-off found it by running the thing.** `Test-IsWindowsSubsystemLauncher` in `tools/verify-phase.ps1` says it matches "on the file name and its directory" and reads only the directory, so every candidate under `System32`, `Sysnative` or `WindowsApps` is refused as the Windows Subsystem for Linux launcher whatever it is: pointed at `cmd.exe` the wrapper rejects it in those words. The refusal path itself is correct and reached, which is 3.14(f) working, and the behaviour probe beside it reports accurately; what is wrong is the reason given, and the reason is the operator's only guide when the gate will not start **Repointed from 4.1 to 6.8 on 2026-08-31**, because both describe the status band's stage ordering, which is read in the same pass. | 6.8 |
| 3.12 | **A night recovered after its scoreboard has run leaves that night's panels stating the night that was lost.** `ScoreboardBuilder` inserts `ON CONFLICT DO NOTHING` and has no update path, so rebuilding a past date writes nothing and refuses, which is 3.9(e)'s guard working. The consequence is that 2026-08-28's band 0 still reads `nightsRecorded` 1 and `setupsOnFile` 44, computed before the 73 setups of that night existed. The next night's panels are right and the stale ones are a dated measurement, so nothing is wrong in the store; what is missing is a supported way to rebuild a date's panels, and the stage's own message offers deleting them, which no declared writer does. **Repointed from 4.1 to 5.1 on 2026-08-31**, because the baseline is frozen at 5.1 and a panel that cannot be restated is a figure that cannot be corrected afterwards. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.12 | **The degraded mark's window is the session's calendar day, so a repair of the previous night falls inside it.** `RunLogger.IncompleteStagesOf` bounds from midnight in the session zone to the last instant of the same day, and the nightly for session N runs in the evening of N, so a stage rerun in the early hours to repair session N-1 is inside session N's window. Every setup recovered for 2026-08-28 carries `recheck` in its mark, from the partial recheck at 00:23 Eastern that morning repairing the fifteen cluster verdicts of 2026-08-27. The mark is not wrong by its own definition and it names a stage that has nothing to do with the night it is written on, which is a different statement from the one a reader takes from it. **Extended at the 3.12 sign-off to the status band, which now shares the window.** `LabStatus.LatestRun` resolves the newest run's session through the clock and bounds on `SessionBoundaries.At` and `StoreText.EndOfSession`, which is the repair the band owed and is the same calendar day this row is about. The recovery of 2026-08-28 ran at 00:03 Eastern on the 29th, so its run rows fall in the 29th's window and the band reads them as the 29th's night. The 3.12 entry anticipates the reading and gives the reason a session's rows appear after that session's own end of day; what this row adds is that the question is now asked of two readers rather than one, and an answer that moves the mark and leaves the band moves the two apart. **Repointed from 4.1 to 6.8 on 2026-08-31**, because the degraded count is a band 0 figure and is repaired with the panel that states it. | 6.8 |
| 3.12 | **The status band names the last stage to reach the night's worst outcome, not the first.** Within one outcome the read orders by `started_at DESC`, so on 2026-08-28 it named `vectorize` where `detect-long` had failed five minutes earlier and the other three failed because it did. The cause is what an operator needs and the consequence is what the band shows. Not changed at 3.12 because the clean case wants the opposite ordering, the last stage finishing clean being what says the night reached its end, so the rule is not one sort and the question is which reading each outcome deserves. **Repointed from 4.1 to 6.8 on 2026-08-31**, because same band, same read, same pass. | 6.8 |
| 3.13 | **`ControlSampler` was reported as executing its figures source twice per draw, and the shape the report named is not in the code today.** The row raised at 3.13 carried three items found in one read. Two are discharged at 4.2: `LabStatusView.SchemaBehind` collapsed a store ahead of the build into a store behind it and now says which way the mismatch runs, and the two stacked `<summary>` blocks in `ForwardReturnFiller.cs` and `StatedCountsCheck.cs` are gone, the second having left `StatedBetween` with no documentation at all, which it now has. **The third is neither discharged nor reproduced.** The row names `ControlSampler.Figures` executed once directly and once through `Pool`, beside a dead `drawnAt` parameter; there is no `Figures` member in that file, `Pool` and the draw are both handed one `ISessionFigures` constructed once at the top of the pass, and `drawnAt` is read where it is passed. Either the shape was repaired by another change and nothing recorded it, or the report described something else. **Repointed from 4.2 to 4.6 on 2026-09-01 rather than struck**, because a session removing a row on the grounds that it could not find the subject is the one act this corpus never permits: what is owed is a reading by someone with the 3.13 report open, and 4.6 is where the other verification rows are already classified | 4.17 |
| 3.11 | **The dispersion behind the 262-observation minimum is measured over a population the minimum does not govern.** `ForwardDispersion.Of` pools the cross-sectional variance of every name with history; the figure it produces is the bar for the effective count of flagged setup paired differences, and a flagged setup has cleared `moves-enough` at ADR at or above 5%, which is a hard volatility selection. Since the sample size goes as the square of the dispersion, measuring it over the wider population understates the minimum. The arithmetic reproduces exactly and the input is over the wrong rows. `DECISIONS.md` argues the figure is a floor on survivorship grounds; this is a second, unnamed term pushing the same way. A ruling rather than a repair, because it moves the number 3.6 turns on. **Repointed from 4.1 to the operator at the 3.15 sign-off.** Its reasoning is that a due point at 4.1 sits behind the checkpoint that spends the figure, which is the shape the classification below spends its length arguing against, and that the twin raised at 3.5 says the same thing about the same quantity from the other side and is already the operator's. Both are rulings on one number rather than repairs, so they are answered together or not at all. **Executed on 2026-08-30**, which 3.15 could not do: moving a row was a source edit until the counts came off their literals, and a session that commits code may not sign it off | the operator |
| 1.5 | **The empty tool set on the subscription path holds only as long as the test asserting it does.** It is a property of configuration rather than of there being no mechanism, so nothing structural prevents a later change handing the researcher seat a tool. Recorded in the phase 1 corrections entry and never given a row here, which is what `carried-obligations` found the moment it started reconciling: 6.5's own done condition already names the test, so the obligation was scheduled in substance and invisible to the table that is supposed to be where obligations live. | 6.5 |
| 1.6 | One `CONFIRMED` value per hand-picked ticker, checked against a charting platform's own readout. The derivation at 1.6 is a second implementation by the same author, which rules out a transcription error and not a shared misreading of a definition. The page that makes the comparison possible exists as of 1.10: `/chart/IESC` shows the three averages and the figures the lab stored, on the adjusted basis, in the two decimals a platform shows. What is left is a person reading a platform and writing the three figures down, which is the one step in this corpus a build session cannot do for itself. Moved from 1.12 to 2.11 at the 1.12 sign-off: it cannot be discharged by any build session, and holding a phase on an act no session can perform makes a due point permanent rather than pending. 2.11 is the first thing that reads these figures, since calibration is where a threshold is set against them. **Moved to 2.12 at 2.11**: the calibration ran and set no threshold, because the count distribution showed the band is out of the five thresholds' reach, so nothing yet rests on these three figures. 2.12 is where a person is already reading a screen. **Moved to the operator at the 2.12 sign-off**: it has now been moved three times, each time for a good local reason, and a due point that moves with every sign-off is permanent while reading as pending. It is an act no session can perform, which is the same shape as step 6 of the move and the vendor's reset boundary below, and those are the rows whose due point is honest | the operator |
| 2.11 | **The threshold adjustment the count distribution calls for, which the five thresholds cannot deliver.** The live run measured a median of nought candidates a night on both sides against a band of 5 to 60, so an adjustment fires. It cannot be a nudge, and the two sides fail the same way, which is what makes it structural rather than a long-side accident. The recording floors are healthy: a median of 44 long rows a night and 13 short over 2,016 names. The pattern test then admits 7 long rows in 631 sessions and no short row at all, and the same two shapes account for both. **The retrace cap**, 0.40 on both sides, against a median give-back of 1.060 long over 5,849 dips of 2 to 7 bars and 1.006 short over 3,448 bounces of the same length: the typical pullback gives back the whole thrust it is measured against. Both figures restated at the 2.12 sign-off, where the long one was found to have been taken over dips of two bars or more with no upper bound. **The give-up cap**, 0.5 daily ranges on both sides, against a median stop distance of 1.157 long and 1.191 short: a stop at the extreme of a two-to-seven bar move is being asked to sit inside half a day's range, which the geometry cannot produce. `trigger-near` is the counter-example that rules out "everything is too tight": its cap is 1.5 ranges against a median of 0.513, and 96% of measurable rows clear it. With the two shapes set to pass always, the remaining conjunction recounted at the 2.12 sign-off yields a median of 7 rows a night long and nought short, against a band floor of 5. So the band is out of reach on the long side by a hair and on the short side by an order of magnitude, and it is reachable at all only by removing two checks from each list. That is a change to what the strategy is rather than a calibration of it. Two readings, and the choice is not a build session's: the thresholds are wrong, or the quantities they are applied to are. The second has a specific candidate, which is that the geometry measures the thrust from the session before the scan hit, the right origin for a one-day scan and the wrong one for a twenty-session scan. That candidate is now a row of its own, raised at 2.11 on where the thrust is measured from, carrying the prediction it is judged against. **The two are worked in order rather than together**: a pass that moves the geometry and the thresholds at once can say nothing about either, and the adjustment here is once-only. Due before 3.1, because phase 3 must not start recording against thresholds nothing has calibrated **Judged at 3.0(c) and still open.** The corrected geometry moved the retrace half of the prediction and not the count half: the median falls from 1.060 to 0.5208 long and 1.006 to 0.4568 short over dips and bounces of 2 to 7 bars, and the nightly candidate count stays at a median of nought on both sides, 30 in total long over 602 sessions and nought short. So the geometry was one fault and something else holds the count down. **The once is unspent**, because a count five hundred times below the band's floor argues for a second wrong quantity rather than for thresholds set too tight, and the once cannot be re-spent. **Due at the operator**, on the same terms as the other three: it is a ruling rather than work, no build session can take it, and a due point that moves to the next checkpoint at every sign-off is permanent while reading as pending **Ruled by the operator on 2026-08-29, and the ruling is that the once stays unspent.** Of the four readings put to them, being spend the once now, hunt the second wrong quantity, re-derive the 5 to 60 band instead, or defer to the sign-off, the second was taken. So this row stays open and it is no longer open for want of an answer: what it now waits on is the identification of the second wrong quantity, on the reasoning already in this row, that a nightly count five hundred times below the band's floor argues for a second wrong quantity rather than for thresholds set too tight. The band is not re-derived either, which was the third reading and would have made the count correct by moving what counts as correct. **What this settles for phase 4**: the plan is written against flagged setups rather than passing ones, which is what 4.1 renders in any case, and nothing in phase 4 may assume a trade will ever fire until the second quantity is found. Recorded at 3.14 **Hunted on 2026-08-30, and the framing did not survive the measurement.** Over the 602 calibration sessions there is no *second* wrong quantity in the sense this row assumes. The long side has two: forcing `exit-tight` to pass takes the median from nought to 3 a night against a band floor of 5, and forcing `dip-shape` as well takes it to 12. The short side has none that any single relaxation reaches, and no pair either; nine short rows in 602 sessions reach `exit-tight` at all, so its funnel dies three gates upstream of where the long one does. **The premise that the two sides fail the same way is what breaks**, and it is what this row's "structural rather than a long-side accident" rested on: unconditionally `exit-tight` passes 1.29% of 32,533 long and 1.37% of 16,917 short, and conditionally it is 1.51% of the 1,981 long rows reaching it against 0 of the 9 short. What the row waits on is now a judgement rather than a search, and the figures it needs are in the entry of 2026-08-30 **Re-measured at 4.4 on 2026-09-01, and the gate this row points at is not the one that binds.** Both figures above were taken with `reached-ceiling` running two of its three clauses, and the reading built on them, that short's funnel dies three gates upstream of where the long one does, is what the re-measurement refutes. Given the third clause its maximum, so that every one of the 432 short rows reaching `reached-ceiling` is admitted, **431 rows reach `exit-tight` and 4 pass, 0.93%**, against the long side's **1.51% over 1,981 rows**, both over the same 602 calibration sessions; the funnel still ends at 4 survivors, median nought a night, on 4 nights of 602. So the deferred clause was never what emptied it, the per-row rates are comparable between the two sides, and what the short side is short of is rows reaching the gate rather than a gate set too strict. **The once is still unspent and no threshold is proposed here**: what changes is which quantity the ruling is about, and the figures it now needs are in the entry of 2026-09-01 | the operator |
| 2.9 | **The `CONFIRMED` gallery expectations.** The tier the corpus defines as what the chart page and the setup gallery produce has no entries at all, and no build session can make one: it is a person opening the gallery, reading a night, and writing down what they saw. The page is built and every property a session can assert about it is asserted, so what is left is the looking. Raised due at 2.12, where the gallery review is part of the sign-off. **Moved to the operator at the 2.12 sign-off**, which did not perform the review and says so: a sign-off session cannot page through a gallery either, so leaving this at a checkpoint would move it again at 3.7 | the operator |
| 2.11 | **The source-scan assertions nothing exercises, and the three scans written outside a check.** The sweep run at 2.11 found thirteen assertions the checks make by reading the shipped source: nine are exercised by a behavioural test, one by the rehearsal job, and three by nothing. The three are `writer-ownership`'s attribution of every write to its declared writer, whose behavioural form is `order-provenance` and starts at 4.6 for orders alone; `architecture-conformance`'s catalogue registration, where a registration present and unreachable would pass; and `coverage-reported`'s own scan for the trait, which rests on the phase report's coverage-record requirement rather than on a test. Three more scans live in ordinary tests and have no coverage record to declare a backing in, so the sweep can say they exist and not whether anything backs them: `SignalVectorizerTests`, `RegimeLabelerTests` and `SharedCandidateListTests`. Reported rather than fixed at 2.11 on purpose, because the fix is a behavioural test per scan and doing that in the pass that found them is how a sweep turns into a checkpoint. Due at 3.1, whose deliverable is the same kind of assertion: what a setup row may and may not have rewritten **A live instance was found and shipped at 3.12, which is an argument for pulling this earlier rather than a change to it.** `architecture-conformance`'s store-version claim read `Program.cs` for three patterns and every one of them is satisfied inside `WhyThisStageCannotRun` and `WhyTheStoreCannotBeRead`, which are declared in that same file. Deleting the block at the top of `Main` that calls the guard before the dispatch left all three patterns in place, the claim green and a detector free to run against a store two migrations behind it; the deletion was run and the claim did stay green until the claim was replaced. Its verdict is now a detector run through the CLI against a store one migration short, and both it and the new test fail on the call site alone. What that instance adds is that the sweep counted assertions whose subject is a declaration and this one's subject was a call, which no reading of the file distinguishes: an unbacked scan is not a list of thirteen risks of equal size, it is a list on which at least one had already failed | 4.17 |
| 1.1 | **Whether the vendor's own quota resets on the UTC date the lab counts against.** The daily call budget counts calls against the UTC date and the vendor's reset boundary is assumed rather than confirmed. Raised at 1.1, restated at 1.3 as still open, and never in this table either. If the two boundaries differ the lab can believe it has a full ceiling while the vendor has already begun refusing, which reads as a partial run rather than as a wrong assumption. It cannot be settled by a build session or by a sign-off session: it is a question for the vendor or an observation of one night's refusals. Due at the operator, on the same terms as step 6 of the move | the operator |
| 3.6 | **The nightly job runs only while the user is logged on.** Seventeen Task Scheduler entries were registered on 2026-08-27 and the scheduling obligation they discharge is closed. What is left is narrower and is a property of how they had to be registered: an unelevated shell cannot set an S4U principal or store a password, so the tasks carry an `Interactive` logon type and a logged-out evening is a lost night. **That is not a night like any other.** Every other stage can be rerun for its date, but `universe-build` writes the snapshot of who was listed, `UniverseSnapshotReader.Members` matches the date exactly with no fallback by design, and a delisted name is simply absent from tomorrow's symbol list. So a logged-out evening is a permanent hole in the evidence rather than a delay. Closing it is one elevated command to re-register with an S4U principal, which no build session can run | the operator |
| 3.5 | **The `band0.degradedRuns` panel states three different populations and reads red nowhere.** The figure counts distinct non-clean run instants, the count beside it is the number of nights, and the population label says "runs recorded". None of the three is the denominator of the other, so a reader cannot form the ratio the caption asks for. `ARCHITECTURE.html` says the panel "reads red if degraded nights exceed 5% of the record"; the caption is a static string and nothing computes the ratio or renders anything red. Fixing it means deciding which population the panel is over and saying so, then making the threshold a computed state rather than a sentence **Repointed from 4.1 to 6.8 on 2026-08-31**, because the scoreboard's own panels are next opened there, when band 3 is added. | 6.8 |
| 3.5 | **`ForwardReturnFiller` stores nought where an excursion is undefined.** `ForwardOutcome` returns null for MFE and MAE when the subject has no ATR, and the insert coalesces both to `0`. A row that could not be measured is then indistinguishable from one whose path never moved, and `mae_atr` of nought reads as a subject that never went against its entry. `WinRateCeiling` happens to be safe because it refuses a subject with no range before it reads the excursion, so this is a latent conflation rather than a live defect, and closing it means a nullable column and a migration **Repointed from 4.1 to 5.1 on 2026-08-31**, because an unmeasurable excursion read as nought is a stored value the frozen baseline would carry for ever. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.5 | **`intended_date` is a calendar step, so it differs across any weekend rather than across a holiday.** `SCHEMA.md` says the two dates "differ across a holiday" and the counter is named `AcrossAHoliday`, but the intended date is the as-of plus the horizon in calendar days, which for a horizon of 3, 5 or 10 is almost never a trading day. A synthetic run with no holidays in it counted 7,392 of 10,560 rows as slipped. The console line is worded accurately and the column is useful; what is wrong is the name and the sentence beside it, and the fix is to decide whether the intended date is a calendar step or a session step and say which. **It is also the one counter on `FillResult` still pooled across both subject kinds**, where the five beside it were split when the control fill landed, so it is where the population rule gets applied to this figure as well **Repointed from 4.1 to 5.1 on 2026-08-31**, because it decides which date a horizon lands on, which the frozen baseline's outcomes are measured from. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.5 | **Ratios are read back through the price crossing.** `StoreText` names two crossings for the same operation and says the naming is what stops a percentage being written where a fraction was meant. `ScoreboardBuilder` reads `return_signed`, `bound` and `achieved` through `StorageTextToPrice`, and `CeilingCalculator` reads `return_signed` and `mae_atr` the same way. The values are identical either way, so nothing is wrong today and the convention the rule rests on is already half broken **Repointed from 4.1 to 5.1 on 2026-08-31**, because a ratio crossing the price boundary is a value defect in the figures the baseline freezes. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.5 | **`CeilingCalculator`'s insert comment says it replaces its own row and the statement does nothing on conflict.** "A week recomputed replaces its own row and no other" sits above `ON CONFLICT (as_of, direction) DO NOTHING`, so the first write of a week wins and a recomputation is discarded. The behaviour is probably the right one and the comment is wrong, but which of the two is intended is a decision rather than a typo, because the bound is stated as recomputed weekly. **And the same statement makes this an instance of a general form 3.9(e) implemented in one place only.** That checkpoint made `ScoreboardBuilder` report what it attempted and what it skipped and fail when the two are equal, because a rebuild that writes nothing and reports a clean run is the failure shape this lab keeps producing. `CeilingCalculator.Insert` returns void, counts no skip and calls no `CountSkipped`, so recomputing a week that already has a row is exactly the no-op wearing a clean run that 3.9(e) was written about, one stage over. The general form is that **a stage whose insert can silently do nothing reports attempted and skipped and fails when they are equal**, and today it is stated nowhere and implemented once. What is owed is the form written down and applied to every stage in that position, which is this one and any later stage whose insert carries `ON CONFLICT DO NOTHING`; `ForwardReturnFiller` is deliberately not in it, because an elapsed horizon has one answer and its no-op is the property rather than a fault. Extended rather than given a row of its own, because the comment and the asymmetry are the same statement read two ways and two rows would be worked through separately **Repointed from 4.1 to 5.1 on 2026-08-31**, because a recomputation silently discarded is a stored figure that cannot be corrected after the freeze. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.5 | **Nothing reconciles the `surface-claims` stub against the text its producer emits.** The check renders authored JSON, so a claim whose `mustCarry` text is also written into the stub proves only that the template does not swallow the string. If `ScoreboardBuilder`'s wording changes and the stub does not, the check stays green while the page carries different words. Raised while adding exactly such a claim: its `mustCarry` was reconciled against `accumulation.starved.withheldBecause` by hand, which is the thing that should not be by hand **Repointed from 4.1 to 4.11 on 2026-08-31**, because same register, same checkpoint, and the two are one repair. | 4.11 |
| 3.5 | **The minimum sample of 262 was sized for a normal-theory test and the test actually run is a studentised bootstrap.** `MinimumSample.Of` is `n = ((z_alpha + z_beta) * sigma / delta)^2`, which is the sample size for a z-test. What band 1 reads is a bound from a studentised moving-block bootstrap over `nights / 10` blocks, which is two blocks at the twenty-session floor and five at fifty-two, and its tails are wider than the normal's. So reaching 262 effective observations does not deliver the 90% power the figure was ratified at, and the correction widens rather than narrows the requirement. **The direction matters**: it is under-power, so the risk is reading a flat band 1 as "the pattern has nothing in it" when the sample could not have found it, and CLAUDE.md's own reasoning for choosing 90% over 80% is that a false negative here has no downstream. The fix is a power calculation against the procedure actually run, and the two inputs it turns on are judgements this corpus says belong to a person, so it is raised rather than taken. Surfaced by the sign-off review's own re-review of the interval replacement | the operator |
| 3.5 | **`ControlSampler` can draw a name that did not trade on the night it is drawn for.** Eligibility is an `indicator_daily` row at the as-of, and `IndicatorEngine` writes one for a universe member whose last bar is older than the session, because `DailyBarReader` takes the last 150 bars at or before the as-of without requiring one dated on it. So a halted or vendor-omitted name is indicated, is drawable, and becomes a control. The outcome is no longer mis-measured, because the fill now refuses a subject whose window does not start on its own session and counts it, but the set is then thinner than five and nothing says which name went missing or why. `TierClassifier` already refuses a name whose latest bar is not the as-of session, which is the shape the draw wants. Found by the review of the control fill, which reproduced the whole chain through the shipped stages **Repointed from 4.1 to 5.1 on 2026-08-31**, because the control population is what every band 1 figure is a difference against, and the baseline freezes those figures. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.5 | **`ForwardReturnFiller` re-walks every subject ever recorded on every night, so its cost grows with the square of the accumulation.** Both subject queries are bounded only by `s.as_of <= @as_of`, with no lower bound and no filter for subjects whose four horizons are already written, and the fill issues one path query per subject per night. Every already-written row is recomputed and discarded by `ON CONFLICT DO NOTHING`. At about eighty-two flagged setups a night and ten controls each, night 200 walks roughly 180,000 subjects to write the few thousand that closed. Correctness is unaffected and the immutability rests on the key rather than on the query, which is why this is a cost rather than a defect. The fix is a lower bound on the as-of, or a left join excluding subjects already complete **Repointed from 4.1 to 6.1 on 2026-08-31**, because SignalBackfiller computes across the entire stored setup history in one run, which is the same quadratic walk at the same scale. | 6.1 |
| 3.5 | **A subject whose horizon can never close is counted as "not yet elapsed" for ever and drops silently out of the control mean.** `ForwardOutcome.Of` returns null both for a horizon that has genuinely not elapsed and for a subject whose own series is too short to ever reach it, which is a control delisted, halted for a run of sessions, or dropped from the universe so no more bars arrive. The two states are indistinguishable in the counter and the control simply contributes nothing, so a setup whose controls were the ones that got halted is compared against however many survive without anything saying the comparison narrowed. The new `WithoutABarOnTheirOwnSession` counter catches the near neighbour of this, being a subject with no bar on its own session, and not this **Repointed from 4.1 to 5.1 on 2026-08-31**, because a control dropping silently out of the mean biases the difference the baseline is frozen on. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.5 | **`interval.*` is tiered `DERIVED` over a restatement that now mirrors the shipped algorithm closely enough that the tier claims more than it holds.** Reproducing a seeded resampling scheme to four places requires the same generator, the same seed, the same block order and the same tail convention, so `tools/derive-indicators.py --interval` is close to a transcription and agreement between the two says little beyond that neither was mistyped. That is exactly how the defect it replaced survived. The property tests in `PairedIntervalTests` are what actually verify the scheme now, and the question this raises is whether a tier that reads as independent verification should be claimed for any figure produced by a seeded algorithm, or whether such figures want a tier of their own **Repointed from 4.1 to 5.2 on 2026-08-31**, because VariantScorer is where the interval starts deciding whether a variant is accepted, so a tier claiming more than it holds costs a decision rather than a report. | 5.2 |
| 3.6 | **`tools/nightly.ps1` has no macOS counterpart and rebuilds from the working tree on every slot.** The corpus pairs `ci.ps1` with `ci.sh` because the two platforms need different syntax, and the nightly runner has only the Windows half while the move to the Mac is still an open obligation. It also invokes `dotnet run` without `--no-build`, so a slot compiles the working tree at 17:15 and a half-finished edit costs the night | the move |
| 3.8 | **`security.sector_resolved_at` is when the lab asked, and every reader treats it as when the fact became true.** The two are the same for a bar, whose observation is dated by the market, and are not the same for an attribute looked up lazily: a sector is true for years before anyone asks. **Attempted at 3.8 and stopped; scoped at 3.9(h) and still not started.** The plan is "The lazily-resolved attribute, scoped and not started" above, which carries the counts, the three ways the attribute could be dated and their costs, and the before-or-after-phase-4 question. It is not restated here, because a row and a plan that both describe the work are two places one decision lives. What the row carries is the due point: the operator, because it decides whether a night's evidence may be completed after the fact, and the conservative answer is the one in force | the operator |
| 3.8 | **The slot script depends on Windows PowerShell and two of its parts behave differently under pwsh.** `tools/nightly.ps1` is the Windows half of a pair whose macOS half does not exist, and the machine it runs on executes `powershell.exe`. Two parts turn on which interpreter is running. `Invoke-Stage` exists because Windows PowerShell wraps a native command's stderr in a NativeCommandError under `Stop`, which pwsh does not do, so under pwsh the function is harmless and unnecessary rather than load-bearing. And the exit-code handling turns on `$LASTEXITCODE` surviving a pipeline, where pwsh 7.4 additionally raises on a native non-zero exit under `Stop` through `$PSNativeCommandUseErrorActionPreference`. The `slot-diagnostics` job now runs the script under both and prints what each did, recorded rather than required, because requiring them to agree would fail for a reason nobody has decided and pinning one would assert something about an interpreter the scheduler does not use. **First reading, 2026-08-28**: with the fix in place both interpreters keep the message and the stop line and both exit 1, so they do not diverge on the outcome. They diverge on whether `Invoke-Stage` is load-bearing, which is the thing a macOS counterpart would have to decide about rather than inherit. What is owed is that decision, at the point the counterpart is written, since that is when a second interpreter stops being hypothetical | the move |
| 1.11 | **Step 6 of the move procedure only, copying `appsettings.Secrets.json`.** The rest of the rehearsal now runs on every push: the `rehearsal` job on `ubuntu-latest` exercises the whole pipeline on a case-sensitive filesystem, which is the fault class neither development machine can see, then copies the store the fixture replay built and verifies it on arrival with counts and an integrity check. What a container cannot do is carry a gitignored file between two machines by hand, which was only ever verified in the negative: confirmed not to travel with a clone, never confirmed to have been copied. Due at the actual move, not at a checkpoint | the move |
| 3.7 | **`ControlSampler` threads a `drawnAt` into `Figures` that the query ignores.** `Figures(connection, asOf, drawnAt)` takes the draw instant through three call sites and then binds `@drawn_at` to `$"{asOf:yyyy-MM-dd}T23:59:59.999Z"`, so the bound is the end of the session day and the parameter is dead. The end-of-day form is the corpus's own convention and every reader in `PullbackStrategyLab.Data` uses it, so the figure the query returns is right; what is wrong is a signature claiming a bound it does not apply, in the one method whose doc-comment cites (see: A reader's signature does not establish point-in-time; the query does). Bind it or drop the parameter; do not leave the third reading **Repointed from 4.1 to 5.1 on 2026-08-31**, because the draw's own bound, in the stage the frozen baseline's controls come from. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.7 | **Eight checks besides `point-in-time` reconcile a hand-named list against the corpus in one direction only, and the missing direction is the one the corpus grows past.** Surveyed at the 3.7 sign-off across every check on the roster. `surface-claims` reads a claim file and never asks which corpus sentences claim visibility; `stated-counts` and `pinned-constants` read authored claim lists and never ask which stated counts and constants have no entry; `fixture-inputs` names three endpoints where the vendor client calls four, `fundamentals` being the fourth and captured only by luck; `bar-append-only` names three bar tables and never asks the migrations for a fourth; `shell-executable` names four entry points and never reads `tools/`, where `derive-indicators.py` already carries a shebang and mode 100644; and `decision-resolves` and `no-superseded-citation` read nine documents and `src/**/*.cs`, where `.github/workflows/ci.yml` and `fixtures/expectations.json` both carry a `see:` citation neither one scans. `coverage-reported`, `check-completeness`, `fixture-replay`, `ci-parity` and `architecture-conformance` already run both ways and are the model. What is owed per check is the second direction or a recorded reason it cannot have one, not eight repairs | 4.17 |
| 3.8 | **The repair made 2026-08-27's `cluster` column a mixed population, and the rows it could not touch are damaged in the same way as the fifteen it could.** The recomputer forms its cluster over the night's whole scan population, which is the right basis and is now asserted. But it runs after the sector walk completed, so the fifteen repaired rows are counted over all **234** of that night's scan names while the other **29** carry what `clusters` computed at 18:15 over the **148** resolved at the time. Measured: all 15 repaired rows match the 234-name count and none matches the 148-name count; 28 of the 29 untouched rows match the 148-name count, and one, `INFQ`, matches neither at 4 against 6 and 13, so a third population exists that nothing has explained. **20 of the 29 would move** if recomputed, and **two would change verdict**: `FOUR` short and `HTFL` long both record a fail at a cluster of 1 and are 2 over the full population, which passes. Those two are the fifteen's own defect wearing a number instead of a null, and they are invisible precisely because a partial input produced a plausible value. `passed_all` is untouched either way, since `cluster` is recorded and never gating. Two things are owed and only the second is a build session's: a ruling on whether a verdict computed from a partial input may be revisited, which the stage refuses by design and the correction rule does not currently permit; and, failing that ruling, any figure stated over that night's `cluster` column reports the split rather than one number (see: Long and short are never pooled into one figure) **Repointed from 4.1 to 5.1 on 2026-08-31**, because a mixed population in a stored column is exactly what a freeze makes permanent. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.9 | **The store readers name the session zone rather than reading the configured one.** `StoreText.EndOfSession(date, zone)` takes the zone explicitly and every caller passes one, but a store reader is a static helper with no configuration to read, so nine reader methods across five readers pass `SessionBoundaries.UsEquities` while the ten stage and API sites pass `_options.SessionZone`. Those are the same string today: `PullbackStrategyLabOptions.SessionZone` defaults to the constant rather than restating it, and a test asserts every `appsettings.json` sets that same value, so configuration cannot diverge without the suite failing. **The guard is real and it is a guard rather than a design.** The fix is to thread the configured zone through the nine reader signatures, which reaches 66 call sites, 44 in shipped source and 22 in the suite. It was not done in the same pass as the boundary itself because a signature change on every store read, landed beside a change to what every bound means, would make one of the two impossible to review **Repointed from 4.1 to 5.1 on 2026-08-31**, because a session boundary read from a constant rather than from configuration decides which bars every frozen figure was taken over. **One of the ten due before the freeze.** Registering V0 at 5.1 freezes the baseline, and a stored figure repaired after that closes every open version as unresolved and starts a new generation, so this repair is free until the freeze and costs the accumulated variant record for ever afterwards (see: An approved proposal creates a new version from zero, and a running version is never edited). | 5.1 |
| 3.0(f) | **The 160-observation minimum sample for a selection variant was computed as though the observations were independent, and they are not.** `ARCHITECTURE.html` states 160 paired setup observations as the bar `VariantAdmitter` writes into a version's pre-registration, which (see: Targets and minimum samples are written at creation and are immutable) then makes unrevisable, so a figure that is too small is frozen into every version admitted under it rather than being corrected later. Ten-day forward labels overlap and same-night setups share a market factor, so 160 rows carry fewer than 160 observations' worth of information and the honest figure is larger. **Not moved at 3.0(f), and those reasons stand**: the figure is pinned, moving it on an unmeasured ratio would repeat the same error in the other direction, and nothing reads it until phase 5. What closes it is the measured design effect and serial term from the first scoreboard run, which is the same pair of quantities `PairedInterval` already discounts 3.6's effective count by, so it is the second of two minimum samples in this corpus sized as if observations were independent. **This row is the obligation, and until 3.14 it existed only in the record.** 3.0(f)'s `Carried` block wrote it as "due before 5.1" and no row was ever added. `carried-obligations` could not see it: its due-point pattern read a literal `due ` optionally followed by `at `, so "due before" matched nothing, and the check whose entire subject is an obligation nobody scheduled was itself holding one for a phase and a half. A due point the pattern never matched never enters the count, so the floor under that count could not show it either. The pattern, its five unread forms and its missing proof are repaired at 3.14 | 5.1 |
| 3.3 | **The market-capitalisation clause of `tradable-shortable` is exempt in every reconstructed run, so the short side has been measured over history through a gate with one clause missing.** `SCHEMA.md` names it as one of the three reconstructions every calibration row rides on, and buying the delisted history closed the first of the three and not this one (see: Delisted daily history is bought so a reconstructed walk is not confined to survivors). Closing it is a shares-outstanding history per name at one fundamentals call each, which with the stored closes reconstructs a market capitalisation per date. **It is scoped here and deliberately not started.** It spends the same ceiling as the delisted purchase, so it follows that purchase rather than running beside it, and a market capitalisation reconstructed for a name the store does not hold is quota spent on a row nothing reads. **The narrowing criterion is the short gate set as it will run after 4.4, not as it runs today**, and the difference is not cosmetic: `reached-ceiling` is a disjunction whose third clause needs an anchored volume-weighted average price and does not run until 4.4, so narrowing against today's two clauses passes strictly fewer names than the finished gate would. Every name the third clause alone would have admitted would be left without a capitalisation for ever, invisibly, and undoing that costs a second sweep at the same price. So the sweep is narrowed on the gates whose clause set is settled and `reached-ceiling` is held open rather than applied at two clauses, which is a superset of the finished funnel and is the only form of the criterion that cannot bake a temporary shape into the store. **Due at 5.3 because that is what leans on it**: ReplayHarness re-filters the stored history with a new rule, and a short selection rule replayed over rows produced under a gate missing a clause is screened against a funnel the live detector does not have | 5.3 |
| 4.5 | **The `plans` slot was missing from `tools/nightly.ps1` and four more slots were refused by the script's own parameter set, so five stages of phase 4 were never dispatched.** Found at 4.5 and closed in the same commit, and it is rowed rather than only fixed because what is owed outlives the fix. `plans` was in neither the slot table nor the `ValidateSet`, so PlanBuilder was registered, dispatched by the worker, scheduled by RUNBOOK at 18:30 and run by nothing; `spread-open`, `spread-close`, `watchlist` and `vwap` were declared as slots and rejected by the attribute, so the two spread passes of 4.3, the watchlist stage of 4.1 and the averages of 4.4 exited on a parameter binding error before reaching the worker. **`tools/ci.*` was green and the phase report was GREEN throughout**, which is the seventh failure shape one step back from the store: the dispatcher is a file in this repository, so it was always a subject a check could have had, and no check had it. `slot-roster` now reconciles the slot table, the parameter set, the worker's advertised stages and RUNBOOK's schedule in every direction, and `ComponentReachabilityTests` reconciles the roster in the direction it was missing. **What is owed is not the fix but the damage.** The four rejected slots have been in the schedule since their checkpoints landed, so the nights between are nights those stages did not run, and reading which is a matter of opening the night logs and the live store. A build session may not do either | the operator |
| 4.2 | **`decision-resolves` reads the git index, so its population is the staged tree and never the working one, and a green states nothing about the files a build session has just written.** The scan takes its file list from `git ls-files` on purpose: the reasoning recorded with it is that an untracked scratch file must not be able to add a citation. That is right and it has a second half nobody wrote down. A file a session has created and not staged is invisible in both directions, so the check runs green over exactly the files most likely to carry a citation nobody has resolved yet, which is every new file in every checkpoint. **The row is about the population and not about the instance.** It was found at 4.2 by citing a decision that did not exist and watching the check pass, and staging the file caught it only because the staging happened first; a session that stages last gets a green run over the files it wrote and a red one after the commit. This is the survivorship shape CLAUDE.md names: a check that errors loudly gets fixed because it blocks, and one that silently narrows its own scope keeps passing. **The cheap interim is that the check reports how many tracked-but-modified and untracked text files it did not examine**, so its green states its own coverage rather than stating nothing, and a session can read the number before it commits. The full repair is a decision about which tree the scan should read and is not taken here. **Due at 4.6**, beside the source-scan rows already classified there as blocking a done condition, because the property is the same one: an assertion whose subject set is narrower than its label | 4.17 |

### What the two due at 4.1 were

**Both are discharged, at 4.1, on 2026-09-01, and no row in the table above falls due there.** They
survived the repointing of 2026-08-31 because both were 4.1's own subject rather than rows that
landed on it, which is what that pass was sorting for, and this is the check that pass was predicting.

**Which clause of a multi-clause gate failed**, raised at 2.9 by the gallery review and repointed
once with its reason. `CheckResult` now carries a verdict and a number per clause, so
`tradable-shortable` says which of liquidity, price, capitalisation and listing age a row missed and
the watchlist names it beside the greyed row. **The row predicted the fix would move the `setup.*`
and `check.*` expectations at 2.6, 2.7 and 2.11 and it moved none of them**: the stored JSON gained a
field, and every expectation over those rows records a derived figure rather than the JSON, so
nothing that was frozen changed. The prediction was reasonable and wrong, and the reason it was
wrong is worth more than the prediction: an expectation is only moved by a change to what it
measures, and none of them measured the shape.

**The direction `surface-claims` does not reconcile**, raised at 3.12 and named at 3.7 before that.
It now reads the corpus back against the claim file: a sentence claiming something appears on a
surface is declared, or it is exempted by name with the reason it is not a claim about a surface.
The reverse read found two claims held by nothing, which is what the row said would be there.

**The fixture permits are still nought, and the sentence saying so is kept here.** It was written
in the section this one replaces, and it is a stated count rather than a note about the pile that
emptied: `fixtures/expectations.json` names seven frozen-only checkpoints under `frozenOnly`, of
which nought still rest on an open obligation. `fixture-replay` fails a permit whose due checkpoint
`PROGRESS.md` already records, in those words: "that checkpoint shipped without discharging it and
nothing said so at the time". So the first CI run after this entry lands turns red nought times over
on that account, where before 2026-08-30 it would have turned red nine. It is restated rather than
carried by accident: rewriting the section around it deleted it, `stated-counts` failed naming the
sentence it could no longer find, and that is the registry doing the one thing a prose count cannot
do for itself.

**Two things about it are worth keeping.** The first pattern matched thirty-four sentences of which
half were prose that happened to hold a surface word and a verb, and a scope answered by its own
exemption list is a scope measuring the exemption list; requiring the verb within thirty characters
of the surface it names brings it to sixteen, and the floor was lowered with that reasoning rather
than left where it was recorded. And the reverse read **passed on its first run**, which is the shape
this corpus distrusts most, so a proof beside it removes one declaration and requires that claim's
own sentence to surface.

### What the nineteen due at 4.6 were

**Emptied at 4.6: six discharged and thirteen repointed to 4.17.** This section is now a record of
what the pile was and what happened to it, on the shape the 4.1 section took when it emptied, and the
four figures `stated-counts` read from it are registered against the 4.17 pile below instead. A count
in a section with nothing left to count is a sentence written to keep a claim passing.

**The six discharged are the seven the classification called 4.6's own subject and the three it called
blocking, less the one that was neither wholly.** `writer-ownership` now tracks braces, so a write is
attributed to the type whose braces enclose it rather than to the last declaration above it.
`price-storage-form` parses `ALTER TABLE ADD COLUMN`, which is where sixteen columns had been arriving
unread. `foreign_key_check` runs inside the migration's own transaction, so a rebuild that would orphan
a row is refused rather than reported after the commit. SCHEMA's column tables are reconciled against a
built store in both directions, which is what found nothing wrong and would have found the five columns
that were. `calibration_setup` states its six-column divergence from `setup` rather than claiming
sameness. And `SetupJournalTests` holds the two behavioural halves it claimed.

**The seventh was partly 4.6's and the rest of it is on the record**, being the 2.11 row about
source-scan assertions nothing exercises. One of its three is closed: `writer-ownership`'s attribution
is now backed by `order-provenance`, which runs the gate over an authored session, reads every row back
and asks whether a run of that stage was open when it was written. The other two and the three scans
outside a check are untouched and travel to 4.17 with the row.

**The thirteen that remain are what the classification called landed there, and the ruling is that they
move together.** Every one of them is a verification or record defect with no natural owner among the
checkpoints phase 4 has left: 4.7 to 4.12 build execution machinery and a page, and 4.13 is a sign-off
that may not commit code. Repointing them one at a time to the next checkpoint that plausibly cares is
exactly what put all nineteen here, so they go to a checkpoint whose deliverable is that pile and
nothing else. That is the decision this row's done condition asked for, taken once with the whole table
in front of it.

### What the thirteen due at 4.17 are

Thirteen of the fifty-four rows above fall due at 4.17, which is the largest pile in the table and is
the 4.6 pile moved whole. It is registered here in the commit that creates it, on the reason the 4.6
pile was: a classification written after the first time its counts go stale is a classification nobody
could have read.

**Unlike its two predecessors this due point was chosen rather than arrived at.** 4.1's pile and 4.6's
both formed because a verification row with nowhere obvious to go lands at the next checkpoint that
plausibly cares, which is a due point nobody picked. 4.17 exists to hold exactly these rows, so the
question a later session asks about it is whether the pile was worth a checkpoint rather than why it
ended up here.

| Group | Count | What it means |
|---|---|---|
| A check that reconciles in one direction only | 6 | The 3.7 row naming eight such checks, of which 4.6 closed three and left five. `decision-resolves` taking its population from the git index rather than the working tree. `point-in-time`'s statement half, which cannot see a statement built by interpolation, and the one such statement that reads `daily_bar` with no bound. `path-casing`'s literal scanner, which reads neither verbatim nor multi-line raw string literals while its own comment says it reads both. `architecture-conformance`'s catalogue scan, whose named backing is itself a scan. And the 2.11 residue, being two unbacked scan assertions and three scans written outside a check |
| A recorded figure or sentence that is wrong | 4 | `SectorResolver`'s two figures over a population other than the one named beside them. `ControlSampler` reported as executing its figures source twice per draw, in a shape the code does not have today. RUNBOOK step 3 telling the operator to create a data root outside the repository where the decision names two roots inside it. And the ten phase-3 commits that replaced lines in a spec and recorded no prior text in CHANGELOG |
| A component that does more than it says | 3 | Both `recheck` rows, being `--expect` compared after every correction has been written so it reports a set that moved, and a dry run that opens a write connection and can mark the night degraded. And the detectors writing a trigger and a give-up price for a geometry the same row calls absent |

**None of the thirteen is about a risk cap, an order or a fill**, which is the same sentence the 4.6
section carried and the reason the pile moved rather than shrinking. What changed at 4.6 is that the
seven whose subject was 4.6's are gone, so what is left is a pile with one subject: the verification
harness and the record, which is what 4.17 is for.


### The eleven that are the operator's, and what each one blocks

Listed together because they are answered together or not at all. Each was moved here by a session that had a good local reason to move it, and a due point that moves at every sign-off is permanent while reading as pending. None can be taken by a build session: each is a judgement, a reading of a screen, or an act on a machine.

**It was nine until 3.9(b), eight until 3.14, nine again, ten on 2026-08-30, nine by the end of that day, ten again on 2026-08-31, nine by the end of that one, and eleven at 4.5.** Two things moved and they moved in opposite directions, which is why the count came back to where it started rather than nothing having happened. `ForwardDispersion` arrived, repointed here from 4.1 at the 3.15 sign-off, and it is the twin of the row raised at 3.5 that was already present. 3.3 left, because the operator ruled it: the tight control set draws from any session sharing the market mood, so the judgement is closed and the draw is a build session's work, due at 3.6. The branch-or-`main` question left earlier and is answered rather than moved: PR #5 merged at `6f27926` and the job runs from `main`. Departures are named rather than only deleted, because a list that silently shrinks reads the same as one nobody is working through. **The two that arrived at 4.5 were not moved here for want of a due point**, which is the reason this section says every earlier arrival was. The fetch window is priced and what is left in it is how large a store the operator wants; the nights the four rejected slots did not run are read off a live store no build session may open. Both are judgements or acts on a machine, which is the test this list is drawn on.

**3.3 left, returned and left again, and the three movements are one argument working itself out.** It left on 2026-08-30 when the operator ruled that the tight set may reach across sessions, which closed the judgement and made the draw a build session's work. It returned on 2026-08-31 when the reconstructed read measured what that reach costs: the market factor common to one night stops cancelling, the tight comparison came back worth about a seventh of the loose one over identical rows and nights, and band 1's minimum sample went 202 to 1,030 forward nights out of reach against a condition of twenty sessions. It left again the same day, when the operator reversed the reach on the reading that a mood held constant across a night's pool is controlled exactly rather than left unchecked (see: The tight control set draws within the night, because a within-night draw controls the market mood exactly). **A row that arrives with a measurement and leaves with the ruling that measurement asked for is not churn**, and it is recorded at its full length rather than netted to nothing, because a table showing only the net shows a day on which nothing happened.

**Two of the counts in this section read eight while the table held nine, from 3.14 until 2026-08-30.** Nothing could see it: `stated-counts` named the obligations table's total, the count due at 4.1, the classification's three groups and the permits, and never this table. A registry cannot catch a figure nobody registered, which is the row raised at 3.7 about checks that reconcile a hand-named list in one direction only. The heading's count is now derived from the table below it, and the table is reconciled against the obligation rows due at the operator, so the two cannot disagree again.

The column that matters is the last one. Seven of the eleven block no scheduled work and are cheap to leave open; four sit in front of work that is already scheduled, being the three that move a number 3.6 spends and the fetch window short's twenty-session count waits on.

| Raised | The question | What it blocks | What stalls without it |
|---|---|---|---|
| 1.1 | Does the vendor's quota reset on the UTC date the lab counts against | Nothing today. The nightly job spends about 803 against 5,000, so a boundary six hours out of step still could not exhaust it | Nothing until the budget is close to binding, which no phase currently plans |
| 1.6 | Three `CONFIRMED` indicator values, read off a charting platform | The `CONFIRMED` tier has no entries at all, so nothing in the corpus is verified against anything outside it | Nothing blocks. It is the tier's only possible source and the page that makes it possible has existed since 1.10 |
| 2.9 | The `CONFIRMED` gallery expectations, from paging through a night | Same tier, other half. A person reading the gallery and writing down what they saw | Nothing blocks |
| 2.11 | **Ruled twice, and what is owed is no longer a quantity.** The reading of 2026-08-29 was that the once-only adjustment stays unspent and the second wrong quantity gets hunted. The hunt of 2026-08-30 answered it and answered past it: the binding constraint is neither the pattern's rarity nor the universe's size, and the gate that empties the short funnel is a three-clause disjunction running two, the third deferred to 4.4 with VwapEngine, against a long detector carrying no deferred clause at all. So the decision left is not a threshold and not a quantity. **It is whether the once is spent now, against a funnel with a known deferred clause in it, or held until the gate runs as documented.** **Ruled on 2026-08-30: held until 4.4.** Spending it against two clauses of three would calibrate a threshold to a gate that is about to change, and the once cannot be re-spent, so the cheaper order is to let the clause land and re-measure. The figures are in the record and are not copied here (see: Long and short are never pooled into one figure) **The hold ended at 4.4 and the re-measurement is on the record.** The 2026-08-30 funnel was reproduced figure for figure over the same 602 sessions, and the third clause was then given its maximum: even admitting **every one of the 432 short rows that reach `reached-ceiling`**, which is the most a disjunct can do, the short funnel ends at **4 survivors over 602 sessions**, median nought a night. **So the deferred clause was never what emptied the short funnel.** The gate that does is `exit-tight`, at 0.93% over 431 rows against the long side's 1.51% over 1,981, which is the first time that rate has been measured on anything but a sample of nine. What is owed is unchanged in kind and better informed: whether the once is spent, and on what. Nothing here proposes a threshold. | **Band 2 and everything downstream of it.** At a median of nought candidates a night, no trade is ever planned, so phase 4 builds a trading layer nothing reaches. **It does not block 3.6**, corrected at 3.9(i): band 1 counts effective observations over every flagged setup rather than over candidates, so the two are different populations and both figures hold | **Phase 4 is buildable and untestable against live rows**, unchanged by the ruling. What the ruling changes is that this row no longer waits on a measurement: it waits on 4.4, which is inside phase 4, so the hold and the phase are not independent of each other and whoever plans phase 4 is reading both |
| 3.5 | 262 was sized for a z-test and a studentised bootstrap is what runs | The minimum sample, in the direction of under-power. Reaching 262 does not deliver the 90% the figure was ratified at | **3.6**, but weakly: it moves the threshold rather than the method, and the panel reports the count either way |
| 3.6 | The tasks run only while the user is logged on | A logged-out evening is a permanent hole in the evidence, because `universe-build` writes the snapshot of who was listed and nothing can reconstruct it | Nothing blocks, and it costs a night every time it fires. One elevated command closes it |
| 3.8 | Does a lazily-resolved attribute carry a validity date separate from its lookup instant | Whether a night's evidence may ever be completed after the fact, beyond the narrow case the lateness bound admits | Nothing blocks. The lateness bound reaches the case that mattered, and this is the general question behind it |
| 3.11 | Whether the 262-observation minimum is re-derived over the population it governs | The minimum sample, in the same direction as the row raised at 3.5 and for a second reason. `ForwardDispersion.Of` pools every name with history, where the minimum governs flagged setups that have cleared a hard volatility selection, so the figure understates | **3.6**, on the same terms as its twin. Repointed here from 4.1 at the 3.15 sign-off, because a due point behind the checkpoint that spends the figure is not a due point |
| 4.4 | Which fetch window `IntradayFetcher` buys, being one session as built, eight, twenty-seven, or the vendor's 120-day maximum | **Short's twenty-session count, and through it band 1 on the short side.** `VwapEngine` prices an anchor only where the store reaches back to that row's own swing, and a swing sits three to twenty-seven sessions back, so at one session a night no anchor is ever in reach and the count never starts | **Nothing else.** No option costs a vendor call more than another, the window is a one-off per name rather than a nightly multiplier, and 4.5's row states the four with the rows each would add. What it costs is the size of the store, which is why it is here |
| 4.5 | Which nights the four rejected slots did not run, and whether anything of them is recoverable | **Nothing forward.** The dispatcher is fixed and `slot-roster` reconciles it on every CI run. What is open is backward: `spread-open`, `spread-close`, `watchlist` and `vwap` were refused by the script's own parameter set from the checkpoints that added them, so some number of nights carry no spread sample and no session average | **The two spread passes are the unrecoverable half**, because a quote has no history to buy back, and the averages can be recomputed from stored minutes any evening after. It is here rather than with a build session because it is read off the night logs and the live store, and a build session may open neither |
| 3.14 | Whether the two live rows carrying a superseded `cluster` value are put right | Nothing. Both verdicts are unchanged, because the threshold is 2 and both the wrong number and the right one clear it, so what is wrong is a recorded value and not a gate | Nothing blocks. It is two rows of two nights' evidence, it costs a restore and a re-run of one date, and it is here because a build session may not touch the running store |

**2.11 was the one to answer first and it has been answered**, on 2026-08-29, which leaves it the only one of the nine that stalls a phase and no longer the one that stalls for want of a ruling. What it waits on now is the identification of a second wrong quantity, which is work rather than a judgement, and the operator's remaining half is spending the once when that quantity is found. **The two that would move a number are 3.5 and 3.11**, both of them 3.6's, and they are one quantity seen from two sides. 3.3 was a third twice, and moved a definition rather than a number both times: ruled on 2026-08-30, raised again on 2026-08-31 with the cost of that ruling measured, and ruled the other way the same day. The other six cost a tier with no entries, a night lost when somebody logs out, two rows of evidence carrying a number nobody would compute today, and a modelling question nobody is currently paying for. **The two added at 4.5 cost differently from all nine**, and it is worth saying which: the fetch window costs disk and nothing else, and the slot damage costs evidence that has already been lost rather than evidence at risk, so answering it recovers nothing and only says how much.

---

## Elapsed time

Roughly 35 to 45 coding sessions. The calendar is set by accumulation, not by code: phase 3 runs until band 1 has the evidence to be read, phase 5 needs a few months more before proposals settle, and the loop probably closes once somewhere around month seven.

**Every figure in that sentence is an estimate and none of them is a trigger.** They are here to say roughly how long this takes, which is a fair thing for a plan to guess at. **3.6 fires on a measured sample and nothing else**, and the three months that used to sit in this paragraph is exactly the kind of estimate that got read as a derivation once already.
