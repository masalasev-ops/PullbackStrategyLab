# BUILD_PLAN.md

Six phases. Each checkpoint states its deliverable, its done condition, and how it is verified. The general definition of done is in `CLAUDE.md` under "Definition of done for a checkpoint" and applies on top of everything here.

Cross-document references cite a heading, never a section number, for the same reason decisions are named rather than numbered: a misremembered number resolves to the wrong place silently.

Every phase produces two things: a screen you can open, and a generated report saying whether what was built matches the architecture. Neither replaces the other. The screen is how you notice something looks wrong; the report is how you know, and how a build session checks its own work without asking anyone.

The harness is checkpoint 1.7, built before most of what it checks exists. From there every checkpoint adds its expectations to the golden fixture, and every phase sign-off requires `tools/verify-phase` green with nothing listed as unexamined.

---

## Phase 1 — Ingest and charts

Nothing here depends on any open question. Start immediately.

**`git init` before 1.1.** There is no repository yet, and the done conditions, the progress entries and the merge rule are all written against commits.

| # | Deliverable | Done when |
|---|---|---|
| 1.1 | Solution `PullbackStrategyLab.sln` under `/src`: Core, Data, Worker, Api, Web, Tests, all namespaced `PullbackStrategyLab.*`. Clock abstraction, config binding via one shared extension, store pragmas set in that same extension, `RunLogger` as sole writer of `run_log` | `git init` done first. `tools/ci.*` green. Api has no transitive reference to Worker, asserted against compiled deps, with `PullbackStrategyLab.Tests` the one declared exemption. **The CI matrix runs windows and macos from this commit onward**. Config is wired by one shared extension used by Worker, Api and Tests alike, pinned by two tests: an environment variable overrides a value present in `appsettings.Secrets.json`, and a project starts cleanly with no secrets file on disk (see: Secrets live in a gitignored appsettings.Secrets.json, registered before environment variables) |
| 1.2 | Clock abstraction proven on both platforms | A test resolves `America/New_York` and asserts it is behind UTC by at most a day, with both bounds read from the clock. The matrix already exists from 1.1, so this checkpoint adds the test rather than the runners (see: Every line of code runs unmodified on Windows and on Apple Silicon macOS) |
| 1.3 | UniverseBuilder from the exchange symbol list, common stock only, $5 floor and the liquidity floor | `universe_member` populated. **The survivor count is measured and recorded in PROGRESS**, since the backfill call budget is one call per survivor and no figure for it exists yet. `universe_snapshot` written per night |
| 1.4 | DailyBarIngestor, one bulk request per night, idempotent | Re-running the same date changes no row. Bar count and date range recorded. A test asserts the connection reports `journal_mode=wal` and `foreign_keys=1`, since both are silently off by default |
| 1.5 | ActionIngestor, and the EMA rebuild-on-split path | A synthetic split fixture forces a full recompute for that ticker and only that ticker |
| 1.6 | IndicatorEngine: EMA 9/21/50, ADR20, ATR14, median dollar volume | Values for three hand-picked tickers match an independent calculation to 4 decimal places. **The independent calculation and its source are recorded in PROGRESS**, and those values carry forward as the fixture's first `DERIVED` expectations at 1.7 rather than being checked once and discarded (see: Every fixture expectation records how it was produced, and only the independently derived ones verify anything) |
| 1.7 | **Phase report harness** and the golden fixture: real bars for 30 tickers over 250 sessions, committed, with expected indicator values beside them, **each carrying its tier**. `tools/verify-phase` runs the pipeline over the fixture, diffs against expectations, parses the architecture tables, located by heading text rather than by position, and asserts each claim against the code, then writes `artifacts/phase-report.html` and `.json` | **Openable, and machine-readable.** Report shows three sections: doc conformance, fixture diff, coverage. Every architecture claim is pass, fail, or **unexamined**, and unexamined is not a pass. The fixture diff is broken down **by tier** rather than reported as one number, and the report states how many expectations changed since the last commit alongside how many passed. **The fixture's bars are `CAPTURED` from real vendor responses**, which the backfill at 1.6 makes possible, and the synthetic split case from 1.5 is kept and marked `AUTHORED`. **The report breaks inputs down by tier beside expectations**, and a path with no captured input reads as unexamined however many authored cases pass (see: Fixture inputs record where they came from, and a path a live run exercises needs a captured one). **1.7 back-fills expectations for 1.1 to 1.6 with their tiers**, since those checkpoints predate the fixture and could not meet done condition seven when they landed. That discharges the obligation raised at 1.1 as far as tiers reach and no further: done condition seven also asks each checkpoint for one expectation that is `DERIVED` or `CONFIRMED`, 1.3, 1.4 and 1.5 came back frozen-only, and so did 1.7 itself, so the obligation stays open against those four and is carried below. From here on every checkpoint adds its expectations to the fixture (see: Every phase ends in a generated phase report, not in a page somebody looks at) (see: Every fixture expectation records how it was produced, and only the independently derived ones verify anything) |
| 1.8 | Web shell: routing, layout, the five-item nav, the status band, and one shared candlestick component | **Openable.** Every page is reachable and renders its empty state. Nothing is stubbed with fake data, because an empty page that says so is honest and a page of invented rows is not |
| 1.9 | IndexIngestor | `index_bar` populated for SPY, QQQ, IWM |
| 1.10 | Chart page: any ticker, candles, three averages, ADR readout | **Openable.** You compare it against a chart you already trust and agree |
| 1.11 | Migration rehearsal end to end | `RUNBOOK.md` "Moving the store to another machine" executed on a second machine, all ten steps, counts matched. Result recorded in PROGRESS |
| 1.12 | Phase sign-off | Fresh session reviews. Findings batched into one pass, and `tools/verify-phase` green with nothing unexamined |

**1.11 is not optional and not deferred.** It is cheap now and it is how you discover which paths are hardcoded and which secrets do not travel, at a point where losing the store costs nothing.

---

## Phase 2 — Detection

| # | Deliverable | Done when |
|---|---|---|
| 2.1 | Four things, in this order, because the spec pass authors what the code is written against: **(a)** BUILD_PLAN's malformed obligation row and the parser that dropped it; **(b)** the spec pass, whose deliverable is a `Signals` section of SCHEMA giving every signal its formula and its source columns, plus the phase 2 underspecifications settled as decisions; **(c)** the examined floor moved from per check to per scope; **(d)** the `DERIVED` expectations owed at 1.3, 1.4, 1.5 and 1.7 | Every signal traces to named stored columns. Any that does not is a finding, not an assumption. Every check states a floor under each scope it names, with the size-of-corpus scopes marked as context and never summed with the scope carrying the property, and the narrowing that passed at the phase 1 sign-off fails. The four frozen-only permits are gone from `fixtures/expectations.json` rather than re-dated |
| 2.2 | SignalVectorizer and the frozen signal row shape, `setup_signal`, with migration 011 creating `setup`, `calibration_setup` and `setup_signal`. Plus the out-of-scope coverage naming rule, moved here from 2.6 because this is where it bites | Written once, never updated by the vectorizer. Asserted four ways: a rerun writes nothing, a restated bar does not revise a frozen value, the store's own key refuses a second write, and no `UPDATE` against `setup_signal` exists in the shipped source. Every `active` signal in SCHEMA's library is either frozen here or names the checkpoint that supplies what it reads, and that checkpoint has to exist and be open. Every out-of-scope coverage item names a checkpoint, a price, or a stated reason nothing can close it |
| 2.3 | ScanEngine, six scans, three per direction, with migration 012 creating `scan_hit` and the magnitudes shared in Core. The six thrust signals move from awaiting a checkpoint to frozen | Hit counts per scan per night recorded. Each scan takes the top 50 by its own magnitude, ranked, with ticker as the tiebreak so the boundary does not depend on row order. Every magnitude reads the adjusted basis, asserted against a split inside the month window rather than described |
| 2.4 | TierClassifier, writing a later observation of the session rather than updating the row IndicatorEngine wrote. The ladder grade moves from awaiting a checkpoint to frozen | Ladder grade on every universe member per night. The three grades are a partition, swept over every arrangement of the four numbers rather than sampled, and `mixed` is a grade rather than a bucket for names the stage failed on. A grade the store refused is counted and reported rather than absorbed |
| 2.5 | RegimeLabeler, two scores summed, with migration 013 creating `regime_daily`. The three mood signals move from awaiting a checkpoint to frozen | Both raw scores stored alongside the label, and both raw ladder counts with them. **Label filters nothing**, asserted against the shipped source rather than stated: no component outside the labeller, its reader and the vectorizer names either extreme. Neither extreme is reachable without both scores agreeing, and an unmeasurable tracker scores zero rather than minus one |
| 2.6 | LongSetupDetector, ten checks, all results recorded, with SectorResolver and ThemeClusterer behind the cluster check. The geometry and the rules live in Core so the nightly run, the calibration run and a test share one implementation. The last seven signals move from awaiting a checkpoint to frozen | `check-completeness` passes: every recorded setup has a result for every check, with the check names read from ARCHITECTURE's own gate lists and reconciled in both directions. **Per check, how many fixture setups passed and how many failed, with any one-sided check named individually in the report**; more than two one-sided is decided at this checkpoint rather than deferred |
| 2.7 | ShortSetupDetector, ten checks, with the three that are not sign flips written as their own rules. Plus the two things all twenty gates first make possible: the degeneracy proof over the gate list, and the authored boundary suite that closes the one-sidedness raised at 2.6 | Same as 2.6, both directions. No setup row carries a direction its detector does not own. A name a detector cannot read gets an error row and the run is recorded partial, rather than being skipped into a night that merely looks lighter. Every gate on both lists fails on an input that makes its quantity absent, zero or undefined, proved over the gate list so a gate added later inherits it and a gate with no degenerate case fails the proof. Every gate has a pass and a fail from a case built to sit either side of its own threshold, no check reported one-sided, and the boundary cases marked `AUTHORED` so nothing reads them as evidence about the market. `reached-ceiling` runs two of its three clauses and the third is out of scope naming 4.4 |
| 2.8 | SetupCapper, verb `cap`, 60 a night, 40 long 20 short, unused slots released, with the arithmetic in Core so the release rule can be swept rather than sampled | Truncation recorded with the pre-cap count, so a night can say whether the cap bound rather than only what it kept. The release rule holds over every arrangement of the two counts up to twice the cap, and which side is offered the spare first never changes the answer, which is what makes the absent tiebreak a property rather than an omission. Ranked within a direction and never across, on give-up distance ascending with ticker as the tiebreak. **The cap applies to the shared candidate list**, asserted now while there are no versions: `setup` carries no column naming one, and no public read of it takes a name of any kind |
| 2.9 | Setup inspector, the gallery page: prev/next, filter by failed check, agreement capture, with `LabSetups` in the Api answering a night both directions and taking the one write the read surface makes | **Openable.** You page through a night's setups by keyboard and record agree or disagree per setup. The two sides are two lists on the wire and two blocks on the screen. Every check appears on every card, passed and failed alike, and a check handed nothing shows what was absent rather than a blank. The thumbnails come from the one shared candlestick component. The recording is a form post and works with the script removed, which is what makes the keyboard paging a convenience rather than the mechanism. A filter that hides everything says it hid it rather than showing an empty night |
| 2.10 | `point-in-time` as a named CI step, three halves: the readers' signatures, the statements written by hand beside them, and the behaviour | A deliberately future-dated observation causes a loud failure, in CI rather than at runtime, and the test is permanent rather than a manual break-and-revert. Every public read on a store reader takes a date, with the one calibration read exempted by name and its reason. Every hand-written statement selecting from a table that carries an observation stamp bounds that stamp, or is exempted by file with its reason. And the same session read from both sides of one correction's instant returns two different figures, because one figure alone is satisfied by a row that is simply missing |
| 2.11 | One-time calibration. Calibration mode carries the session in memory: each name's figures through `IndicatorEngine.Calculate`, its ladder through `TierClassifier.Grade`, and the session's six scan rankings through `ScanMagnitudes` and `ScanEngine.Top`, all four shared with the nightly stages that own them. The market-cap clause of `tradable-shortable` is exempted by name. The run over the golden fixture becomes the fixture's expectations; the distribution a threshold is read from comes from the live universe | Detector rows **into `calibration_setup`, never into `setup`** (see: The evidence store holds only setups flagged forward, never setups reconstructed from history), asserted by comparing the evidence store before and after rather than by asking whether it is empty, and asserted again on a rerun of the same range. **The fixture's thirty names cannot answer for a threshold and the record says why**: thirty candidates against a scan breadth of fifty puts every name inside every scan on every session, so every geometry check fails on every row and the count is nought by construction. The distribution is recorded per side as a raw count and as a rate per name per session. If the median falls outside 5 to 60 per side, the thresholds marked "phase 2 count check" are adjusted **once**, before phase 3, recorded as a dated event with before and after counts and naming which distribution it was set against. **Unless the run shows the band is out of the five thresholds' reach**, in which case the finding is recorded with the pass rate of every check and the distribution of every threshold's own quantity, and the adjustment waits on a decision: spending a once-only adjustment on a threshold set that cannot reach the band spends it for nothing, and the condition that fires it was written assuming a nudge would do. Every short verdict a calibration run writes records that the cap clause was exempt, and a forward night still fails a name with no resolved capitalisation |
| 2.12 | Phase sign-off | Fresh session. The gallery review is part of it, and `tools/verify-phase` green with nothing unexamined. **Plus one reading pass over what the fixture's composition cannot be evidence about.** `thrust` is the worked example: thirty fixture names against a scan breadth of fifty puts every one of them inside every scan on every session, so the check's own quantity is nought on every fixture row and the authored gate cases carry its threshold alone, while the live calibration run reads it at 100% only because it is one of the four checks in the recording floor. Both instances of that shape so far were found by an accident in a different checkpoint rather than by anything looking for them, so the pass asks which other checks stand where this one does and records the answer whether or not it is acted on |

**2.11 is calibration, not tuning.** At this point no forward return exists anywhere in the store, so there is nothing to fit toward. It is a row count and nothing else.

**And the rows it counts are not evidence.** A historical detector run has no record of who was listed on those dates, because the nightly universe snapshot only starts when the lab does. Delisted names are absent, so every reconstructed setup carries survivorship bias. Counting is unaffected; measurement would be destroyed. The rows go to `calibration_setup` and the evidence store stays empty until the first forward night. After phase 3 begins, these thresholds move only through the normal proposal route.

**2.9 is where the real time goes,** and it should. Paging through the gallery and disagreeing with the detector is the actual transfer of the strategy into code.

---

## Phase 3 — Measurement, and the decision point

| # | Deliverable | Done when |
|---|---|---|
| 3.0 | Seven things, in this order, because the instrument has to exist before the thing it measures and the spec pass authors what the code is written against: **(a)** the geometry instrument, being `DERIVED` expectations over `PullbackGeometry.Of` on non-degenerate inputs; **(b)** the thrust scan recorded on the setup row; **(c)** the thrust-window correction, alone, and the prediction settled; **(d)** the surfaces sweep; **(e)** the remaining hygiene obligations; **(f)** the spec pass settling control draw, ceiling arithmetic and the interval method as named decisions, plus the SCHEMA repairs phase 3's tables need; **(g)** a check result carrying a value per clause | Every obligation due here is discharged or repointed with its reason, and none names a checkpoint whose deliverable is not its work. (a) lands before (c), and (c) moves the geometry and nothing else, because a pass that moves the geometry and the thresholds together can say nothing about either. **The prediction is settled by the calibration re-run and recorded in PROGRESS, never encoded as an assertion**: the fixture cannot produce a claim about 2,016 names over 631 sessions, and a threshold in the suite saying the median retrace is below 1.0 is a number a later session adjusts to make green. **The once-only threshold adjustment is not spent here whatever the prediction does**, because a nightly count five hundred times below the band's floor argues for a second wrong quantity rather than for thresholds set too tight, and the once cannot be re-spent |
| 3.1 | SetupJournal finalised | Setup rows immutable after write, except by a correction the lateness bound admits, and asserted in both directions: nothing rewrites a plan or a gating verdict, and a correction whose input arrived more than the bound after the setup's own session is refused (see: A late answer is attributed to the session it was fetched for, up to a recorded lateness bound) |
| 3.2 | ForwardReturnFiller: 1, 3, 5, 10 sessions, signed by direction, plus MFE and MAE in ATR | Holiday handling correct: next trading day used, actual date stored beside the intended horizon |
| 3.3 | ControlSampler: loose and tight control sets per flagged setup | Controls drawn from names that cleared the liquidity floor and were not flagged. Match quality recorded |
| 3.4 | CeilingCalculator | Bound computed from the actual outcome distribution, recomputed weekly |
| 3.5 | ScoreboardBuilder and the Lab scoreboard page, bands 0, 1 and 2 | **Openable.** Flagged against both control sets with confidence intervals, rank-decile curve, ceiling gap |
| 3.6 | **The decision point. Parked, and not a sign-off condition** | Both band 1 conditions reported reached, which is at least twenty sessions **and** at least 262 effective observations, per direction and per control set, never pooled. Then a person reads the scoreboard and decides whether to continue. Due when the panel reports both, which the panel says every night (see: The minimum sample is 262 effective observations, ratified at two points and 90% power) |
| 3.7 | Phase sign-off | Fresh session, and `tools/verify-phase` green with nothing unexamined. **Signs off 3.0 through 3.5 and does not wait on 3.6** |
| 3.8 | **The correction pass, after the sign-off and before accumulation runs on.** Seven parts, in this order because the diagnostic has to survive before anything is diagnosed by it: **(a)** the slot script's lost stderr, with a check and the runner job that exercises it; **(b)** one call spent on the name that threw, captured as the fixture's first failing-shape response for `fundamentals`; **(c)** SectorResolver skipping a bad name rather than dying on it, with the skipped count in the run log; **(d)** the sector walk run to completion and what the failures have in common; **(e)** the correction rule, superseding immutability as it was written; **(f)** CheckRecomputer and the fifteen setups an input stage's death left null; **(g)** the obligations this pass raises rather than takes | A stage that writes to stderr and exits non-zero puts both its message and the line saying the slot stopped in the night's log, proved by a real stage denied a real precondition on a runner rather than by hand, and under the interpreter the registered tasks actually execute. One ticker's failure costs that ticker and no other, and a run that spent calls without product is not recorded as clean. **The repair is bounded to inputs the night had**, asserted rather than trusted: a recompute reading a value stamped after the setup's own date fails and says so, and a corrected row records that it was corrected, with the date and the reason, so a later reader can exclude corrected rows. **What stays forbidden is unchanged**: no trigger, stop, size or gating verdict is ever rewritten |
| 3.9 | **The post-pass, in run order, before phase 4 planning resumes.** Nine parts: **(a)** the three questions the repair owed, being the population its cluster was formed over, what lateness is measured from, and what forty-four is; **(b)** the merge, and the nightly job pointed at the branch it merges to; **(c)** the session boundary, closed at the end of the session's own day in the session zone, separately from the conflation it is not; **(d)** `scan_hit` given an observation stamp, with the blast radius of refusing a null measured before any bound moves; **(e)** an in-place rebuild that writes nothing refusing rather than reporting success; **(f)** a claim for every behaviour change this pass and the last one made; **(g)** the obligation ledger reconciled move by move against its commits; **(h)** the session-date migration scoped and dated rather than started; **(i)** 2.11's funnel reported with a denominator per stage | The cluster a repaired row receives is asserted to be formed over the night's whole scan population, by a test in which a member with no setup row at all is counted. Lateness has one stated origin, the session's own end of day, and every figure in the corpus measured from anything else is corrected. **No point-in-time bound is built by appending a UTC literal to a session date**, asserted by a claim that fails if one returns and by a behavioural test that fails against the old expression in January and in July. `scan_hit` is stamped going forward and the count of existing rows and the reads that would refuse them are reported before the bound changes. A rebuild that writes nothing exits non-zero naming what it skipped. Every behaviour change is mapped to the claim asserting it or to a recorded reason it is not an architecture claim, one line each. The obligation count reconciles line by line with a commit against every move |
| 3.10 | **The verification repair, in dependency order, because every part after the first is verified by it.** Eight parts: **(a)** `tools/ci.sh` reporting a failing step's own exit code, and each shipped step function run against a command that fails; **(b)** `point-in-time` reading the store readers it excludes, and the unbounded reads that turns red; **(c)** the two session bounds still built on a fixed UTC offset, and the guard whose two patterns could not match them; **(d)** `carried-obligations` reconciling a live tail that is not empty by construction; **(e)** the claims that pass by comparing nothing, counted apart from the claims that pass; **(f)** `bar-append-only` and `writer-ownership` reading migration SQL, where every table rebuild in this project lives; **(g)** the scope floors re-recorded against a run rather than left where 2.6 put them; **(h)** the shipped-code defects (a) through (g) reveal | **A failing step fails the run, on both scripts, proved by running the shipped function rather than by reading it**, because a scan for the corrected shape passes against the broken one. Every public read on a store reader is read by `point-in-time`'s statement half, and a read that names its stamp without bounding it fails: naming the column in a `SELECT` list is not a bound. No point-in-time bound is built from a fixed UTC offset in any syntactic form, asserted by a guard that fails against the constructor form and the concatenation form alike. `carried-obligations` states the count it reconciled and that count carries a floor, so a filter that empties itself fails rather than passes. A claim decided by comparing two empty collections is reported as unexamined rather than as passed. Every floor in `fixtures/checks-baseline.json` is re-recorded from a full run on the date the file states, and the file states the date of that run. **Each defect (h) discharges is fixed with the check that would have caught it, in the same commit**, so no part of this checkpoint lands a repair whose guard arrives later |
| 3.11 | **The findings 3.10 recorded and did not start, in the order the harm runs.** **(a)** the setup row expressing an absent trade geometry as absence rather than as nought, and `SignalVectorizer` freezing no signal the detector recorded as absent; **(b)** the `degraded` clause of the vendor-ceiling rule, which has no column anywhere; **(c)** the status band reading one `run_log` row of about eighteen; **(d)** the decile panel's denominator, which is pooled where the rank it divides is per direction; **(e)** `CeilingCalculator` given the tests it has none of | **A quantity the detector recorded as absent is absent in the store, in the frozen signal and on the screen**, asserted by a setup whose geometry is degenerate carrying no geometry signal and no figure derived from one. The fixture already holds such a row, so this is asserted over the golden fixture rather than over an authored case alone. **A stage that stopped short marks the setups it degraded**, and a read that reports a night's outcome reports the worst outcome of that night rather than the last one written. **The decile denominator is the direction's own allocation**, and the panel states which. Each part lands with the check or the test that would have caught it |
| 3.12 | **The sign-off's findings, in the order the harm runs.** **(a)** a stage refusing to run against a store at a version other than the build's, and the band saying so; **(b)** the status band's night, bounded in the session zone rather than on the UTC date; **(c)** the vendor-ceiling claim asserting the third of its three clauses; **(d)** the two rendered statements 3.11 added and no surface claim held; **(e)** the night of 2026-08-28 recovered from the inputs it already had, or recorded as lost with the reason; **(f)** the seam 3.11(f) dated to a session on which nothing was flagged, corrected | **A stage that needs a column the store has not got refuses before it opens the store, naming both versions**, proved by standing a store up one migration short and running a detector against it, with the three exemptions asserted rather than listed. **A night is the night in the session zone**, proved over run rows on the installed schedule, which crosses UTC midnight: a test population that does not cross it cannot tell the two bounds apart, and that is why the ones written at 3.11 did not. Every clause of a corpus sentence a claim names is asserted by that claim, or the claim says which clause it does not reach. Every statement 3.11 put on a page is named in `fixtures/surface-claims.json` and read off the rendered page. **The recovery is bounded to inputs the night already had** and writes no row for a session whose inputs were not present, and what it produced is recorded against what the first night produced. A record corrected by a new dated entry, never edited |

**3.5 was reopened on 2026-08-27 by the sign-off review, before the nightly job's first run.** Three defects blocked it and all three are fixed under this heading rather than as a new checkpoint, because none of them adds a deliverable: each is a stated behaviour of 3.2, 3.4 or 3.5 that the code did not have.

- **Nothing ever wrote a control forward return.** ForwardReturnFiller bound `subject_kind` to the literal `setup`, so band 1's difference series was empty on every night, `n_effective` was pinned at nought, and 3.6's trigger could never fire. 3.5's done condition asks for the flagged population measured against both control sets with confidence intervals, and no interval could ever be produced.
- **PairedInterval was not a bootstrap.** Every draw was one lattice rotated, so ten thousand draws was bit-identical to N draws and the intervals ran two to three point seven times too narrow.
- **A positive adverse excursion was read as its own size.** A subject that never traded against its entry was judged stopped out, which removes from the ceiling exactly the rows a perfect forecaster selects.

**The sign-off that found them did not fix them, and the session that fixed them cannot sign them off.** Both halves are the fresh-session rule.

**3.6 is parked, and 3.7 does not wait on it.** The two ask different questions and only one of them is answerable today.

**3.7 signs off that 3.0 through 3.5 hold and meet their done conditions.** That is a claim about code, documents and a fixture, it is what `tools/verify-phase` was built to answer, and it is verifiable now (see: Every phase ends in a generated phase report, not in a page somebody looks at). **3.6 decides whether the pattern works**, which needs a sample that does not exist yet and which no report can produce. Holding the sign-off on it would hold finished, reviewable work behind an accumulation nobody can hurry.

**Phase 1 is the precedent and it is the argument.** It signed off with the `CONFIRMED` indicator values outstanding, on the reasoning that an obligation whose due point moves at every sign-off is permanent while reading as pending. The same reasoning applies here in the other direction: a checkpoint that cannot be reached for months, left as a sign-off condition, makes the sign-off itself permanently pending while every part of it that could be checked already passes.

**What parking does not change, said outright so it is not read as dropping.**

- **3.6 still has to happen.** It is the project's own question and phase 4 should not start without its answer.
- **Its trigger is measured rather than waited out.** Both band 1 conditions, reported by the panel, not a date and not a judgement about whether enough time has passed.
- **The panel is what fires it.** Band 1 shows its raw count, its effective count and the minimum every night, so the trigger arrives on the screen rather than in somebody's diary. A trigger a reader cannot see is a date in disguise.

**Two conditions, and both are needed because they are settled by different things.** Twenty sessions is what the block bootstrap needs before an interval exists at all, and no number of rows substitutes for a session. 262 effective observations is what the decision needs, and no number of sessions substitutes for the evidence. A panel can reach one and not the other, so the trigger names both.

**What has to happen before 3.6, none of which is code.**

1. **Schedule the nightly job.** Scheduling lives outside the application, so no checkpoint starts the clock, and nothing accumulates until it runs. The one item whose absence stops everything, and it is carried as an obligation rather than left in this list.
2. **Accumulate**, against the branch the phase lives on if 3.7 has not merged it, otherwise against `main`. Band 1 fills from about eighty-two flagged setups a night; band 2's decile curve fills from candidates and at the calibrated thresholds it will stay near empty, which is a finding rather than a fault.
3. **Rule on whether the tight control set may draw from neighbouring sessions**, before 3.6 rather than after, because the tight comparison is the number 3.6 turns on. Carried as an obligation.
4. **3.6 itself**, once the panel reports both conditions reached.

**How long step 2 takes is not known and is deliberately not estimated.** The effective count is the row count divided by the measured design effect and discounted by the measured serial overlap, and neither has been measured against a real series. If the pairing removed most of the common movement the trigger arrives near the twenty-session floor; if the ten-day overlap dominates it is months. That is a wide range, it is the honest one, and the panel closes it within about three weeks of the job running.

**Step 2 used to say "three months" and no longer does, and the reason is worth keeping.** That figure was written before anything was measured and read as a derived quantity ever since. It was not: it rested on about twelve setups a night, band 1 as built fills at about eighty-two, and it is a paired comparison against same-night matched controls, so the market factor that dominates cross-sectional correlation cancels rather than needing to be waited out.

**A calendar is not restored here later.** If the effective count climbs slowly the answer is that the evidence is thin, which is a finding about the strategy rather than a reason to read the scoreboard anyway on a date. If it climbs fast the decision arrives early, which was always the point of measuring instead of estimating.

**The threshold ruling is not on this path and that is deliberate.** It was, while it was unclear which population band 1 measured. `ARCHITECTURE.html` settles that: its worked night is twenty-two flagged, fourteen passing every check, and all twenty-two followed up, so the evidence population is the flagged one and band 1 fills whatever the thresholds do. What the thresholds decide is whether anything is ever *traded*, which band 2 reports and phase 4 acts on.

**3.6 is the project's own question, answered.** If the tight-control comparison does not clear zero, the pattern has nothing in it beyond owning stocks in uptrends and everything built after this point would be optimising noise. If the ceiling gap is near zero, selection has no room and the loop should point at execution instead. Either answer is worth having before phase 4 starts, and both arrive before the trading and research layers exist.

---

## Phase 4 — Trading

| # | Deliverable | Done when |
|---|---|---|
| 4.1 | **Watchlist page** and WatchlistPublisher, long and short in divided panels, ranked, failed checks greyed with the failing check named, conflict banner | **Openable, and first in this phase on purpose.** Plans do not exist yet, so it renders the flagged setups with their computed trigger, stop and distance. Everything built after this checkpoint shows up on a page you already have rather than in a log |
| 4.2 | IntradayFetcher, every flagged setup | Minute bars stored for all 60, not only the planned ones |
| 4.3 | SpreadSnapshotter, twice per session | The only intraday job. 120 calls. Failure to capture is unrecoverable and is logged as such |
| 4.4 | VwapEngine, session and anchored | Values match an independent calculation on a fixture day |
| 4.5 | TriggerResolver over SessionReplayClock, point in time enforced within the day | A test proves the resolver cannot see a later minute than the one it is evaluating |
| 4.6 | RiskGate, all six caps, both directions, sizing off fixed notional equity | Blocked orders written with reason, including the contention case. A test asserts that when two plans trigger and only one slot remains, the earlier trigger fills and the later is blocked (see: Plans are resting orders and fills go in time order when the caps bind). `order-provenance` passes |
| 4.7 | PaperBroker and the fill model | Gap-through recorded as a loss greater than 1R and tagged. Never clamped |
| 4.8 | PositionManager, two rule sets | Long trail and short trim are separate code paths, not one routine with a sign flag |
| 4.9 | PlanAudit and TradeJournal | Planned stop against executed stop recorded on every trade |
| 4.10 | LossClassifier, four causes | Every closed loss classified. Unclassifiable is itself a recorded category, not a silent skip |
| 4.11 | Trade journal page, split long and short | **Openable.** Intraday chart per trade with trigger, fill, stop and exit drawn |
| 4.12 | Retire the mockups | `SCREENS.html` deleted (see: The corpus is eight documents plus one artefact, and a ninth requires retiring one). Any layout detail it carried that the built UI relies on has moved into the built UI, not into another document |
| 4.13 | Phase sign-off | Fresh session, and `tools/verify-phase` green with nothing unexamined |

---

## Phase 5 — Variants, without any AI

| # | Deliverable | Done when |
|---|---|---|
| 5.1 | `variant` store, VariantAdmitter registering it and VariantResolver deciding which are live, PlanBuilder produces one plan per live variant | Baseline V0 registered and frozen |
| 5.2 | VariantScorer, two families scored differently | Selection on forward return, execution on R. A variant spanning both is rejected at admission |
| 5.3 | ReplayHarness | Re-filtering the stored history with a new rule completes in seconds and reproduces the baseline's own selections exactly |
| 5.4 | HoldoutRegistry, eight quarterly windows | Spent windows recorded with date range, purpose and outcome. A spent window cannot be re-spent |
| 5.5 | Research ledger page | **Openable.** Variants, samples, targets, status |
| 5.6 | Two hand-written variants per family running | Paired differences accumulating and visible |
| 5.7 | Phase sign-off | Fresh session, and `tools/verify-phase` green with nothing unexamined |

**5.3's acceptance test is the important one:** running the baseline's own rule through the harness must reproduce the baseline's historical selections exactly. If it does not, the harness and the live detector disagree and every replay result is worthless.

---

## Phase 6 — The loop

| # | Deliverable | Done when |
|---|---|---|
| 6.1 | SignalBackfiller | A new signal computes across the entire stored setup history in one run |
| 6.2 | SignalAdmissionTest | A signal is admitted only if it tightens outcome-similar neighbourhoods, and rejected above 0.70 correlation unless stored as a residual |
| 6.3 | TwinPairFinder | Z-scored over the trailing 250 setups. Pairs under 0.5 distance with outcomes over 15 points apart |
| 6.4 | ContextPacker, versioned, with the planted null signal | Pack version recorded on every proposal. The null appears in the conditional tables |
| 6.5 | ResearcherSeat, built against the narrow interface, with both implementations present and selected by configuration | A test asserts the subscription implementation is constructed with an **empty tool set**. A test asserts `ANTHROPIC_API_KEY` is absent from the environment. A proposal row carries the **configured model**, the **transport** used and the **served model** string the response reported. Budget or plan-limit exhaustion queues the job and returns nothing, on both paths, and never a partial proposal (see: The researcher transport is a configuration switch between subscription and API key, over a deliberately narrow interface) |
| 6.6 | ProposalRegistry, rule proposals and signal requests | Abstention is a valid recorded outcome, not a failure |
| 6.7 | AcceptanceGate | Reads only status and resolution date. Cannot touch a target |
| 6.8 | Scoreboard band 3 and the Pack comparison page | **Openable.** Proposal hit rate by pack version |
| 6.9 | Phase sign-off | Fresh session, and `tools/verify-phase` green with nothing unexamined |

**6.5 has a trap, and it survives the transport switch.** `ANTHROPIC_API_KEY` stays banned from the environment on both paths, for a reason that now applies to each of them: on the subscription path its presence silently defeats plan auth and bills API rates, and on the API path the key belongs in configuration like every other secret, so one in the environment means two places supply the same credential and nothing on the surface says which won. Assert its absence at startup and fail loudly.

**A proposal citing the planted null signal fails that pack version immediately.** No proposal from that version is admitted.

---

## The lazily-resolved attribute, scoped and not started

An observation stamp records when the lab asked. Every reader treats it as when the fact became
true. For a bar the two are the same, because the observation is dated by the market. For an
attribute looked up lazily they are not: a company's sector was true for years before anyone asked,
so a name resolved on the 28th is invisible to the session of the 27th although the sector it
carries was as true on the 27th as it is now.

Written up at 3.9(h) and **not started**. What follows is the scope, the counts and the decision the
work waits on. The obligation points here rather than restating it.

### It is one table wide, and the count that stopped it was a count of something else

The 3.8 attempt stopped on **43 stamp-bounding query sites across 17 shipped-source files**, which is
the size of changing the basis for every stamped table at once. That is not this work. Re-derived
per table, thirteen of the fourteen stamped tables carry a value whose truth is dated by the session
it belongs to, and the stamp on them is doing exactly what a point-in-time bound needs:

- `daily_bar` and `index_bar` are dated by the market through `bar_date`; a bar for a past session
  fetched today is still information the lab did not hold on that session, which is what the bound
  says.
- `indicator_daily`, `setup_signal`, `control_setup`, `forward_return`, `ceiling_bound`, `scoreboard`
  and `setup` hold something computed for a stated session. Recomputing an old session today
  produces a value the session did not have, and the bound is right to hide it.
- `corporate_action` and `history_refetch` record when the lab learned something it genuinely could
  not have known earlier.
- `detector_error` and `scan_hit` are dated by the session they were produced for.

`security` is the exception and is the whole of it: `sector`, `industry` and `market_cap` are
resolved on demand, stamped with `sector_resolved_at`, and **the table has no session-date column at
all**. That absence is the conflation rather than a consequence of it.

**Five files read that stamp**, being `SecurityReader`, `SectorResolver`, `ThemeClusterer`,
`CheckRecomputer` and `SessionFigures`. That is the blast radius, not seventeen.

### What a session-date column would mean here

Not a session date. An attribute that is true over a span needs the date **from** which the lab is
willing to assert it, which is a different thing from the date it was asked on and a different thing
again from the session that asked. The three candidates, in increasing cost:

**Assert it from the date the scan first surfaced the name.** The lab asks about a name because a
scan hit it, and that hit is dated. `security.first_seen` already holds a date of that shape for
2,083 rows. Cheapest, and it claims the least: the attribute is asserted from the first session the
lab had any reason to hold it.

**Assert it from a validity date the vendor states.** The fundamentals endpoint carries an updated-at
field the lab does not currently read. Truer, and it costs a re-capture and a re-derivation of the
fundamentals expectations, of which there are 121.

**Assert it from the beginning of the record.** A sector is stable over years and treating it as
timeless is defensible for this lab's horizon. Cheapest of all and the least honest, because it
would make a name resolved next year visible to a session two years ago.

### What happens to rows whose instant was never recorded

**2,083 securities, of which 234 carry a resolution and 1,849 have never been asked.** The 1,849 need
nothing: they carry no attribute, so there is no value to date. Of the 234, one is a name the vendor
holds nothing on, stamped so it is never asked again, and it carries no attribute either.

So the backfill is **233 rows**, and every one of them has a first scan hit whose date is recorded.
There are no rows whose instant was never recorded, which is the condition that made this look like a
migration with an invented backfill in it.

### What it costs downstream

**29 frozen signal values** would change basis: `industry`, `market_cap` and `cluster_count` are
recorded on the 29 setups of 2026-08-27 that had a resolved sector when the vectorizer ran. Those are
frozen by design and would be recomputed under the new basis, which is a correction under the
lateness rule and records prior state like any other. The other 15 of that night carry the repaired
cluster and no industry, and would gain one.

Every fixture expectation resting on a `security` bound would be re-derived. Today that is the
`fundamentals.*` set, 121 expectations, all `DERIVED`.

### The decision, which is the operator's

**Before or after phase 4.** Stated here rather than assumed, because the two are not the same
project.

**If it lands before**, phase 4 starts against a store whose one lazily-resolved attribute is dated
the way every other value in the lab is dated, and 29 frozen signals are corrected once, while
nothing is trading. The cost is a phase-4 start delayed by the work above.

**If it lands after**, phase 4 records orders against setups whose `industry` and `market_cap`
signals rest on the current basis, and the correction then has to reach rows an order was placed
against. Nothing in the corpus permits rewriting a plan, so those setups would keep the basis they
were flagged under and the record would carry two bases with a date between them. That is not fatal
and it is not free: every figure computed over the boundary would have to state which side of it the
rows came from, which is the population rule applied to a basis change rather than to a filter.

**The conservative answer is in force until it is answered**, which is that the stamp is the basis and
a late resolution is invisible except through the lateness bound.

## Carried obligations

Findings that did not block a checkpoint. Each names the checkpoint at which it falls due.

| Raised | Obligation | Due at |
|---|---|---|
| 3.12 | **A phase branch that has gone green stays checked out, so the nightly runs from it rather than from `main`, and two sign-offs said so without either becoming a row.** The 3.7 sign-off of 2026-08-27 recorded that merging PR #4 leaves "the working tree the tasks run from" still on `phase-3-measurement`, and called the checkout an act only the operator can take. The phase 3 sign-off of 2026-08-29 recorded the branch 18 commits ahead of `main` with every slot of 2026-08-28 running from it at six different commits during one night, which is the night the lab flagged nothing. Neither reached this table, so the one place anything reads for work carried no trace of a hazard raised twice. The merge at 3.12 discharges the instance and not the mechanism: `tools/nightly.ps1` records the branch and the commit on the first line of every night's log, nothing reads that log, and a guard that refused an unexpected ref was written at 3.6 and removed because it would have stopped accumulation on the day phase 3 merged. What is owed is the decision between leaving it to the log and giving the slot a ref it expects, taken once rather than re-argued at each sign-off | 4.1 |
| 3.12 | **`foreign_key_check` runs after the migration it checks has committed, so it reports the damage rather than preventing it.** `MigrationRunner.Apply` commits each migration's transaction and then asks `foreign_key_check` of the whole store, and the message it throws says as much outright: the migration has committed and the store needs the snapshot taken before it. `foreign_key_check` is a read and answers inside a transaction, so asking before the commit would let a rebuild that orphaned rows roll back instead of leaving the operator a restore. Not changed at 3.12, and the reason is that it is a change to the repair path made in the pass that was repairing with it: every migration would gain a new failure mode at once, on the same night a store was being recovered by hand, and the recovery the current shape depends on exists and is what the RUNBOOK already runs before every migration | 4.1 |
| 3.12 | **The band's schema-mismatch line is shown only when the read surface answers, and it is the class of fault that stops it answering.** `LabStatusView.SchemaBehind` is `Store == "ready" && SchemaVersion != SchemaVersionExpected`, and `LabStatusView.Down` constructs the unreachable state with both version fields nought, so a `/status` that could not answer produces a view positively stating no mismatch rather than an unknown one. It is latent today only because `LabStatus.Read` happens to select from `run_log`, `universe_snapshot`, `universe_member` and `daily_bar` and no column a recent migration added; the next migration touching one of those takes the reporter down with the thing it exists to report, and the band then reads "the read surface is not answering" without the line saying why. The same shape as a correct answer discarded downstream, arrived at from the other end: here the answer is never computed and the fallback asserts the negative | 4.1 |
| 3.12 | **Two doc comments state behaviour the code does not have, and both were written in the checkpoint that recorded the behaviour correctly elsewhere.** `LabStatus.LatestRun` says "the band names the stage that went wrong rather than the stage that went last", where the read orders by `started_at DESC` within one outcome and names the last stage to reach it, which is the row above this one; its neighbouring sentence "the ordering was right and the population was a different night" is half of the same overstatement. `MigrationRunner.Apply` says its `throughVersion` bound "exists for one caller", where 3.12 added `StoreVersionGuardTests` standing a store up one migration short through the same parameter. Neither is a defect in behaviour and neither blocks: the first cannot be corrected before the tie-break question above is answered, because a corrected sentence would state a behaviour 4.1 may change, and the second travels with it rather than being swept alone. Recorded because a comment asserting the opposite of the code is the same instrument fault as a check asserting less than its name, in the one place nothing scans | 4.1 |
| 3.12 | **A night recovered after its scoreboard has run leaves that night's panels stating the night that was lost.** `ScoreboardBuilder` inserts `ON CONFLICT DO NOTHING` and has no update path, so rebuilding a past date writes nothing and refuses, which is 3.9(e)'s guard working. The consequence is that 2026-08-28's band 0 still reads `nightsRecorded` 1 and `setupsOnFile` 44, computed before the 73 setups of that night existed. The next night's panels are right and the stale ones are a dated measurement, so nothing is wrong in the store; what is missing is a supported way to rebuild a date's panels, and the stage's own message offers deleting them, which no declared writer does. | 4.1 |
| 3.12 | **The degraded mark's window is the session's calendar day, so a repair of the previous night falls inside it.** `RunLogger.IncompleteStagesOf` bounds from midnight in the session zone to the last instant of the same day, and the nightly for session N runs in the evening of N, so a stage rerun in the early hours to repair session N-1 is inside session N's window. Every setup recovered for 2026-08-28 carries `recheck` in its mark, from the partial recheck at 00:23 Eastern that morning repairing the fifteen cluster verdicts of 2026-08-27. The mark is not wrong by its own definition and it names a stage that has nothing to do with the night it is written on, which is a different statement from the one a reader takes from it. | 4.1 |
| 3.12 | **The status band names the last stage to reach the night's worst outcome, not the first.** Within one outcome the read orders by `started_at DESC`, so on 2026-08-28 it named `vectorize` where `detect-long` had failed five minutes earlier and the other three failed because it did. The cause is what an operator needs and the consequence is what the band shows. Not changed at 3.12 because the clean case wants the opposite ordering, the last stage finishing clean being what says the night reached its end, so the rule is not one sort and the question is which reading each outcome deserves. | 4.1 |
| 3.12 | **`surface-claims` reconciles its claim file against the pages and nothing reconciles the corpus against the claim file.** Named as the check's missing direction at 3.7 and exercised twice at 3.11, which put the degraded mark on the gallery and the population on the decile panel and declared neither: both were true of the pages and held by nothing, and 3.12 added them by reading the commits rather than by anything failing. A sentence claiming something is shown is guarded only if somebody remembers to declare it, which is the shape of the defect the check was built for, one level up. | 4.1 |
| 3.11 | **The dispersion behind the 262-observation minimum is measured over a population the minimum does not govern.** `ForwardDispersion.Of` pools the cross-sectional variance of every name with history; the figure it produces is the bar for the effective count of flagged setup paired differences, and a flagged setup has cleared `moves-enough` at ADR at or above 5%, which is a hard volatility selection. Since the sample size goes as the square of the dispersion, measuring it over the wider population understates the minimum. The arithmetic reproduces exactly and the input is over the wrong rows. `DECISIONS.md` argues the figure is a floor on survivorship grounds; this is a second, unnamed term pushing the same way. A ruling rather than a repair, because it moves the number 3.6 turns on. | 4.1 |
| 3.10 | **The seven landed checkpoints that contributed no fixture expectation at all.** `fixture-replay` grouped the expectations to ask done condition seven, so a checkpoint with none was not a group and was never asked: 1.1, 1.2, 1.11, 2.1, 2.12 and 3.7 each landed without contributing, and 3.10 does the same. Some are plausibly expectation-free, being solution scaffolding, a sign-off or a pass over documents, and none of that has been established. What is owed per checkpoint is one independently produced expectation or a recorded reason it can have none, not a blanket permission. The permits in `fixtures/expectations.json` name this row and fall with it. | 4.1 |
| 1.5 | **The empty tool set on the subscription path holds only as long as the test asserting it does.** It is a property of configuration rather than of there being no mechanism, so nothing structural prevents a later change handing the researcher seat a tool. Recorded in the phase 1 corrections entry and never given a row here, which is what `carried-obligations` found the moment it started reconciling: 6.5's own done condition already names the test, so the obligation was scheduled in substance and invisible to the table that is supposed to be where obligations live. | 6.5 |
| 1.6 | One `CONFIRMED` value per hand-picked ticker, checked against a charting platform's own readout. The derivation at 1.6 is a second implementation by the same author, which rules out a transcription error and not a shared misreading of a definition. The page that makes the comparison possible exists as of 1.10: `/chart/IESC` shows the three averages and the figures the lab stored, on the adjusted basis, in the two decimals a platform shows. What is left is a person reading a platform and writing the three figures down, which is the one step in this corpus a build session cannot do for itself. Moved from 1.12 to 2.11 at the 1.12 sign-off: it cannot be discharged by any build session, and holding a phase on an act no session can perform makes a due point permanent rather than pending. 2.11 is the first thing that reads these figures, since calibration is where a threshold is set against them. **Moved to 2.12 at 2.11**: the calibration ran and set no threshold, because the count distribution showed the band is out of the five thresholds' reach, so nothing yet rests on these three figures. 2.12 is where a person is already reading a screen. **Moved to the operator at the 2.12 sign-off**: it has now been moved three times, each time for a good local reason, and a due point that moves with every sign-off is permanent while reading as pending. It is an act no session can perform, which is the same shape as step 6 of the move and the vendor's reset boundary below, and those are the rows whose due point is honest | the operator |
| 2.11 | **The threshold adjustment the count distribution calls for, which the five thresholds cannot deliver.** The live run measured a median of nought candidates a night on both sides against a band of 5 to 60, so an adjustment fires. It cannot be a nudge, and the two sides fail the same way, which is what makes it structural rather than a long-side accident. The recording floors are healthy: a median of 44 long rows a night and 13 short over 2,016 names. The pattern test then admits 7 long rows in 631 sessions and no short row at all, and the same two shapes account for both. **The retrace cap**, 0.40 on both sides, against a median give-back of 1.060 long over 5,849 dips of 2 to 7 bars and 1.006 short over 3,448 bounces of the same length: the typical pullback gives back the whole thrust it is measured against. Both figures restated at the 2.12 sign-off, where the long one was found to have been taken over dips of two bars or more with no upper bound. **The give-up cap**, 0.5 daily ranges on both sides, against a median stop distance of 1.157 long and 1.191 short: a stop at the extreme of a two-to-seven bar move is being asked to sit inside half a day's range, which the geometry cannot produce. `trigger-near` is the counter-example that rules out "everything is too tight": its cap is 1.5 ranges against a median of 0.513, and 96% of measurable rows clear it. With the two shapes set to pass always, the remaining conjunction recounted at the 2.12 sign-off yields a median of 7 rows a night long and nought short, against a band floor of 5. So the band is out of reach on the long side by a hair and on the short side by an order of magnitude, and it is reachable at all only by removing two checks from each list. That is a change to what the strategy is rather than a calibration of it. Two readings, and the choice is not a build session's: the thresholds are wrong, or the quantities they are applied to are. The second has a specific candidate, which is that the geometry measures the thrust from the session before the scan hit, the right origin for a one-day scan and the wrong one for a twenty-session scan. That candidate is now a row of its own, raised at 2.11 on where the thrust is measured from, carrying the prediction it is judged against. **The two are worked in order rather than together**: a pass that moves the geometry and the thresholds at once can say nothing about either, and the adjustment here is once-only. Due before 3.1, because phase 3 must not start recording against thresholds nothing has calibrated **Judged at 3.0(c) and still open.** The corrected geometry moved the retrace half of the prediction and not the count half: the median falls from 1.060 to 0.5208 long and 1.006 to 0.4568 short over dips and bounces of 2 to 7 bars, and the nightly candidate count stays at a median of nought on both sides, 30 in total long over 602 sessions and nought short. So the geometry was one fault and something else holds the count down. **The once is unspent**, because a count five hundred times below the band's floor argues for a second wrong quantity rather than for thresholds set too tight, and the once cannot be re-spent. **Due at the operator**, on the same terms as the other three: it is a ruling rather than work, no build session can take it, and a due point that moves to the next checkpoint at every sign-off is permanent while reading as pending | the operator |
| 2.9 | **The `CONFIRMED` gallery expectations.** The tier the corpus defines as what the chart page and the setup gallery produce has no entries at all, and no build session can make one: it is a person opening the gallery, reading a night, and writing down what they saw. The page is built and every property a session can assert about it is asserted, so what is left is the looking. Raised due at 2.12, where the gallery review is part of the sign-off. **Moved to the operator at the 2.12 sign-off**, which did not perform the review and says so: a sign-off session cannot page through a gallery either, so leaving this at a checkpoint would move it again at 3.7 | the operator |
| 2.11 | **The source-scan assertions nothing exercises, and the three scans written outside a check.** The sweep run at 2.11 found thirteen assertions the checks make by reading the shipped source: nine are exercised by a behavioural test, one by the rehearsal job, and three by nothing. The three are `writer-ownership`'s attribution of every write to its declared writer, whose behavioural form is `order-provenance` and starts at 4.6 for orders alone; `architecture-conformance`'s catalogue registration, where a registration present and unreachable would pass; and `coverage-reported`'s own scan for the trait, which rests on the phase report's coverage-record requirement rather than on a test. Three more scans live in ordinary tests and have no coverage record to declare a backing in, so the sweep can say they exist and not whether anything backs them: `SignalVectorizerTests`, `RegimeLabelerTests` and `SharedCandidateListTests`. Reported rather than fixed at 2.11 on purpose, because the fix is a behavioural test per scan and doing that in the pass that found them is how a sweep turns into a checkpoint. Due at 3.1, whose deliverable is the same kind of assertion: what a setup row may and may not have rewritten **A live instance was found and shipped at 3.12, which is an argument for pulling this earlier rather than a change to it.** `architecture-conformance`'s store-version claim read `Program.cs` for three patterns and every one of them is satisfied inside `WhyThisStageCannotRun` and `WhyTheStoreCannotBeRead`, which are declared in that same file. Deleting the block at the top of `Main` that calls the guard before the dispatch left all three patterns in place, the claim green and a detector free to run against a store two migrations behind it; the deletion was run and the claim did stay green until the claim was replaced. Its verdict is now a detector run through the CLI against a store one migration short, and both it and the new test fail on the call site alone. What that instance adds is that the sweep counted assertions whose subject is a declaration and this one's subject was a call, which no reading of the file distinguishes: an unbacked scan is not a list of thirteen risks of equal size, it is a list on which at least one had already failed | 4.6 |
| 1.1 | **Whether the vendor's own quota resets on the UTC date the lab counts against.** The daily call budget counts calls against the UTC date and the vendor's reset boundary is assumed rather than confirmed. Raised at 1.1, restated at 1.3 as still open, and never in this table either. If the two boundaries differ the lab can believe it has a full ceiling while the vendor has already begun refusing, which reads as a partial run rather than as a wrong assumption. It cannot be settled by a build session or by a sign-off session: it is a question for the vendor or an observation of one night's refusals. Due at the operator, on the same terms as step 6 of the move | the operator |
| 2.9 | **Which clause of a multi-clause gate failed.** Raised by the gallery review, whose first night produced two defects in how a check's number reaches the screen; both are fixed, and this one is not a defect fix. `CheckResult` carries a single `Value`, so `tradable-shortable` tests four floors and records one number. The screen can now say which clause the number came from and cannot say which clause the gate failed on, so a failing `tradable-shortable` tells a reader nothing about whether it was turnover, price, capitalisation or listing age. The same holds for `tradable` and, in a smaller way, for `dip-shape`, whose bar count lives in a note beside a value that is the retrace. Fixing it means a result carrying a value per clause, which changes the shape of the JSON in `check_results` and moves the `setup.*` and `check.*` expectations at 2.6, 2.7 and 2.11. That is a checkpoint's work rather than a fix, and 3.1's deliverable is the neighbouring one, being what a setup row may and may not have rewritten **Repointed once, at 3.0, from 3.0 to 4.1, with the reason.** It is checkpoint-sized by its own admission and it is not blocking: it moves the frozen `check_results` shape, so it moves expectations at 2.6, 2.7 and 2.11 and leaves the 49,450-row calibration store in the old shape until another re-run. 3.0 spent its night on the geometry correction and the obligations that gate phase 3's measurements, and taking this on as well would have cost a second calibration pass for a diagnostic improvement. **4.1 is the honest due point rather than a convenient one**: the watchlist greys a failed check and names the failing check, which is exactly the screen where "which of four floors did `tradable-shortable` miss" is the question a person asks. Moved once and named as moved, because a due point that shifts at every sign-off is permanent while reading as pending | 4.1 |
| 3.3 | **Whether the tight control set may draw from neighbouring sessions.** The tight set is declared to match on the trend ladder **and the market mood**, and within a night the mood cannot be a dimension: it is a property of the session, so every candidate carries the same one and matching on it excludes nothing. The draw leaves it out rather than implementing a comparison that is true by construction, because a dimension that always matches reads in the record as a dimension that was checked. Making it real means letting the tight set reach into sessions with the same mood, which trades a matched dimension for a comparison across time and is a change to what the tight number means rather than to how it is computed. **Due at 3.5**, where the scoreboard first reports the tight comparison, because that is the panel whose meaning the answer changes **Reached 3.5 and repointed once, to the operator.** The checkpoint built the panel that reports the tight comparison and did not decide what the comparison should be, which is the honest split: letting the tight set reach into other sessions trades a matched dimension for a comparison across time, and that is a judgement about what the number means rather than about how to compute it. It sits with the threshold ruling for the same reason: no build session can take it | the operator |
| 3.6 | **The nightly job runs only while the user is logged on.** Seventeen Task Scheduler entries were registered on 2026-08-27 and the scheduling obligation they discharge is closed. What is left is narrower and is a property of how they had to be registered: an unelevated shell cannot set an S4U principal or store a password, so the tasks carry an `Interactive` logon type and a logged-out evening is a lost night. **That is not a night like any other.** Every other stage can be rerun for its date, but `universe-build` writes the snapshot of who was listed, `UniverseSnapshotReader.Members` matches the date exactly with no fallback by design, and a delisted name is simply absent from tomorrow's symbol list. So a logged-out evening is a permanent hole in the evidence rather than a delay. Closing it is one elevated command to re-register with an S4U principal, which no build session can run | the operator |
| 3.5 | **The `band0.degradedRuns` panel states three different populations and reads red nowhere.** The figure counts distinct non-clean run instants, the count beside it is the number of nights, and the population label says "runs recorded". None of the three is the denominator of the other, so a reader cannot form the ratio the caption asks for. `ARCHITECTURE.html` says the panel "reads red if degraded nights exceed 5% of the record"; the caption is a static string and nothing computes the ratio or renders anything red. Fixing it means deciding which population the panel is over and saying so, then making the threshold a computed state rather than a sentence | 4.1 |
| 3.5 | **`ForwardReturnFiller` stores nought where an excursion is undefined.** `ForwardOutcome` returns null for MFE and MAE when the subject has no ATR, and the insert coalesces both to `0`. A row that could not be measured is then indistinguishable from one whose path never moved, and `mae_atr` of nought reads as a subject that never went against its entry. `WinRateCeiling` happens to be safe because it refuses a subject with no range before it reads the excursion, so this is a latent conflation rather than a live defect, and closing it means a nullable column and a migration | 4.1 |
| 3.5 | **`intended_date` is a calendar step, so it differs across any weekend rather than across a holiday.** `SCHEMA.md` says the two dates "differ across a holiday" and the counter is named `AcrossAHoliday`, but the intended date is the as-of plus the horizon in calendar days, which for a horizon of 3, 5 or 10 is almost never a trading day. A synthetic run with no holidays in it counted 7,392 of 10,560 rows as slipped. The console line is worded accurately and the column is useful; what is wrong is the name and the sentence beside it, and the fix is to decide whether the intended date is a calendar step or a session step and say which. **It is also the one counter on `FillResult` still pooled across both subject kinds**, where the five beside it were split when the control fill landed, so it is where the population rule gets applied to this figure as well | 4.1 |
| 3.5 | **Ratios are read back through the price crossing.** `StoreText` names two crossings for the same operation and says the naming is what stops a percentage being written where a fraction was meant. `ScoreboardBuilder` reads `return_signed`, `bound` and `achieved` through `StorageTextToPrice`, and `CeilingCalculator` reads `return_signed` and `mae_atr` the same way. The values are identical either way, so nothing is wrong today and the convention the rule rests on is already half broken | 4.1 |
| 3.5 | **`CeilingCalculator`'s insert comment says it replaces its own row and the statement does nothing on conflict.** "A week recomputed replaces its own row and no other" sits above `ON CONFLICT (as_of, direction) DO NOTHING`, so the first write of a week wins and a recomputation is discarded. The behaviour is probably the right one and the comment is wrong, but which of the two is intended is a decision rather than a typo, because the bound is stated as recomputed weekly | 4.1 |
| 3.5 | **Nothing reconciles the `surface-claims` stub against the text its producer emits.** The check renders authored JSON, so a claim whose `mustCarry` text is also written into the stub proves only that the template does not swallow the string. If `ScoreboardBuilder`'s wording changes and the stub does not, the check stays green while the page carries different words. Raised while adding exactly such a claim: its `mustCarry` was reconciled against `accumulation.starved.withheldBecause` by hand, which is the thing that should not be by hand | 4.1 |
| 3.5 | **`SetupJournalTests` claims the immutability property is asserted four ways and holds two, both source scans.** Its own summary names a rerun writing nothing and the store's key refusing a second write; neither exists for `setup`. The equivalents exist for `setup_signal`, which is the pattern the file says it copies. The property does hold, because the detectors insert `ON CONFLICT (setup_id) DO NOTHING`, so what is missing is the behavioural half rather than the behaviour. 3.1's done condition asks for the property to be asserted in both directions | 4.6 |
| 3.5 | **The minimum sample of 262 was sized for a normal-theory test and the test actually run is a studentised bootstrap.** `MinimumSample.Of` is `n = ((z_alpha + z_beta) * sigma / delta)^2`, which is the sample size for a z-test. What band 1 reads is a bound from a studentised moving-block bootstrap over `nights / 10` blocks, which is two blocks at the twenty-session floor and five at fifty-two, and its tails are wider than the normal's. So reaching 262 effective observations does not deliver the 90% power the figure was ratified at, and the correction widens rather than narrows the requirement. **The direction matters**: it is under-power, so the risk is reading a flat band 1 as "the pattern has nothing in it" when the sample could not have found it, and CLAUDE.md's own reasoning for choosing 90% over 80% is that a false negative here has no downstream. The fix is a power calculation against the procedure actually run, and the two inputs it turns on are judgements this corpus says belong to a person, so it is raised rather than taken. Surfaced by the sign-off review's own re-review of the interval replacement | the operator |
| 3.5 | **`ControlSampler` can draw a name that did not trade on the night it is drawn for.** Eligibility is an `indicator_daily` row at the as-of, and `IndicatorEngine` writes one for a universe member whose last bar is older than the session, because `DailyBarReader` takes the last 150 bars at or before the as-of without requiring one dated on it. So a halted or vendor-omitted name is indicated, is drawable, and becomes a control. The outcome is no longer mis-measured, because the fill now refuses a subject whose window does not start on its own session and counts it, but the set is then thinner than five and nothing says which name went missing or why. `TierClassifier` already refuses a name whose latest bar is not the as-of session, which is the shape the draw wants. Found by the review of the control fill, which reproduced the whole chain through the shipped stages | 4.1 |
| 3.5 | **`ForwardReturnFiller` re-walks every subject ever recorded on every night, so its cost grows with the square of the accumulation.** Both subject queries are bounded only by `s.as_of <= @as_of`, with no lower bound and no filter for subjects whose four horizons are already written, and the fill issues one path query per subject per night. Every already-written row is recomputed and discarded by `ON CONFLICT DO NOTHING`. At about eighty-two flagged setups a night and ten controls each, night 200 walks roughly 180,000 subjects to write the few thousand that closed. Correctness is unaffected and the immutability rests on the key rather than on the query, which is why this is a cost rather than a defect. The fix is a lower bound on the as-of, or a left join excluding subjects already complete | 4.1 |
| 3.5 | **A subject whose horizon can never close is counted as "not yet elapsed" for ever and drops silently out of the control mean.** `ForwardOutcome.Of` returns null both for a horizon that has genuinely not elapsed and for a subject whose own series is too short to ever reach it, which is a control delisted, halted for a run of sessions, or dropped from the universe so no more bars arrive. The two states are indistinguishable in the counter and the control simply contributes nothing, so a setup whose controls were the ones that got halted is compared against however many survive without anything saying the comparison narrowed. The new `WithoutABarOnTheirOwnSession` counter catches the near neighbour of this, being a subject with no bar on its own session, and not this | 4.1 |
| 3.5 | **`interval.*` is tiered `DERIVED` over a restatement that now mirrors the shipped algorithm closely enough that the tier claims more than it holds.** Reproducing a seeded resampling scheme to four places requires the same generator, the same seed, the same block order and the same tail convention, so `tools/derive-indicators.py --interval` is close to a transcription and agreement between the two says little beyond that neither was mistyped. That is exactly how the defect it replaced survived. The property tests in `PairedIntervalTests` are what actually verify the scheme now, and the question this raises is whether a tier that reads as independent verification should be claimed for any figure produced by a seeded algorithm, or whether such figures want a tier of their own | 4.1 |
| 3.6 | **`tools/nightly.ps1` has no macOS counterpart and rebuilds from the working tree on every slot.** The corpus pairs `ci.ps1` with `ci.sh` because the two platforms need different syntax, and the nightly runner has only the Windows half while the move to the Mac is still an open obligation. It also invokes `dotnet run` without `--no-build`, so a slot compiles the working tree at 17:15 and a half-finished edit costs the night | the move |
| 3.8 | **`rows_written` distinguishes nothing on a stage that only updates, and two stages are now in that position.** `RunScope.Complete` measures it as a row-count delta over the tables the stage declares, so `sectors` and `clusters`, which issue `UPDATE` and never `INSERT`, report 0 rows on a perfect run and 0 on a run that died after 149 calls. `CheckRecomputer` joins them. 3.8 closed the half that mattered on the night by adding `run_log.skipped` and by making a run that did not finish its list `partial`, so a reader is no longer told nothing at all. What is left is the column itself: it is measured, it is what the nightly halt keys on, and on these three stages it is measuring the wrong thing rather than measuring nothing. The fix is either a stage reporting rows affected where the delta cannot see them, which breaks the rule that the figure is measured rather than self-reported, or a column that says the delta does not apply here. Which of the two is a decision rather than a repair. Due at 4.1 with the other band 0 item, because the status band is where a reader forms the impression this figure gives them | 4.1 |
| 3.8 | **`security.sector_resolved_at` is when the lab asked, and every reader treats it as when the fact became true.** The two are the same for a bar, whose observation is dated by the market, and are not the same for an attribute looked up lazily: a sector is true for years before anyone asks. **Attempted at 3.8 and stopped; scoped at 3.9(h) and still not started.** The plan is "The lazily-resolved attribute, scoped and not started" above, which carries the counts, the three ways the attribute could be dated and their costs, and the before-or-after-phase-4 question. It is not restated here, because a row and a plan that both describe the work are two places one decision lives. What the row carries is the due point: the operator, because it decides whether a night's evidence may be completed after the fact, and the conservative answer is the one in force | the operator |
| 3.8 | **The slot script depends on Windows PowerShell and two of its parts behave differently under pwsh.** `tools/nightly.ps1` is the Windows half of a pair whose macOS half does not exist, and the machine it runs on executes `powershell.exe`. Two parts turn on which interpreter is running. `Invoke-Stage` exists because Windows PowerShell wraps a native command's stderr in a NativeCommandError under `Stop`, which pwsh does not do, so under pwsh the function is harmless and unnecessary rather than load-bearing. And the exit-code handling turns on `$LASTEXITCODE` surviving a pipeline, where pwsh 7.4 additionally raises on a native non-zero exit under `Stop` through `$PSNativeCommandUseErrorActionPreference`. The `slot-diagnostics` job now runs the script under both and prints what each did, recorded rather than required, because requiring them to agree would fail for a reason nobody has decided and pinning one would assert something about an interpreter the scheduler does not use. **First reading, 2026-08-28**: with the fix in place both interpreters keep the message and the stop line and both exit 1, so they do not diverge on the outcome. They diverge on whether `Invoke-Stage` is load-bearing, which is the thing a macOS counterpart would have to decide about rather than inherit. What is owed is that decision, at the point the counterpart is written, since that is when a second interpreter stops being hypothetical | the move |
| 1.11 | **Step 6 of the move procedure only, copying `appsettings.Secrets.json`.** The rest of the rehearsal now runs on every push: the `rehearsal` job on `ubuntu-latest` exercises the whole pipeline on a case-sensitive filesystem, which is the fault class neither development machine can see, then copies the store the fixture replay built and verifies it on arrival with counts and an integrity check. What a container cannot do is carry a gitignored file between two machines by hand, which was only ever verified in the negative: confirmed not to travel with a clone, never confirmed to have been copied. Due at the actual move, not at a checkpoint | the move |
| 3.7 | **Nothing reconciles SCHEMA's column tables against the migrations, and five columns are already missing.** `writer-ownership` reads SCHEMA for writers and `SchemaDeclarations.TablesInMigrations` for which stores exist, and the columns each `### `table`` section lists are read by nothing at all. Measured at the 3.7 sign-off against the migrated store, discounting the two sections that legitimately say "same shape as" another table: `scoreboard` omits `computed_at` and `withheld_because`, `control_setup` omits `drawn_at`, `ceiling_bound` omits `computed_at`, and `regime_daily` omits `indexes_above`, which has been missing since 2.5. SCHEMA's second line says "Complete for phases 1 to 3" and `stated-counts` exempts nothing here because nothing derives that claim. What closes it is the reconciliation rather than the five repairs, on the grounds the corpus already gives for `writer-ownership` running both ways: repairing five columns leaves the sixth to arrive unnoticed | 4.1 |
| 3.7 | **`ControlSampler` threads a `drawnAt` into `Figures` that the query ignores.** `Figures(connection, asOf, drawnAt)` takes the draw instant through three call sites and then binds `@drawn_at` to `$"{asOf:yyyy-MM-dd}T23:59:59.999Z"`, so the bound is the end of the session day and the parameter is dead. The end-of-day form is the corpus's own convention and every reader in `PullbackStrategyLab.Data` uses it, so the figure the query returns is right; what is wrong is a signature claiming a bound it does not apply, in the one method whose doc-comment cites (see: A reader's signature does not establish point-in-time; the query does). Bind it or drop the parameter; do not leave the third reading | 4.1 |
| 3.7 | **`price-storage-form` reads only `CREATE TABLE` bodies, so a column added by `ALTER TABLE` is never parsed.** **Half closed at 3.8**: the check now counts the columns it did not examine and reports them out of scope against this row, so its green states its own coverage rather than reading as complete. What is left is the parse. The count rose from ten to thirteen at 3.8, the three being `run_log.skipped`, `setup.corrected_at` and the two correction columns beside it, and all thirteen are `TEXT` or `INTEGER` so nothing is wrong in the store. The regex matches `CREATE TABLE name (body);` and the check counts 23 tables and 162 column declarations. Ten columns in the migrations arrive by `ALTER TABLE ... ADD COLUMN` and are in neither figure: `run_log.counts_against_ceiling`, four on `setup` and `calibration_setup`, two on `universe_snapshot`, and `scoreboard.population`, `n_minimum` and `withheld_because`. All ten are `TEXT` or `INTEGER` today, so nothing is wrong in the store; what is wrong is that the one guard on the storage half of the decimal rule cannot see the statement form a later phase is most likely to add a column with | 4.1 |
| 3.7 | **Eight checks besides `point-in-time` reconcile a hand-named list against the corpus in one direction only, and the missing direction is the one the corpus grows past.** Surveyed at the 3.7 sign-off across every check on the roster. `surface-claims` reads a claim file and never asks which corpus sentences claim visibility; `stated-counts` and `pinned-constants` read authored claim lists and never ask which stated counts and constants have no entry; `fixture-inputs` names three endpoints where the vendor client calls four, `fundamentals` being the fourth and captured only by luck; `bar-append-only` names three bar tables and never asks the migrations for a fourth; `shell-executable` names four entry points and never reads `tools/`, where `derive-indicators.py` already carries a shebang and mode 100644; and `decision-resolves` and `no-superseded-citation` read nine documents and `src/**/*.cs`, where `.github/workflows/ci.yml` and `fixtures/expectations.json` both carry a `see:` citation neither one scans. `coverage-reported`, `check-completeness`, `fixture-replay`, `ci-parity` and `architecture-conformance` already run both ways and are the model. What is owed per check is the second direction or a recorded reason it cannot have one, not eight repairs | 4.6 |
| 3.8 | **The repair made 2026-08-27's `cluster` column a mixed population, and the rows it could not touch are damaged in the same way as the fifteen it could.** The recomputer forms its cluster over the night's whole scan population, which is the right basis and is now asserted. But it runs after the sector walk completed, so the fifteen repaired rows are counted over all **234** of that night's scan names while the other **29** carry what `clusters` computed at 18:15 over the **148** resolved at the time. Measured: all 15 repaired rows match the 234-name count and none matches the 148-name count; 28 of the 29 untouched rows match the 148-name count, and one, `INFQ`, matches neither at 4 against 6 and 13, so a third population exists that nothing has explained. **20 of the 29 would move** if recomputed, and **two would change verdict**: `FOUR` short and `HTFL` long both record a fail at a cluster of 1 and are 2 over the full population, which passes. Those two are the fifteen's own defect wearing a number instead of a null, and they are invisible precisely because a partial input produced a plausible value. `passed_all` is untouched either way, since `cluster` is recorded and never gating. Two things are owed and only the second is a build session's: a ruling on whether a verdict computed from a partial input may be revisited, which the stage refuses by design and the correction rule does not currently permit; and, failing that ruling, any figure stated over that night's `cluster` column reports the split rather than one number (see: Long and short are never pooled into one figure) | 4.1 |
| 3.9 | **The store readers name the session zone rather than reading the configured one.** `StoreText.EndOfSession(date, zone)` takes the zone explicitly and every caller passes one, but a store reader is a static helper with no configuration to read, so nine reader methods across five readers pass `SessionBoundaries.UsEquities` while the ten stage and API sites pass `_options.SessionZone`. Those are the same string today: `PullbackStrategyLabOptions.SessionZone` defaults to the constant rather than restating it, and a test asserts every `appsettings.json` sets that same value, so configuration cannot diverge without the suite failing. **The guard is real and it is a guard rather than a design.** The fix is to thread the configured zone through the nine reader signatures, which reaches 66 call sites, 44 in shipped source and 22 in the suite. It was not done in the same pass as the boundary itself because a signature change on every store read, landed beside a change to what every bound means, would make one of the two impossible to review | 4.1 |

### The eight that are the operator's, and what each one blocks

Listed together because they are answered together or not at all. Nine sessions have each moved one of these to the next checkpoint for a good local reason, and a due point that moves at every sign-off is permanent while reading as pending. None can be taken by a build session: each is a judgement, a reading of a screen, or an act on a machine.

**It was nine until 3.9(b).** The branch-or-`main` question is answered rather than moved: PR #5 merged at `6f27926` and the job runs from `main`. It is named here rather than only deleted, because a list that silently shrinks reads the same as one nobody is working through.

The column that matters is the last one. Five of the eight block nothing at all today and are cheap to leave open; three sit in front of work that is already scheduled.

| Raised | The question | What it blocks | What stalls without it |
|---|---|---|---|
| 1.1 | Does the vendor's quota reset on the UTC date the lab counts against | Nothing today. The nightly job spends about 803 against 5,000, so a boundary six hours out of step still could not exhaust it | Nothing until the budget is close to binding, which no phase currently plans |
| 1.6 | Three `CONFIRMED` indicator values, read off a charting platform | The `CONFIRMED` tier has no entries at all, so nothing in the corpus is verified against anything outside it | Nothing blocks. It is the tier's only possible source and the page that makes it possible has existed since 1.10 |
| 2.9 | The `CONFIRMED` gallery expectations, from paging through a night | Same tier, other half. A person reading the gallery and writing down what they saw | Nothing blocks |
| 2.11 | The threshold ruling: the thresholds are wrong, or the quantities they apply to are | **Band 2 and everything downstream of it.** At a median of nought candidates a night, no trade is ever planned, so phase 4 builds a trading layer nothing reaches. **It does not block 3.6**, corrected at 3.9(i): band 1 counts effective observations over every flagged setup rather than over candidates, so the two are different populations and both figures hold | **Phase 4 is buildable and untestable against live rows.** It is the largest of the eight. The funnel at 3.9(i) names `exit-tight` as the gate the numbers point at, passing 1.29% of 32,533 flagged long rows and 1.37% of 16,917 short, an order of magnitude tighter than the next check on either side |
| 3.3 | May the tight control set draw from neighbouring sessions | The meaning of the tight comparison, which is the number 3.6 turns on | **3.6.** Accumulating against a tight set whose definition may change spends the accumulation twice |
| 3.5 | 262 was sized for a z-test and a studentised bootstrap is what runs | The minimum sample, in the direction of under-power. Reaching 262 does not deliver the 90% the figure was ratified at | **3.6**, but weakly: it moves the threshold rather than the method, and the panel reports the count either way |
| 3.6 | The tasks run only while the user is logged on | A logged-out evening is a permanent hole in the evidence, because `universe-build` writes the snapshot of who was listed and nothing can reconstruct it | Nothing blocks, and it costs a night every time it fires. One elevated command closes it |
| 3.8 | Does a lazily-resolved attribute carry a validity date separate from its lookup instant | Whether a night's evidence may ever be completed after the fact, beyond the narrow case the lateness bound admits | Nothing blocks. The lateness bound reaches the case that mattered, and this is the general question behind it |

**The one to answer first is 2.11**, and it is the only one of the nine that stalls a phase. The other eight cost a tier with no entries, a night lost when somebody logs out, and a modelling question nobody is currently paying for.

---

## Elapsed time

Roughly 35 to 45 coding sessions. The calendar is set by accumulation, not by code: phase 3 runs until band 1 has the evidence to be read, phase 5 needs a few months more before proposals settle, and the loop probably closes once somewhere around month seven.

**Every figure in that sentence is an estimate and none of them is a trigger.** They are here to say roughly how long this takes, which is a fair thing for a plan to guess at. **3.6 fires on a measured sample and nothing else**, and the three months that used to sit in this paragraph is exactly the kind of estimate that got read as a derivation once already.
